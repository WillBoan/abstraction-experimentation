"""Analysis: the read-side instrument — MDL compression metrics over solved corpora.

Consumed by both the learn stack (sleep's governance objective) and the analyze /
study-report layer (EXECUTION.md). Pure measurement; never a training signal.
"""
