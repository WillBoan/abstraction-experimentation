{
  "name": "vscode-ladder",
  "displayName": "Ladder (.ladder)",
  "description": "Syntax highlighting and diagnostics for arc-lab `.ladder` files.",
  "version": "0.2.0",
  "publisher": "arc-lab",
  "private": true,
  "license": "MIT",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Programming Languages",
    "Linters"
  ],
  "activationEvents": [
    "onLanguage:ladder"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ladder.restartServer",
        "title": "Restart Language Server",
        "category": "Ladder"
      }
    ],
    "languages": [
      {
        "id": "ladder",
        "aliases": [
          "Ladder",
          "ladder"
        ],
        "extensions": [
          ".ladder"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "ladder",
        "scopeName": "source.ladder",
        "path": "./syntaxes/ladder.tmLanguage.json"
      }
    ],
    "configuration": {
      "title": "Ladder",
      "properties": {
        "ladder.server.command": {
          "type": "string",
          "default": "uv",
          "description": "Executable that launches the .ladder language server."
        },
        "ladder.server.args": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "run",
            "arc-lab",
            "lsp"
          ],
          "description": "Arguments passed to the language-server command."
        },
        "ladder.server.cwd": {
          "type": "string",
          "default": "",
          "description": "Working directory for the language server (defaults to the first workspace folder)."
        },
        "ladder.server.restartOnSourceChange": {
          "type": "boolean",
          "default": true,
          "description": "Restart the language server when the Python sources its diagnostics come from change. The server snapshots the substrate registry and check plan at import time, so without this a primitive added mid-session reads as unresolved in every open .ladder file."
        },
        "ladder.server.watchGlobs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "src/arc_lab/program_search/substrate/**/*.py",
            "src/arc_lab/program_search/ladders/**/*.py"
          ],
          "description": "Globs (relative to the server's working directory) watched when ladder.server.restartOnSourceChange is enabled."
        },
        "ladder.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Trace the communication between VS Code and the language server."
        }
      }
    }
  },
  "scripts": {
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "typecheck": "tsc --noEmit",
    "vscode:prepublish": "node esbuild.js --production",
    "package": "vsce package"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.75.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0"
  }
}
