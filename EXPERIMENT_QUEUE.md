# Experiment queue

Planned experiments for arc-lab — a **drain-only queue**, rough priority order, top = next. This is _state, not events_ (the counterpart to `EXPERIMENTS.md`): it holds what we currently intend to run, and it only shrinks or gets deliberately re-fed.

**Priority spine (2026-07-14):** single-task **search-behavior** probes (read the search-cost graph per task) → **fundamental-floor grain contrast** → invention on **designed families** (gap-climbing / real-ARC) → downstream: selection-correct governance _(deferred)_, basis/ablation science. Full-corpus runs are a **solve-count gauge**, not the object of study — we run **1 task** (or a small designed handful), not 400. Two sections: **Active** (next runs) then **Backlog** (deferred / downstream).

Floors are named per [substrate/primitives/_PRIMITIVE_BUNDLES.md](src/arc_lab/program_search/substrate/primitives/_PRIMITIVE_BUNDLES.md) (coherence-checked in `execution/bundle_sheet.py`).

**Discipline** (or this rots like its predecessor):

- **Drain on run.** When an experiment runs, its `EXPERIMENTS.md` entry is the record — delete the Active row in the same session. Rejected without running → delete too (log a dead-end entry first if the reason is itself a finding).
- **Unit of analysis.** A SEARCH run executes the _same_ search independently on every task; search _behavior_ is fully observable on **one** task (the always-on tracker + `--track-all` give the whole round-by-round object). So search-behavior probes run on **1 task**, or a small handful stratified by size/type. Invention is intrinsically cross-task → a small **designed family** (~5-8 tasks sharing structure), never the full corpus.
- **Active vs Backlog.** Active = intend to run next, specced enough to start. Backlog = real but deferred or gated; promote when next and fully sized. Backlog only shrinks (run/reject) or gets promoted.
- **No IDs.** E-numbers are minted at run time, in the log — never here. Rows get names, not numbers.
- **Experiments only.** Machinery-level work is never queued here — `MACHINERY.md` tracks it (Status / Gates). The **Unmet machinery** column links each experiment to the not-yet-built mechanisms that gate it.
- **One row's worth of sizing.** The columns _are_ the sizing frame; full design is still design-time work for the experiment about to run.

---

## Active — next runs

_Search-behavior probes read the search-cost graph directly (what gets tried, where budget goes, where search walls) — no solve-count fixation. Invention experiments need a small designed family._

| Experiment | Type | Starting primitives (floor) | Target / abstractor demands | Corpus / task | Params | Metrics / what to look for | Unmet machinery | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **grain spine (cost-vs-grain curve)** | search (single-task) | `FLOOR → FLOOR_AFFINE → FLOOR_DIV → UNIVERSAL_FLOOR` (the pixel lattice), fixed target | — (SEARCH) | 1 designed multi-shape geometric task (rot180 / a reflection, as `build_grid`) | `fill=lambda-synthesis`; const per floor (none for FLOOR*, finite for UNIVERSAL); beam sweep to find the cliff; `--track-all` | the cost-vs-grain _curve_ for one target; partition per rung; where the reflection sits in the cost ranking (the E7–E9 beam cliff, re-read on the new instrument) | `search --bundle`; designed generator | Samples the continuous curve the row above samples at two points; validates the instrument against a target ladder we already know. E13's `universal-floor`/`geom` presets + `grain-contrast` generator are directly reusable here |
| **control-floor cost concentration** | search (single-task) | `UNIVERSAL_FLOOR` (CTRL-heavy) | — (SEARCH) | 1 designed per-cell **decision** task (threshold / conditional recolor) | `finite-enumerate`; `fill=lambda-synthesis`; watch `eq` over same-typed pairs | which keys dominate `considered` — confirm `eq`/`__if__` **O(pool^2)** blow-up; the concrete partition that **motivates E4 machinery** (commutative pruning, per-round cost budget) | `search --bundle`; designed generator | The B1 probe most likely to _produce a machinery finding_ |
| **search-behavior size-scaling** | search (handful) | one floor (e.g. `UNIVERSAL_FLOOR` or a gifted floor), one target type | — (SEARCH) | a handful: the same task type at ~3 grid sizes (e.g. 5x5 / 12x12 / 25x25) | `harvest` **vs** `finite-enumerate` contrast (the constant-explosion _is_ the point); fixed budget across sizes; `--track-all` | how `considered` + pool growth + partition scale with grid size; where `finite-enumerate` explodes (the ~6M-candidate mechanism, isolated) | designed multi-size tasks (`taskgen`); `search --bundle` | Isolates cost-vs-grid-size — the mechanism behind why the full corpus is expensive and why `harvest ≠ finite` matters |
| **learned intermediate type** | study | `MASK_MIN` (`nonbg_mask`, `crop_to_mask`) | `crop_to_content(g)=crop_to_mask(g,nonbg_mask(g))`; demands var-sharing (grid ×2) + abstraction over the **Mask** type | new synthetic family (~5-8, varied bg + content) + heldout | `constant_sources=()`, deep depth3 / shallow depth2, arity 2, `AntiunifyPairs`, `iterations=5` | shallow `L1` 0 → `L2` all (enablement) + held-out transfer; the construct-**and**-consume-a-learned-type phenomenon | none (Mask + both prims shipped; the generic engine pools any hashable typed value); needs a `taskgen` generator + `StudySpec` | Deepest gap-climbing, runnable today — the synthetic invention companion to the probes above |
| **point the loop at real ARC** | learn | `SYMMETRY` (alt: `ATOMIC`) | observed — no known target; demands a **cross-task recurring subtree** | a **small curated** `arc1-train` family that shares structure (e.g. symmetry-repair) + `arc1-eval` heldout — **not** the corpus | `harvest`, depth 3, arity 4, pool 500; `GreedyMDLLearnEngine(FrequentSubtree(min_frequency=2))`, `TwoPartMDL`, `iterations=3` | does anything mint at all? train↔eval transfer; partition/speedup before→after. **A null result answers the open "does the bootstrap climb on real ARC"** | corpus curation (a coherent family's task-ids); `learn --bundle` or a `Config` carrying a `FrequentSubtree` LearnSpec | Real-ARC contact via a _designed family_, not 400. `FrequentSubtree` not the default `AntiunifyPairs` — real solutions rarely share _whole-program_ structure |

**Next to spec (promote once sized):** `gifting-collapse` (gen→full pairs — `MASK_MIN→MASK_BASIC`, `D4_GEN_MIN→D4_GEN→D4`, `LAYOUT_GEN→LAYOUT_FULL`, single task each; the value of each gifted primitive) · `payoff-mirror_index` (`FLOOR_AFFINE`, invent `mirror_index` on a small reflection family; payoff = search-savings − invention-cost).

---

## Backlog — deferred / downstream

| Experiment | Type | Starting primitives (floor) | Target / abstractor demands | Corpus | Params | Metrics / what to look for | Unmet machinery | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **full-corpus partition census** | search | `SYMMETRY`=`sym` · `MASK_BASIC` · `PERCEIVE_TRANSFORM` | — (SEARCH: aggregate partition) | `arc1-train` (400) | as-shipped / `harvest`, depth 3 | aggregate `by_category`/`by_provenance` across 400 tasks; new-floor solve counts (region logic, recolor) | `search --bundle` for the two new floors | **Demoted 2026-07-14** — the corpus run is a solve-count/aggregate _gauge_, not the object of study (per the single-task reframe). `d4`/`synth` already ran (EXPERIMENTS.md 2026-07-14; notebook `experiments/2026-07-14-capability-census/`); the `sym` leg + two new floors remain, run opportunistically only |
| **reusable-idiom governance** | learn | `FLOOR_AFFINE` + `build_grid` (the E8/E9 environment) | `mirror_index(n,k)=sub(sub(n,k),1)`, minted for _reusability_ (not by type-fiat); demands **usefulness-scored selection** | E8/E9 reflection family (synthetic) | naive `FrequentSubtree` proposer + a usefulness-scoring `AbstractionSelector` vs `GreedyMDL` | does train-side usefulness mint the reusable idiom over the larger `COLOR` read-bodies, no type gag; selection _correctness_ (fix the `mirror_index` **false negative**) | search-aware / selection-correct governance (F4, 🔜); port the F5 usefulness score to the execution layer | **Deferred** (your call — needs a build, low value right now). The general open-ended fix to the E8 compression/reusability divergence; sibling of library-refactoring (shipped, E10) |
| **basis/ablation science** (cluster) | search / study | a _complete_ low floor (`UNIVERSAL_FLOOR` / `MINIMAL_COMPLETE_FLOOR`) | — (maps axis 2, not a single target) | synthetic grain ladders (single-task / handful) | varies by probe | which abstractions invent + how fast (invention vs search) · which are hardest · do gifted mid-level primitives speed invention (which, how much) · primitive/machinery **ablations** — reshaping the search-cost graph | measurement layer (largely landed) + global completeness (L0 control block) | The **axis-2 payoff program** (RESEARCH-08). Its near-term concrete rows are the single-task probes now in Active; this cluster is where they generalize into a systematic map |

_Dropped 2026-07-14: **old-suite recalibration** — starting fresh; not preserving the old E2–E10 environments is a deliberate choice, not a loss._