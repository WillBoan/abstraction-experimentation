# Experiments

A shared human+AI **event log** of experiments and findings for arc-lab. Append-only, newest last. This records _what happened at a point in time_ — it does not mirror current state (that lives in the code, the regression locks, and `CLAUDE.md`).

**Discipline** (or this rots into noise):

- **Terse.** Hypothesis / what ran / result / interpretation / next. Not essays.
- **Record dead ends.** Negative results are as valuable as wins — often more.
- **Anchor to a commit** so the numbers stay reproducible.
- **Events, not state.** "On date X, measured Y" never goes stale; "current best is Y" does.
- **Planned work lives in `EXPERIMENT_QUEUE.md`.** When you log a run here, drain its queue entry there. `Next:` lines are events (what seemed next at the time); the queue is the canonical current list.
- **This is the abstract; the appendix is `experiments/`.** For a non-trivial investigation, the full write-up + the probe scripts and their outputs live in a [experiments/](experiments/) lab notebook (see [experiments/README.md](experiments/README.md)); link it from the entry. Keep the entry here terse.

Entry template (tier the bullets; put the numbers in an explicit **Metrics** block so they're scannable):

```
## YYYY-MM-DD — <short title>
- **Commit:** <hash>
- **Question:** ...
- **Ran:** ...
- **Result:**
  - <one-line what-happened>
  - **Metrics:**
    - <metric>: <value> (<from → to>)
    - ...
- **Interpretation:** ...
- **Next:** ...
```

> Seed entries below reconstruct the findings made while building the first solvers (this is a back-fill; later entries are written as the work happens).

---

## 2026-07-03 — D4 geometric baseline (`dsl`)

- **Commit:** 498cdbb (measured on current code at a220dd5)
- **Question:** How far does "the output is one rigid whole-grid transform" get on ARC-1?
- **Ran:** `arc-lab eval dsl` on arc1-train and arc1-eval. Library = D4 (8 square symmetries), search = single application.
- **Result:** 7/400 train (2× rot180, one each flip_h / flip_v / rot90, 2× transpose); 0/400 eval.
- **Interpretation:** single rigid transforms are a real but tiny slice of ARC-1, and near-absent in the harder eval split.
- **Next:** expand expressiveness — but the obvious lever (composition) needs checking first.

## 2026-07-05 — D4 is closed under composition (dead end)

- **Commit:** a220dd5
- **Question:** Can we get more from D4 by searching _compositions_ of the 8 transforms?
- **Ran:** composed all 64 ordered pairs of the 8 primitives on a labelled 3×3 grid; counted how many produced a function outside the base 8.
- **Result:** 0/64 escaped — still exactly 8 distinct functions. (The 8 are the dihedral group D₄, closed under composition.)
- **Interpretation:** composition over D4 gains **nothing**. Expressiveness has to come from a different _combinator_ over the transforms' outputs, not from composition depth.
- **Next:** combinators that combine transformed _copies_ (overlay, tile).

## 2026-07-05 — Combinators lift, and generalise (`dsl-sym`)

- **Commit:** a220dd5
- **Question:** Can overlay (symmetry-repair) + tile (mosaic) — combining D4 _outputs_, no new transforms — lift the score, and does it generalise?
- **Ran:** prototyped the exact solver logic to _size_ each combinator on arc1-train first; then built `dsl-sym` and evaluated.
- **Result:** sizing predicted overlay = 3, tile = 9; the built solver got **19/400 train** (7 D4 + 3 overlay + 9 tile, exactly as predicted, 0 errors) and **7/400 eval**.
- **Interpretation:** the biggest single jump (7→19) used _zero_ new transforms — expressiveness came from _how outputs are combined_. It generalises (0→7 on unseen eval), so it's real abstraction, not train overfit. "Size the idea empirically before building" paid off; repeat it.
- **Next:** a _general_ composition engine over more atomic primitives.

## 2026-07-05 — Typed enumeration engine + atomic primitives (`dsl-synth`)

- **Commit:** a220dd5
- **Question:** Does a general bottom-up typed enumeration engine over atomic primitives (map_color, scale) find more?
- **Ran:** `dsl-synth` = ATOMIC_LIBRARY (D4 + map_color + scale) + `Enumerate` (typed, bottom-up, observational-equivalence dedup).
- **Result:** 11/400 train (7 D4 + 1 scale + 3 map_color); 1/400 eval.
- **Interpretation:** the engine works and the atoms add 4 over D4 — but modest.
- **Next:** does composition _depth_ help this time?

## 2026-07-05 — Depth-2 composition adds nothing here (key negative result)

- **Commit:** a220dd5
- **Question:** Does allowing depth-2 program composition (vs depth-1) solve more?
- **Ran:** `dsl-synth` at `max_depth=1` vs `max_depth=2` on arc1-train; plus a synthetic `map_color(rot180(x))` task solvable _only_ at depth 2.
- **Result:** depth-1 = 11/400, depth-2 = **11/400 (identical set)**, at ~30× the runtime. Synthetic task: depth-1 fails, depth-2 solves → the engine composes correctly; the vocabulary just doesn't benefit.
- **Interpretation:** depth is not the lever on this vocabulary. Sequential `map_color` can't express a color _swap_ (1→2 then 2→1 collapses), D4 is closed, and scale rarely composes. **Richer atoms are the lever** — a color _permutation_, crop-to-content, object extraction.
- **Next:** add composition-friendly atomic primitives (color permutation, crop, objects) and re-test whether depth-2 then pays. **[primary open direction]**

> Note: these numbers are pinned by `tests/test_integration.py` and were preserved unchanged across the subsequent behaviour-preserving refactors (Program-as-class, logging, Constraint/Cost — commits 7ed27de … ca9a52c).

---

## 2026-07-06 — Metrics instrument: run artifact + compression (build + validate)

- **Commit:** 00ed308
- **Question:** Can we measure abstraction formation directly — compression, search effort — rather than only solve-count? And does the instrument agree with a known object before we trust it?
- **Ran:** Built `SearchResult`/`SearchStats` (structured, standardised search-effort counters), a two-part-MDL `CompressionMetric` over the `Cost` family (`DL = library_bits + Σ program_bits`), and a content-hashed, cached, resumable `run` artifact (`arc-lab analyze`, gitignored trace + committed summary/library). Validated on `analyze dsl-synth --dataset arc1-train`.
- **Result:** 11/400 — _exactly_ the locked ids (7 D4 + 4 atomic). `program_bits=28` (verified = summed node counts), `library_bits=10`, `considered=283184`. All locks (7/19/11) preserved; `make check` green.
- **Interpretation:** the instrument reproduces the known object, so the metrics are trustworthy. Measuring the ruler first immediately paid off — it corrects the entry above: the 4 atomic solves are **2 scale + 2 map_color**, not "1 scale + 3 map_color".
- **Next:** transfer metric + the library-learning loop, then the low-primitive-floor experiment — does a minimal basis + learned abstractions re-derive D4? **[primary open direction]**

---

## 2026-07-06 — E1: the abstraction loop re-derives rot90 from the D4 generators

- **Commit:** 3538005
- **Question:** Does the library-learning loop actually _form abstractions_ — discover a useful factoring from solved programs, compress, and speed up search — on a controlled testbed with a known-reachable target?
- **Ran:** Built the mechanism (a `Param` hole node + closed-template learned primitives, `make_abstraction`), the wake-sleep loop (antiunify → greedy-MDL governance over the existing `CompressionMetric` → `Library.extended`), a three-library harness + a _behavioral_ (observational-equivalence) checker, and a deterministic testbed generator. **E1:** starting primitives `{flip_h, transpose}`, target `rot90` **withheld**; `arc-lab learn e1-rot90`.
- **Result:**
  - Learned `abs0 = transpose(flip_h($0))`, behaviorally **== target rot90** (matched; 0 missed, 0 novel).
  - **Metrics:**
    - compression: **×1.30** (DL 35→27)
    - speedup: **×1.42** (considered 143→101)
    - enablement: **9 tasks** solved only by the learned library (depth-1 budget)
    - locks 7/19/11 unchanged; `make check` green
- **Interpretation:** the loop forms a real, transferable abstraction on a known-answer microworld — the depth-2 word `transpose(flip_h)` collapses to a depth-1 primitive, and the collapse shows up in _both_ compression and speedup. Targets stayed pure observables (never guided learning), so the anti-teleological design holds. Caveats: the abstractor is v1 (recurring-identical programs, no variable-sharing), and the MDL library term is still flat (undercharges an abstraction's definition size).
- **Next:** E2 (cell-level `{read, set_cell}` → fixed-cell `swap_cells`), then E3 (varied-cell → earns LGG variable-sharing). Then richer testbeds and the low-primitive-floor.

---

## 2026-07-06 — E2: re-derive fixed-cell swap_cells from {read, set_cell}

- **Commit:** 0946c25
- **Question:** Does the loop form a genuinely _cell-level_ abstraction — a multi-step composition of `read`/`set_cell` — and how large is the search-collapse payoff at a lower floor?
- **Ran:** New primitives `read (Grid,Int,Int)→Color`, `set_cell (Grid,Int,Int,Color)→Grid`; `Enumerate(coord_ints=True)` mines coordinate `Int` leaves. Testbed: 8 tasks each swapping cells (0,0)↔(1,1) on 2×2 grids, **3 train demos each** (so a literal-colour program can't fit — the solution must `read`). `arc-lab learn e2-swap-cells`.
- **Result:**
  - Learned `abs0 = set_cell(set_cell($0,0,0,read($0,1,1)),1,1,read($0,0,0))`, behaviorally **== target swap_cells**.
  - **Metrics:**
    - compression: **×6.42** (DL 122→19)
    - speedup: **×104** (considered 31624→304) — the depth-4 solution collapses to depth-1
    - enablement: **8 tasks** (all, depth-1 budget)
- **Interpretation:** the mechanism works unchanged at the cell floor — no AST lambda needed, `read`/`set_cell` compose as ordinary typed transforms. The payoff is far larger than E1 because the raw solution is a depth-4 composition (11 nodes) collapsed to a single call: the deeper the gap the abstraction bridges, the bigger the compression/speedup. Fixed cells → identical solved programs → the simplest abstractor (no variable-sharing) suffices.
- **Next:** E3 — vary the cells so coordinates must generalise (variable-sharing).

## 2026-07-06 — E3 / E4: variable-sharing works; flat MDL bloats, two-part MDL fixes it

- **Commit:** 0946c25
- **Question:** With coordinates that recur across positions (a coord feeds both a `read` and a `set_cell`), does antiunification produce the _correct shared-variable_ abstraction — and does the governance keep the library clean?
- **Ran:** E3 testbed: 8 tasks swapping `(0,X)↔(1,Y)` for varied `(X,Y)` on 2×2 grids. Added variable-sharing to `AntiunifyPairs` (a memo mapping a differing subterm pair to one shared `Param`). **E3** under the flat `CompressionMetric`; **E4** the _same_ environment under `TwoPartMDL` (charges each abstraction its definition size).
- **Result:**
  - Both learned the correct general `abs0 = set_cell(set_cell($0,0,$1,read($0,1,$2)),1,$2,read($0,0,$1))` — `$1`,`$2` each shared across two positions, behaviorally **== target swap_cols**.
  - **Metrics:**
    - E3 (flat MDL): **16 abstractions** — abs0 + 15 marginal specialisations (`abs0($0,0,0)`, …): _library bloat_
    - E4 (two-part MDL): **1 abstraction**, no bloat
- **Interpretation:** two findings. (1) **Variable-sharing is correct** — the memo gives the least-general-generalization that keeps a swap sound. (2) **The flat library cost is a broken governance objective**: a specialisation that saves 2 nodes still nets negative, so the loop hoards them. Charging the _definition size_ (two-part MDL) makes a marginal specialisation cost more than it saves → it is rejected. This is the "abstraction governance" problem made concrete, and the cheapest principled fix. Kept both metrics (flat baseline + `TwoPartMDL`) so the regimes stay comparable.
- **Next:** richer testbeds (perceive→transform, masks), and the low-primitive-floor — where governance pressure will matter far more.

---

## 2026-07-06 — E3 analysis → loop hardening (dedup + DL-stop)

- **Commit:** 3ccc2b1
- **Question:** _Why_ did E3's flat-MDL run bloat to 16 abstractions — and is two-part MDL the only thing keeping the loop honest?
- **Ran:** Inspected E3's learned library + per-generation history, then added two loop safeguards — library-dedup (never re-mint a template already a primitive) and a DL-monotonicity stop (discard a generation that doesn't lower total DL) — and re-ran E3.
- **Result:**
  - The 16 were only **4 distinct** abstractions (abs0 + 3 specialisations); the 3 were **re-minted every generation** under new names, and total DL _climbed_ 20→23→26→29→32.
  - **Metrics (E3, before → after hardening):**
    - abstractions: 16 → **4**
    - generations run: 5 → **1**
    - E1 / E2 / E4: unchanged
- **Interpretation:** the bloat had **three** causes, not one. (1) greedy over-abstraction _within_ a generation [flat MDL — the one two-part MDL/E4 fixes]; (2) no dedup against the library, so duplicates get re-minted; (3) no convergence guard, so DL climbs to the generation cap. Two-part MDL _masked_ (2) and (3) by removing the incentive, but they were latent — the hardening addresses them directly, independent of the metric. A deeper root cause remains noted: `Enumerate` keeps the _first-considered_ program per behaviour (library order), not the smallest, so the wake can return a longer program the next sleep then "compresses" — a keep-smallest fix is worth doing later.
- **Next:** richer testbeds and the low-primitive-floor.

---

## 2026-07-07 — Machinery bundle: governance ABC · subtree-match · keep-smallest · cost-guided beam

- **Commit:** aa8e5df (search: keep-smallest + beam; learn plug points in 4461a9f)
- **Question:** Ship the four tractable `🔜` MACHINERY mechanisms — F1 cost-guided beam, F3 subtree-match rewrite, F4 governance-as-a-plug-point + keep-smallest wake — without disturbing the E1–E4 objects or the 7/19/11 locks. Does anything move?
- **Ran:** Implemented all four (MACHINERY.md rows flipped ✅; lambda-index F0 deferred). Re-ran E1–E4 (`arc-lab learn`, incl. a fresh-cache E1) and `arc-lab eval dsl-beam / dsl-synth --dataset arc1-train`.
- **Result:**
  - `make check` green (114 tests, +8 new); locks 7/19/11 unchanged.
  - **Metrics:**
    - E1–E4 identical to baseline: E1 ×1.30 / ×1.42, E2 ×6.42 / ×104, E3 4 abstractions / novel=[abs1,abs2,abs3], E4 1 abstraction / no bloat.
    - `dsl-beam` (beam_width=16): **11/400 == `dsl-synth` 11/400** (same task ids).
- **Interpretation:** All four are behaviour-preserving everywhere we can measure today — as predicted, a **null result with a reason**. Subtree-match and keep-smallest are no-ops on E1–E4 (templates match at the whole-program root; the cell-swap solutions are already unique-shaped minimal programs), and the beam is near-lossless on the atomic floor (its grid pool never exceeds the width, so the cost-ranked frontier keeps everything the blind cut kept). The value is **latent**: subtree-match bites on heterogeneous corpora, keep-smallest on libraries with size-varying equivalents, the beam once a vocabulary's grid pool actually explodes (objects / cell-floor) — none of which exist yet. What _did_ change structurally: governance is now a swappable `AbstractionSelector` (`GreedyMDL` default), and `Cost` is finally consumed by a search (`BeamSearch`). Net: the critical path collapses to a single remaining keystone, **lambda-index binding (F0)**.
- **Next:** the runnable-today queue (perceive→transform, layered abstraction) exercises the loop on real abstractions with zero new machinery; the low-primitive-floor keystone now waits only on lambda-index.

---

## 2026-07-07 — Landscape & build-strategy review (Ferré/MADIL · Stitch · CompressARC · the field)

- **Commit:** 091d717 (docs only — no code touched)
- **Question:** Where does this project sit vs. prior art, and how should we balance building machinery ourselves vs. adopting third-party tools (YAGNI-vs-rework, in service of the research not the score)?
- **Reviewed:** Ferré's ARC-MDL / MADIL (object-centric _descriptive_ MDL, `L(M)+L(E|M)`, MDL-guided refinement, OCaml/GPLv3, single-CPU, **2%→7% ARC-1**, _no cross-task library learning_); Stitch (top-down library-learning compression, 3–4 orders faster than DreamCoder) + babble/egg + LILO + AbstractBeam; CompressARC (pure-MDL neural, no pretraining, **~20% eval / ~4% ARC-2**); the LLM-TTT / evolutionary frontier (ARChitects 53.5%, SOAR; "fast search still beats smart search"; ARC-2 collapse).
- **Findings:**
  - Pure symbolic/MDL ARC solving lands single-digit→~20% _as a score_ — not our deliverable; understanding is. We sit **deliberately off the LLM-TTT frontier** (it trades away the determinism/inspectability that make findings mean anything).
  - The **descriptive (perceive/render) half** is the identified highest-leverage empty region — Ferré is a worked existence proof. The **unoccupied position:** descriptive representation × cross-task library learning.
  - **Adopt don't rebuild:** Stitch for F4 invention-at-scale, egg/babble for equivalence-at-scale, Hodel's `arc-dsl` as vocabulary reference; own the substrate + instrumentation. Build the lambda-index keystone as a **Stitch-compatible De Bruijn index** (serves the low-floor thesis _and_ cheap future adoption).
- **Interpretation / strategy:** captured in [MACHINERY-STRATEGY-2026-07-07.md](docs/MACHINERY-STRATEGY-2026-07-07.md) (the build methodology); frame updated in [RESEARCH-2026-07-07.md](docs/archive/RESEARCH-2026-07-07.md) (supersedes 07-06); [MACHINERY.md](MACHINERY.md) / [ONTOLOGY.md](ONTOLOGY.md) annotated surgically.
- **Next:** the F0 substrate keystone (De Bruijn λ-index); then shift the workload from synthetic microworlds toward real ARC tasks so reality writes the build queue.

---

## 2026-07-07 — F0 keystone: the Stitch-compatible De Bruijn lambda substrate (`build_grid`)

- **Commit:** a490432
- **Question:** Build the lambda-index binding — the one `unbuilt` F0 row under the low-floor thesis — as a general, Stitch-compatible substrate, and verify a single size-general `build_grid` program can express D4 geometry. (Substrate only; the search + pixels→D4 experiment are the deferred follow-on, per the barbell + Principle 5.)
- **Ran:** Added two AST nodes — `Var` (De Bruijn `$i`, distinct from `Param` = Stitch's `#j`) and `Lam` (unary `(lam …)`) — plus a `scope` channel on `evaluate` (env-based/closure interp → no index-shifting), a runtime `Closure` value, an opaque `FN` type, and the `build_grid`/`width`/`height`/`sub` clique (`BUILD_LIBRARY`, wired into no locked solver). Hand-verified by construction (no search).
- **Result:**
  - A single program `build_grid(width(input), height(input), lam(lam(read(input, $0, sub(sub(width(input),1), $1)))))` re-derives `rot90` on grids of **any shape** (drove 2×3→3×2, 4×2→2×4, all == `np.rot90`); likewise `flip_h`, `transpose`.
  - **Metrics:**
    - `make check` green (121 tests, +7 new); locks 7/19/11 unchanged; mypy `--strict` clean.
    - De Bruijn correctness, `to_dict` round-trip, and `make_abstraction` over a `build_grid` template (arity 1 — loop vars stay internal) all pass.
- **Interpretation:** The keystone lands as a _representation_, not yet a capability — the substrate is proven by hand, but nothing _searches_ for `build_grid` programs yet. Two-channel binding (`env`=`#j` abstraction args vs. `scope`=`$i` bound vars) is what lets a size-general geometry program mint cleanly as an abstraction, and env/closure evaluation sidestepped De Bruijn's index-shifting entirely. Cost was modest and contained (one `FN` tag, a runtime `Closure`, ~physics-only churn in `program.py`), and it's Stitch-shaped so future invention-engine adoption is near-drop-in. Also renamed `Param`'s display `$0`→`#0` to match Stitch's `#j` and free `$i` for `Var`.
- **Next:** the bespoke `build_grid` search (open-term body enumeration — "where does search break?"), then pixels→D4 through the loop (does a learned `mirror_index` bootstrap the deep D4 members?). Or, per the strategy, point the loop at a real ARC slice instead of another microworld.

---

## 2026-07-07 — BuildGridSearch: open-term body enumeration + the "where does search break?" sweep

- **Commit:** 8b0819b
- **Question:** Can a bespoke search find size-general `build_grid` programs (whose bodies are _open terms_ — free `$i` — that `Enumerate` can't pool), and _where does it break_?
- **Ran:** Built `BuildGridSearch` (`search/build_grid_search.py`): enumerate INT coordinate-expressions over `{$0, $1, width, height, 0, 1}` composed with `sub`, deduped by a _per-cell_ battery signature (obs-equivalence lifted to cells), cost-beam bounded; assemble `build_grid(dh, dw, lam(lam(read(input, row, col))))` over dims that match the output shape, cost-ordered early-exit on the first (min-cost) consistent program. Swept depth × beam over the seven D4 members.
- **Result:**
  - Finds `rot90`/`flip_h`/`transpose` as single size-general programs that generalise to unseen shapes; the whole ladder is reachable at `max_coord_depth=2, beam=128`.
  - **Metrics (the ladder):**
    - depth: `transpose` solvable at depth 0 (bare `$0`/`$1`); every reflection/rotation needs depth **2** (a reflection `n-k-1` is inherently two `sub`s — nothing new at depth 1). Program size ladder 11 (transpose) → 16 (single reflection) → 21 (rot180 / anti_transpose).
    - **beam cliff:** at depth 2, all reflection members are found _only_ at beam **128**, and fail sharply at beam ≤ 64 — the reflection expression sits at rank 64-128 in the cost-ranked pool, so a tighter beam _silently_ drops it.
    - effort (beam 128, early-exit): `rot90` ~700 candidate-checks / ~130ms; `rot180` ~15k / ~270ms.
- **Interpretation:** open-term enumeration works, generate-and-test over _behaviours_ (out-of-bounds `read` + cell-battery dedup prune the space). But it does **not** degrade gracefully — there is a hard beam cliff, because the correct coordinate formula is buried deep in the cost ranking. This is the concrete "where search breaks", and it predicts the bootstrap: a learned `mirror_index` would collapse the reflection to a depth-1 call, moving it to the _top_ of the ranking → findable at tiny beam.
- **Next:** run it through the loop (pixels→D4).

## 2026-07-07 — pixels→D4: the loop re-derives D4 (E5/E6/E7), and a bound-var scope bug

- **Commit:** 73af6e5
- **Question:** Starting from the cell-render floor (`{read, set_cell, width, height, sub, build_grid}`, **no D4 primitive**), does the wake-sleep loop re-derive geometry as size-general `build_grid` abstractions — and does a shared `mirror_index` bootstrap the ladder?
- **Ran:** `E5` (rot90 alone), `E6` (full D4 ladder, naive proposer), `E7` (same ladder, a **bound-var-safe** proposer). Tasks are multi-shape demo sets (three varied shapes) so the solved program must be size-general. Taught antiunify to descend `Lam`/`Var`; `search = BuildGridSearch`, `enablement = Enumerate(depth 1)` (which now skips `FN`-typed prims instead of `KeyError`-ing).
- **Result:**
  - **E5:** learned exactly `abs0 = build_grid(width(#0), height(#0), lam(lam(read(#0, $0, sub(sub(width(#0), $1), 1)))))`, behaviorally **== rot90**. matched=[rot90], 0 novel, enablement=6. Clean re-derivation.
  - **E6 (naive):** matched=**['flip_v']**, missed 5, **6 novel** broken abstractions. Whole-program pairwise antiunification generalises _across_ members by holing bound-var-containing coordinate subterms into abstraction params (`read(#0, #1, #2)`) — a **scope violation** (`$i` is per-cell; `#j` is per-call). Worse, two-part MDL _prefers_ the broken abstraction (it "compresses" by covering several members) — compression and correctness diverge.
  - **E7 (bound-var-safe proposer):** matched=**all 6** D4 members, 0 missed, 0 novel, enablement=24. The fix: refuse to hole a differing subterm that contains a bound `$i`; only the sound per-member recurrences mint.
  - **Metrics:** `make check` green (130 tests, +9); locks 7/19/11 + E1-E4 unchanged. E7 DL L1→L2 = 390→492 (compression **×0.79**, i.e. _worse_).
- **Interpretation:** the low-floor thesis holds — the machinery re-derives the entire dihedral group from pixels, size-generally, blind. Two findings fall out: (1) a real **antiunification soundness bug** — it must respect the `$i`/`#j` boundary (now a swappable `bound_var_safe` flag; E6/E7 are the flat-vs-fixed contrast, à la E3/E4); (2) **`mirror_index` does not bootstrap** — E7's DL _rises_ because the six members are six separate large abstractions with no shared reflection idiom. Whole-program antiunification can't mine a recurring _subterm_; that needs a **frequent-subtree proposer** (MACHINERY F4, unbuilt) — the concrete next machinery the compression signal is pointing at.
- **Next:** a frequent-subtree / e-graph proposer (to invent `mirror_index` and actually compress the ladder); or, per the strategy, point the loop at a real ARC slice.

## 2026-07-07 — pixels→D4 compresses: a frequent-subtree proposer + primitive-driven search (E8/E9)

- **Commit:** 8051ad4
- **Notebook:** [experiments/2026-07-07-e8-e9-mirror-index-bootstrap/](experiments/2026-07-07-e8-e9-mirror-index-bootstrap/) — full write-up + probe artifacts
- **Question:** Does a **frequent-subtree proposer** invent `mirror_index` and finally _compress_ the D4 ladder (inverting E7's ×0.79) _and_ speed the search past its beam cliff? Run as a **grammar × learning matrix**: `{sub}` (E8) vs the honest affine `{sub,add,mul}` (E9).
- **Ran:** Added `add`/`mul` (the affine family); made `BuildGridSearch` **primitive-driven** (composes every library `INT^n→INT` op, so it can _reuse_ a learned coordinate); a `FrequentSubtree` proposer (mine Lam-free proper subtrees; sound Var-holing — the mirror image of `bound_var_safe`). E8 = sub-only (beam 128); E9 = affine (beam **224**, the threshold to solve a reflection at all — ≤192 fails). `enablement = BuildGridSearch` at a _tight_ beam (the cliff).
- **Result:**
  - **A rewrite bug, first:** `rewrite_with` didn't descend into `Lam` bodies, so `mirror_index` — which lives _inside_ the coordinate lambda — never folded and nothing minted. Fixed (descend `Lam.body`; E1–E7 unaffected).
  - **The divergence (the load-bearing finding):** the _naive_ proposer + greedy MDL mints two **unreusable `COLOR` read-body idioms** (`read(#0,#1,sub(sub(width(#0),#2),1))` and its height twin — the two "axis half-reflections"), **not** `mirror_index`. They compress the _train corpus_ more, but `BuildGridSearch` can't compose a `COLOR` read-body → re-solve DL _worsens_ (×0.95), zero speedup. And iteration can't recover `mirror_index`: once the read-bodies absorb it into their _definitions_, the corpus-only proposer can't see it (no library refactoring). **Train-DL ≠ reusability** — and a compression-optimal inventor (Stitch) would hit this too.
  - **The fix (a labelled stopgap):** scope invention to the signature the search composes (`INT^n→INT`), **derived from the search itself** (`SearchScopedFrequentSubtree(composes=search.composes_signature)`, not a declared type). Then `mirror_index` mints and the search reuses it.
  - **E8 (sub):** `abs0 = sub(sub(#0,#1),1)`, behaviorally **== mirror_index**. matched, 0 missed/novel.
  - **E9 (affine):** `abs0 = sub(#0, add(#1, 1))` — _same behavior_ via `add` (the checker matches by signature). Same idiom, different structural form → invention is robust to the grammar.
  - **Metrics:** `make check` green (139 tests, +9); locks 7/19/11 + E1–E7 unchanged.
    - compression (L1→L2 DL): E8 390→348 **×1.12**, E9 392→350 **×1.12** (inverts E7's ×0.79)
    - speedup (considered): E8 **×3.47**, E9 **×5.00** (bigger against the wider grammar)
    - enablement (tight-beam solves, L2 not L1): **20 tasks** (all 5 reflection members) in both — the beam cliff dissolved
    - base blowup (L1 considered): sub 77.1k → affine 234.8k (**~3×**) — the measured cost of `add`/`mul`
- **Interpretation:** the `mirror_index` bootstrap works — a shared coordinate factor is invented, _reused_ by the primitive-driven search, so the ladder compresses (×1.12) and the cliff dissolves (×3.5–5); the affine matrix shows the same idiom emerges under a wider grammar and matters _more_ there. But the headline is the **compression/reusability divergence**: greedy MDL over train-DL prefers the largest compressor, which is not the reusable one. The type-scoping that recovers `mirror_index` is _anti-open-ended_ (can only invent what the search already composes, foreclosing new-type / layered vocabulary) — hence a **stopgap**. The general fixes are **search-aware / transfer-aware governance** (score by usefulness, not node-count) or **library refactoring** (compress the _library_, not just the corpus — the missing half of iteration; ≈ Stitch's core). Full approach catalog in `MACHINERY.md` F4.
- **Next:** the general fix — search-aware governance, or library refactoring **via Stitch** (deferred to real-ARC scale per MACHINERY-STRATEGY's "not yet"); and the standing move — point the loop at a real ARC slice.

## 2026-07-08 — Stitch spike: two orthogonal ways it dissolves the E8 divergence (library refactoring vs. higher-order)

- **Commit:** b5268b5 (offline spike, docs only). Full write-up + scripts: [experiments/2026-07-08-stitch-spike/](experiments/2026-07-08-stitch-spike/).
- **Question:** E8 predicted "a compression-optimal inventor (**Stitch**) would hit [the compression/reusability divergence] too." Does _real_ Stitch, fed the D4 `build_grid` programs, prefer the unreusable `COLOR` read-body idiom (reproduce the divergence) or recover `mirror_index` (dissolve it)? Load-bearing because MACHINERY F4 marks library refactoring `🔜 via Stitch` as the general fix, while E8 predicts Stitch reproduces the problem.
- **Ran:** Wrote a 6-case `Program → Stitch s-expr` serializer (the keystone paid off — `Var/Param/Lam` already print `$i`/`#j`/`lam`; only `Apply` needed prefix form; oracle-checked against `to_dict`). Fed `stitch_core.compress` (ephemeral `uv --with`, no dep added) along **two orthogonal axes**: **what you feed it** {raw corpus of the 6 `_d4_targets(input())`; the two minted read-body _definitions_ = library refactoring} × **hole expressiveness** {first-order (`no_curried_metavars`); higher-order (default)}. Cross-checked {default cost prim=100; node-count cost all=1 ≈ TwoPartMDL} and `allow_single_task`.
- **Result:**
  - **The two axes are the whole story (top abstraction per cell):**

    | feed ↓ / holes → | first-order | higher-order |
    | --- | --- | --- |
    | **raw corpus** | `COLOR` read-body → **divergence** (E8 confirmed) | `mirror_index` (perceiver hoisted) → dissolved |
    | **minted defs (refactor)** | **general `mirror_index(n,k)`** → dissolved | `mirror_index` (perceiver hoisted) → dissolved |

  - **Library refactoring is the architecture-relevant fix (bottom-left) — and it works FIRST-ORDER.** Feeding the two read-body definitions back through compression extracts the clean, general `(sub (sub #0 #1) 1)` = `mirror_index(n,k)` — no higher-order needed, because once the read-body _is_ the definition, the bigger competing subtree is gone. This is precisely the capability the wake–sleep loop lacks: the proposer walks corpus **call-sites** (`[program for _, program in corpus]`), never abstraction **definitions** (`.template`), so once the read-bodies bury `sub(sub(·,·),1)` it can never be re-mined. = the MACHINERY F4 `library refactoring` row, ≈ Stitch's core.
  - **Higher-order invention is a SEPARATE, additive mechanism (top-right).** On the _raw_ corpus, higher-order holing hoists the perceiver `#1 ∈ {width,height}` into a function-typed hole, unifying both axis-reflections into one abstraction reused across all 5 reflection members, so `mirror_index` becomes the top compressor _before_ any read-body is minted. It **prevents** the burial rather than **curing** it. Bonus, not a replacement for refactoring.
  - **On the raw corpus, first-order Stitch reproduces the divergence** (top-left): TOP = `(read input (sub (sub (height input) #0) 1))`, the `COLOR` read-body; `mirror_index` drops to a lower, width-specialised `fn_4`. So E8's "Stitch would hit this too" is **correct for first-order raw-corpus compression** — refactoring (or higher-order) is what breaks it.
  - **Not the lever:** node-count vs default cost, and `allow_single_task` on/off, leave every top pick unchanged.
  - **Reusability nuance:** the _higher-order_ `mirror_index` has a function-typed arg + closes over `input`, so the current `INT^n→INT`-only `BuildGridSearch` can't compose it as-is; the _refactored first-order_ `mirror_index(n,k)` is directly composable (same signature the type-scoping stopgap yields).
  - **Metrics** (Stitch's own cost units; tiny corpus — directional, not a benchmark):
    - raw corpus: first-order → read-body (ratio 2.80 default / 2.64 node-count); higher-order → `mirror_index` (3.37 / 3.63)
    - refactor (read-body defs): first-order → general `mirror_index(n,k)` (ratio 1.55); higher-order → perceiver-hoisted `mirror_index` (1.89)

- **Interpretation:** two independent capabilities, each dissolving the divergence at a different failure point. **(1) Library refactoring** — compress the _library_ (walk definitions), not just the corpus — is the piece the current loop lacks and the piece the architecture needs: it _cures the burial_ after read-bodies are minted, and works **first-order** (recovers the clean, search-composable `mirror_index(n,k)`). This is the F4 `library refactoring` row, **confirmed** (an earlier draft of this entry wrongly demoted it — corrected). **(2) Higher-order invention** — a distinct, additive Stitch capability — _prevents the burial_ on the raw corpus. E8's prediction stands for first-order raw-corpus compression; either capability breaks it. Corollary: the in-house `FrequentSubtree` (first-order, Lam-free, sound Var-holing) would _itself_ recover `mirror_index` if simply fed the `.template` bodies and allowed to rewrite definitions — so the refactoring capability isn't intrinsically Stitch-shaped ("feed the proposer the library, rewrite the library"); Stitch is the mature, at-scale, De Bruijn-native implementation of it.
- **Next:** implement **library refactoring** (the F4 row) as the concrete next machinery — either (a) minimal in-house (feed `.template`s to the existing proposer + let `_sleep` rewrite definitions; determinism/no-dep, proves it on the microworld), or (b) adopt Stitch (branch-and-bound, hierarchy-building, De Bruijn-native) as the at-scale engine per MACHINERY-STRATEGY. Standing move unchanged: point the loop at a real ARC slice.

## 2026-07-08 — compression↔transfer correlation (the measurement layer's first read)

- **Commit:** cda7c91 (the transfer-measurement layer — `heldout_transfer` grade + train-side `Usefulness` + `compression_transfer_correlation`). Full write-up + scripts: [experiments/2026-07-08-compression-transfer-correlation/](experiments/2026-07-08-compression-transfer-correlation/).
- **Question:** held-out transfer is now measurable (the grade, previously an IOU). Does **compression predict transfer** across E1–E9, and does the instrument read the **E8 divergence** — does the compression-greedy abstraction score worse on transfer than the reusable one?
- **Ran:** `heldout_transfer` at the shallow enablement budget (an abstraction counts only if it collapses depth enough to bring a held-out task into reach); the correlation harness over E1–E9; a within-experiment control (E8 naive `FrequentSubtree` vs. search-scoped proposer, same 6 held-out); a per-experiment held-out base-vs-L2 decomposition.
- **Result:**
  - **Compression does not predict transfer.** E7 is the witness: worst compression (×0.79 — six verbose-but-correct D4 abstractions inflate the tiny train DL) yet perfect transfer (6/6); E2 best compression (×6.42), transfer 2/2. Train-DL shrinkage and held-out enablement measure different things and can point opposite ways.
  - **The instrument reads the E8 divergence, within-experiment:** naive proposer → transfer **0** (two unreusable `COLOR` read-bodies), search-scoped → transfer **5** (the reusable `mirror_index`), same 6 held-out. The compression-greedy pick transfers to nothing.
  - **Decomposition** (transfer = L2 − base solves, on held-out): deep gap + good abstraction → 100% (E2–E5, E7); E1 = 1/4 because the base already solves 3/4 (rot90 is 2 steps from the floor — a shallow gap, not a bad abstraction); E6 = 1/6 (naive broken abstractions); E8/E9 = 5/6 (base already solves the shallow `transpose`).
  - **Metrics** (compress = L1→L2 DL ratio; full table in the folder): **E7 ×0.79, transfer 6/6** (the anti-correlation) vs **E2 ×6.42, 2/2**; **E8/E9 ×1.12, 5/6** at deep-speedup ×3.47/×5.00.
- **Interpretation:** the empirical case for **selection-correct governance** (score by train-side usefulness/transfer, not train-DL node-count), and a validation of the measurement-bottleneck thesis — the grade had to exist before the governance objective could be defined. Caveats: raw transfer counts are unnormalised (different held-out denominators) and budget-relative — the clean signal is the within-experiment naive-vs-scoped contrast (0 vs 5); the x-axis is full-corpus re-solve DL, not the train-DL greedy actually optimises (the divergence is sharper there); 9 microworld points are directional, not statistical.
- **Next:** **selection-correct governance** — a usefulness-scored selector vs `GreedyMDL` on E8/E9 (the queued _reusable-idiom governance_ row). Standing move unchanged: point the loop at a real ARC slice, where the grade becomes readable at scale.

## 2026-07-08 — E10: Stitch library refactoring recovers mirror_index (a first-order stopgap)

- **Commit:** working tree on b5268b5 (Phase A + B of the best-practices plan, uncommitted).
- **Question:** the _other_ general fix to the E8 divergence (sibling of the selection-correct-governance entry above): does **library refactoring via Stitch** — compress the library's own _definitions_, not just the corpus — recover a **composable** `mirror_index` **without** `SearchScopedFrequentSubtree`?
- **Ran:** Phase A lifted the sleep step behind a pluggable `SleepStrategy` (`GreedyMDLSleep` = the historical greedy-MDL machinery, behavior-identical: E1–E9 + dsl 7/19/11 zero drift, driven end-to-end on E8). Phase B added the `stitch_core` boundary (`learn/stitch_shim.py`: `to_sexpr`/`from_sexpr` with type re-inference; Stitch as a _proposer_ so our `TwoPartMDL` governs — full cost control) and a two-phase `RefactoringSleep`. **E10** (`arc-lab learn e10-stitch-refactor`): E8's sub-only env, `sleep=RefactoringSleep(FrequentSubtree(), StitchProposer(first_order=True))`.
- **Result:**
  - **Recovered, first-order.** Phase 1 (in-house `FrequentSubtree`) mints the two closed `COLOR` read-bodies; phase 2 (Stitch) _antiunifies their differing perceiver_ → `abs2 = sub(sub(#0,#1),1)` == **mirror_index** (matched, 0 missed), composed by `BuildGridSearch`. No type gag.
  - **The load-bearing caveat (probed directly):** Stitch _can_ mine the raw corpus — but the general reflect-across-a-_perceived_-dimension idiom it finds is **higher-order**: `fn_0 = (sub (sub (#1 input) #0) 1)`, perceiver `#1` a _function_ arg, in one pass. Our first-order substrate can't consume it. In **first-order** mode, mining the lambda-bearing corpus yields only partial-applications (`(read input X)`, `(read input)`, `(lam …)`) and a _width/height-specialised_ mirror — unusable. So the two-phase is a **labelled first-order stopgap** (sibling of the type-scope it replaces): in-house does sound bound-var corpus mining; Stitch does the antiunifying refactor the identical-subtree in-house proposer can't. The clean single-pass "Stitch mines the corpus" unlocks with the higher-order phase.
  - **Two dead-ends en route:** one-phase Stitch `iterations=5` → hierarchy `fn_k` refs skipped, greedy mints the specialised (uncomposable `GRID→INT`) mirror → DL ×0.95, enablement 0; `iterations=1` on the corpus → curried partial-app read-body invalid in our non-curried DSL → nothing mints. Both are the same first-order/lambda impedance.
  - **Metrics:** `make check` green (+8 tests, 4 skip without the optional wheel); E1–E9 + dsl locks unchanged.
    - compression L1→L2 DL 390→364 **×1.07** (vs E8 stopgap ×1.12 — e10 also keeps the two read-bodies)
    - speedup **×3.47**; enablement (tight-beam L2-only) **20 tasks**; held-out transfer **5** — E8's payoff, on merit
  - Determinism verified at `threads=1`; multithreading deferred to real-ARC scale.
- **Interpretation:** **library refactoring works and is the sibling general fix** to selection-correct governance (both dissolve the divergence). The pluggable `SleepStrategy` + the `stitch_core` boundary + `rewrite_library_definitions` are the reusable machinery; the two-phase `RefactoringSleep` is a knowingly-first-order stopgap. Straight version: **higher-order Stitch mines the corpus in one pass** — earns its place in the higher-order phase, where the two-phase collapses.
- **Next:** the higher-order substrate (polymorphic types → `AppFn`/`PrimRef` → general higher-order search), which turns "Stitch mines the corpus" from higher-order-only into consumable. Standing move unchanged: a real ARC slice.

## 2026-07-08 — Higher-order stack (C–F): Stitch invents a function-typed abstraction, the search consumes it

- **Commit:** working tree (Phases C–F of the best-practices plan, atop the committed A/B).
- **Question:** the E10 "Next" — build the higher-order substrate so the higher-order idiom Stitch finds becomes _representable, typed, and consumable_. Does the full stack (polymorphic types → `AppFn`/`PrimRef` → higher-order Stitch → higher-order search) work end to end?
- **Ran:**
  - **C** — polymorphic type language: `Type = ValueType | ArrowType | TypeVar` + Hindley-Milner `unify`/`apply_subst`/`instantiate` (base types stay the `ValueType` enum — a labelled compromise, see below).
  - **D** — higher-order representation: `AppFn` (apply a _computed_ function value, reusing `Closure.__call__`), `PrimRef` (a primitive as a first-class value); `Type` threaded through `Primitive`/AST nodes/`make_abstraction`/pools (annotation-widening, behavior-preserving).
  - **E** — general higher-order `Enumerate` (a `higher_order` flag): fills a function-typed hole from a library-derived `PrimRef` pool, pruned by arrow-type unification.
  - **F** — higher-order `from_sexpr` (HM inference solves each metavar's arrow across all its uses); `StitchProposer(first_order=False)`; `_is_useful` now counts `AppFn` as structure.
- **Result:** on a corpus of "apply a transform twice" (`rot90(rot90(input))`, `rot270(…)`, `flip_h(…)`), **higher-order Stitch invents `twice = (#0 (#0 input))`** by holing the repeated _function_; **first-order Stitch invents nothing** (a head is unholeable). Deserialized, `twice : (GRID→GRID, GRID) → GRID`. The **higher-order `Enumerate` then consumes it** — `twice(&rot90, input) = rot180` at depth 1 — which the **first-order search cannot** (rot180 needs depth 2). Genuine higher-order necessity at bounded depth, invention→consumption end to end.
  - **Metrics:** `make check` green throughout (156 → 164 tests + a gated Stitch/HOF suite of 9); every first-order lock byte-identical (E1–E9, dsl 7/19/11, E8 driven end-to-end) — the `higher_order` flag defaults off and `ValueType`-as-base-leaf keeps the first-order substrate unchanged.
- **Interpretation:** the higher-order paradigm is real _and consumable_. The very thing first-order can't do — hole a repeated function — is now invented (higher-order Stitch), represented (`AppFn`/`PrimRef`), typed (arrows, via HM inference), and reused by the search. Two knowing compromises remain, each queued: the type system is a union with `ValueType` base-leaf (Phase H unifies it into the `ArrowType`/`TypeVar` mechanism), and `Enumerate` HOF is PrimRef-filling only (Phase G adds `Lam`-synthesis of _new_ function values + function-behavioral dedup).
- **Next:** Phase G (Lam-synthesis) and Phase H (type-system unification). Standing move unchanged: point the loop at a real ARC slice.

## 2026-07-08 — Phases G + H: the last two higher-order compromises closed

- **Commit:** Phase G = `c66afca`; Phase H = working tree atop it. Closes the "two knowing compromises" the C–F entry flagged.
- **Question:** the C–F stack left two labelled stopgaps — (G) higher-order `Enumerate` could only _reference_ library functions (PrimRef-fill), never _synthesize_ new ones; (H) the type language was a `ValueType`-enum base-leaf grafted onto the `ArrowType`/`TypeVar` ADT. Close both, move no lock.
- **Ran:**
  - **G — `Lam`-synthesis.** `Enumerate(synthesize_functions=True)` builds _new_ function values bottom-up (`lam(body)`), not just `PrimRef`s, deduped by a **function-behavioral signature** (evaluate each candidate over a fixed grid battery; one survivor per behavior, the smaller wins). DoD is a strict-necessity case: over the cyclic library `{rot90, thrice}`, reaching `rot180` needs the _rot180 function itself_ in `thrice`'s hole (rot180³ = rot180) — no primitive provides it, so only a synthesized `lam(rot90(rot90($0)))` fills it. First-order (needs depth 2) and PrimRef-only (`thrice(&rot90,·) = rot270`) both provably fail; Lam-synthesis solves at depth 1.
  - **H — retire the `ValueType` enum.** Base types are now bare module singletons `GRID`/`COLOR`/`INT`/`FN : BaseType` (a frozen dataclass — DreamCoder's `tgrid`/`tint` idiom), so `Type = BaseType | ArrowType | TypeVar` is one uniform frozen-dataclass mechanism, no enum. `unify` gains a `BaseType` arm; serialization stays the bare string (backward-compatible with every artifact); a `_BASE_TYPES` registry + `base_type()` make base types shared **singletons** (identity round-trips through (de)serialization). ~130-site mechanical churn across 29 files (a scripted rename + hand-edited package API).
- **Result:**
  - Both compromises gone; the type language and the HOF search now sit at the plan's best-practice endpoint.
  - **Metrics:** `make check` green — **167 passed, 5 skipped** (the `stitch_core`-gated suite); mypy `--strict` + ruff clean. Every regression lock byte-identical: **dsl 7/19/11**, **E1–E9**, and the Stitch/HOF suite. Zero numeric drift — a pure representation refactor (H) plus an additive search capability (G, off by default).
- **Interpretation:** the higher-order library-learning stack is complete and compromise-free within its scope — invent (higher-order Stitch) → represent (`AppFn`/`PrimRef`) → type (uniform `BaseType`/`ArrowType`/`TypeVar` + HM) → search (PrimRef-fill **and** `Lam`-synthesis, behaviorally deduped, unification-pruned). The one remaining plan caveat is by design: the necessity proofs live on synthetic testbeds (twice/rot180, thrice/rot180).
- **Next:** point the loop at a real ARC slice — where invention-vs-search and the higher-order payoff become measurable at scale (unchanged standing move). The queued **selection-correct governance** and **library refactoring** rows remain the open research fronts.

## 2026-07-09 — Review of the higher-order/Stitch/type stack (corrects the "compromise-free" claim)

- **Commit:** working tree atop `6f38706`. Full ledger: [codebase_review/review-after-stitch-integration-2026-07-09.md](codebase_review/review-after-stitch-integration-2026-07-09.md) (the abstract here; findings + status there).
- **Ran:** a multi-agent adversarial review (three reviewers — Phase-H migration, higher-order search, Stitch shim/sleep — plus independent reading), sharpest claims re-probed against the real `stitch_core` wheel.
- **Result:**
  - **The Phases G+H "compromise-free within its scope" claim (above) was overstated.** The function-dedup battery was in fact _unsound_ — both probe grids were height-2, so `height` collapsed with a constant and a correct candidate could be silently dropped (order-dependent solve). Fixed by widening the battery + never deduping an unprobeable (`None`) signature; but it remains a **finite-battery observational-equivalence heuristic** (standard, sound-in-practice, not complete) — not "compromise-free".
  - **Also fixed** (all in tree, behavior-preserving; locks byte-identical): `from_sexpr` now rejects arity-invalid partial applications (first-order Stitch's "read-body" on the raw corpus _was_ one); De Bruijn `$i` scoping (a flat index→type map wrongly unified sibling `$0`s); deserialization fail-fast (`base_type` raises on an unknown tag, as the retired enum did); `BOOL`↔Stitch round-trip (`true`/`false` tokens); + three misleading docstrings (notably `RefactoringSleep`'s Stitch-necessity justification, which contradicted this log's own E10 finding) and a `PYTHONHASHSEED` ordering leak.
  - **Metrics:** `make check` green — 174 passed, 5 skipped; ruff + mypy `--strict` clean; dsl 7/19/11 + E1–E9 unchanged.
- **Interpretation:** the stack holds up — no correctness bug survived into a lock, and the first-order byte-identity is real — but "compromise-free" was the wrong word: the honest framing is _finite-battery observational equivalence_ + a handful of latent gaps that bite only as the polymorphic/higher-order roadmap lands (tracked in the review file).
- **Next:** the deferred `eq`/`if` search-cost reductions (commutative pruning; per-round budget) are queued in `MACHINERY.md` (F1). Standing move unchanged: a real ARC slice.

---

## 2026-07-09 — RunSpec × Config refactor (machinery as data)

- **Commit:** branch `refactor/runspec-config` (Phases 1–15)
- **Question:** (structural, not empirical) can the machinery be made _data_ — a frozen, hashable `Config` — so parameters decouple from wiring, runs are reproducible, and the activity model is explicit in the code?
- **Ran:** a 15-phase migration; `make check` green and the regression locks (`dsl`=7, `dsl-sym`=19, `dsl-synth`=11) re-verified at every lock-touching step.
- **Result:**
  - config-as-subclass solvers → `Config` + `PRESETS` (`ProgramSearchSolver` is the only solver class); `RunSpec = Config × Corpus` with a content-hashed `run_id` that now includes the search params (fixes a latent cache collision); provenance-agnostic `Corpus`/`AnnotatedTask`; content-addressed library store (`Library.from_dict`); one `execute()` core with a sleep knob (Eval vs Synthesize); transfer reducers moved to a read-side `analysis/transfer`; `Experiment`→`StudySpec`/`run_study`.
  - Behaviour preserved: E1–E10 study outputs unchanged.
- **Interpretation:** the machinery is now diffable / hashable / sweepable; the run/config/activity model is mapped in `ARCHITECTURE.md`.
- **Next:** fold `GeneratedTask` into a `Corpus` of `AnnotatedTask` (regenerate committed testbeds); put the sleep strategy into the run identity; CLI verb split + a config-file / `--set` override surface.

---

## 2026-07-11 — Execution overhaul lands; locks deliberately re-pinned on the new engine

- **Commit:** branch `refactor/runspec-config-2` (the execution-layer overhaul, uncommitted at run time; the new locks pin the numbers)
- **Question:** does the generic typed bottom-up engine + recorded-run execution layer (EXECUTION.md) reproduce the old bespoke-search behavior — and where exactly does it differ?
- **Ran:** each new preset (`execution/presets.py`) over the full `arc1-train` (400 tasks) via `run_search`; the E1 study end-to-end via `run_study`; locks re-pinned in `tests/program_search/test_locks.py`.
- **Result:**
  - **Exact continuity where the old searches were generic:** `d4` = the old `dsl` seven, identical task-ids; `synth` = the old `dsl-synth` eleven, identical — and the old "lock at reduced depth, same set" empirical finding replicates (depth-2 = depth-3 set, 1s vs 103s).
  - **Understood divergence where the old searches were bespoke:** `sym` = 10/400 vs old `dsl-sym` 19 — the D4 seven + all three overlay tasks; the missing nine are exactly the old TILE tasks. Cause: a 3x3 mosaic is nine variadic args (out of `max_arity=4`), and a tiling factor that is neither input dim is out of vocabulary under harvest constants. Budget/policy limits (data), not architecture; lift = structured-composition capability or budget policy, never a bespoke search. Full-corpus `sym` costs ~32 CPU-min (24.7M considered), so its lock runs a fixed curated 30-task slice (~40s).
  - **Two measured budget findings:** `finite-enumerate` constants explode variadic composition on large grids (one 30x30 task ≈ 6M candidates → `sym` uses harvest-from-instance); a 16-wide beam fills with size-1 constants and starves the GRID type entirely (0/400 → `beam` = harvest + width 32, solving 9 = `synth` minus the truncation's two).
  - **The learning loop locks green on the new stack:** E1 mints `abs0`, it behaviorally matches the withheld `rot90` target (probe-based semantic check), enablement 0→8 at the shallow budget, held-out transfer 4/4.
  - **Metrics:**
    - locks: `d4` 7/400 (= old) · `synth` 11/400 (= old) · `sym` 10/400 (old 19; −9 tile, understood) · `beam` 9/400 (newly locked) · E1 study transfer 4/4
    - gate: `make check` 377 passed, 5 skipped (optional stitch wheel), ~60s wall
- **Interpretation:** the generic engine reproduces every behavior the old system got from _general_ search and pays a visible, explainable cost exactly where the old system used _bespoke_ structure — which is now a capability/budget question on one engine instead of a zoo of strategies. The old locks keep guarding the old tree until its deletion pass.
- **Next:** recalibrate E2–E10 environments onto the new engine (queued); structured-composition capability for mosaic-scale variadics if/when tile tasks matter.

---

## 2026-07-11 — `eager_grounding_over_universe` cost-vs-depth on a real `map` recolor task

- **Commit:** branch `refactor/runspec-config-2` at 35a2685 (map/filter/fold/sort_by slice, uncommitted at run time)
- **Question:** `map`'s codomain type var is unshared by any sibling, so it can only be resolved via `unpinned_type_var_mode="eager_grounding_over_universe"` (grounds over `monotype_universe`, a full recursive body search per candidate type). How expensive is that in practice for a real end-to-end task, and is it driven by the `monotype_universe` list-nesting closure (a List-specific inflation) or something more general?
- **Ran:** `row_to_grid(map(lam(if(eq($0,3),7,$0)), cells(input)))` — recolor-one-color, single-row grid — under `BottomUpSearchEngine(unpinned_type_var_mode="eager_grounding_over_universe")` at increasing `max_depth`/library size (probe scripts, not committed).
- **Result:**
  - `monotype_universe` for the trimmed library (`map`/`cells`/`row_to_grid`/`eq`/`if`) is small — 10 types at closure depth 2 — so universe-size inflation from `List` nesting is _not_ the driver here.
  - The memo key (`scope, contexts, budget, enclosing_target`) collapses across all ~10 grounding candidates for `map`'s codomain in this task, because `enclosing_target` for this task never propagates through `_propagate_elementwise` (top-level target is a `Grid`, not tuple-shaped) — so eager grounding here is _not_ paying a 10x multiplier from redundant re-searches, contrary to the naive expectation.
  - **Metrics:**
    - full library (`+from_cells+width+height+read`), depth=4: 97,803 considered, 1.26s, unsolved
    - full library, depth=5: 17,590,552 considered, 116.0s, unsolved
    - trimmed library (no `read`/`width`/`height`, `row_to_grid` instead of `from_cells`), depth=5, pool=1000: 1,453,833 considered, 8.2s, unsolved
    - trimmed library, depth=6, pool=1000: 9,873,756 considered, unsolved (completed within a 280s window)
    - the target program was hand-built and evaluated directly (bypassing search) and is confirmed semantically correct — this is a search-budget shortfall, not an unreachable-program bug
  - `test_higher_order_primitives.py::test_map_solves_recolor_end_to_end` was rescoped to a plain `COLOR -> COLOR` primitive (`next_color`) instead of the synthesized `eq`/`if` body — solves in <10ms at depth=4 — trading away the "map composes with a synthesized conditional" claim (not this slice's core claim) to keep the payoff test fast rather than chasing the eq/if-body cost further.
- **Interpretation:** the real cost driver is ordinary bottom-up round-over-round growth (the same ~180x/depth pattern seen independently in the `fold` toy test), not `eager_grounding_over_universe`'s per-grounding multiplier, which turned out to be memoization-collapsed in this case. A _lambda body requiring composition depth ≥6_ is expensive regardless of whether an unpinned hole is involved — `eager_grounding_over_universe` only compounds this when `enclosing_target` propagation actually fires per grounding candidate (giving each one a genuinely distinct memo key), which didn't happen here.
- **Next:** if a future task needs both a genuinely-unpinned hole _and_ real depth (≥6) in its synthesized body, expect this same wall; worth revisiting whether `eager_grounding_over_universe` should cap/prioritize `state.universe` by relevance (e.g. atomic types before list-nested ones) if that combination becomes load-bearing rather than incidental.

---

## 2026-07-12 — E11: perceive→transform: recolor_bg derived from {map_color, most_common_color}

- **Commit:** working tree atop d53ca8f.
- **Notebook:** [experiments/2026-07-12-e11-e12-gap-climbing/](experiments/2026-07-12-e11-e12-gap-climbing/) — full write-up + run links
- **Question:** the first _perceiver-consuming_ abstraction (EXPERIMENT_QUEUE.md's gap-climbing row) — can the loop derive `recolor_bg(g,c) = map_color(g, most_common_color(g), c)` from the floor `{map_color, most_common_color}`, with `c` a genuine free parameter (not a hardcoded literal)?
- **Design finding en route (worth recording on its own):** the naive corpus design — one example per task, background _and_ target color both varying freely — cannot work under the current WAKE mechanics, and this generalizes beyond this experiment. `execute.py` keeps only `ranked_programs[0]`, the _cheapest_ solving program per task (`extraction.py` ranks by node count); once any `COLOR` constant source exists, `map_color(g, <literal-bg>, <literal-target>)` (1 application) is always cheaper than the composition through `most_common_color` (2 applications) and always reachable wherever the composition is — so WAKE never even retains the perceiving program, for any per-task variation strategy, as long as a task's goal test is satisfiable by one fixed pair of literals. The fix: a task's goal test is over _all_ its train examples at once (`sig == target` on the full training signature, `extraction.py`), so varying the **background across a task's own train examples** makes no single literal source work for that task at all — only `most_common_color(g)`, evaluated per example, can track it. Varying the **target color across different tasks** (fixed within each) then gives antiunify the one differing literal to hole into the free parameter. Background-within/target-across is not symmetric: target varying _within_ a task either leaves it unsolved (wasted, if uncorrelated with input) or collapses to a _different_, zero-free-param abstraction (if it does correlate) — never `recolor_bg(g,c)` with `c` free.
- **Ran:** new `taskgen` generator + `StudySpec` `perceive-transform` (`execution/studies.py`, `taskgen/generators.py`): library `{map_color, most_common_color}`, `constant_sources=("finite-enumerate",)` (needed so any target color is reachable, not just ones already present in an input — `harvest-from-instance` only reads inputs, never outputs). 5 train tasks (target colors {0,2,5,7,9}, 3 examples each, backgrounds {3,4,6} shared across every task) + 2 held-out tasks (unseen target colors {1,8}). `arc-lab run-study perceive-transform`; locked in `tests/program_search/test_locks.py`.
- **Result:**
  - Sleep mints exactly `abs0`, structurally `map_color(#0, most_common_color(#0), #1)` — **byte-identical to the target template**, not just a behavioral match (grid var-shared across both call sites, `_close_template`'s Input-collapsing — the "var-sharing (grid ×2)" abstractor demand, confirmed free).
  - **Enablement + full transfer**, shallow (depth-2, one-application) budget: raw `L1` solves **0/5 train, 0/2 eval**; learned `L2` solves **5/5 train, 2/2 eval** — including both held-out, _unseen-during-training_ target colors, confirming `c` is genuinely a free parameter, not memorized.
  - Deep (depth-3) budget: `L1` already solves 5/5 + 2/2 via raw composition (as expected — the composition is real, just unreachable at depth 2 without the abstraction).
  - **Metrics:** `make check` green (468 passed, 5 skipped); considered nodes at shallow budget: `L1` 590 (train) / 236 (eval) vs `L2` 640 / 256 (learned primitive adds negligible search cost).
- **Interpretation:** the loop derives a real perceiver-consuming abstraction end to end, confirming gap-climbing is runnable today for this phenomenon — but only once the corpus is designed around the WAKE-time cheapest-wins selection rule, which otherwise silently forecloses ever discovering a perceiving composition whenever an equally-valid literal substitution exists (a WAKE-time sibling of the E8 compression/reusability divergence, one step earlier in the pipeline). Multi-example-per-task corpora are the general tool for forcing derivation over literal substitution.
- **Next:** the two remaining queued gap-climbing rows — **layered abstraction** (L2 built on a learned L1) and **learned intermediate type** (`Mask`-constructing-and-consuming) — plus watch for the same cheapest-wins trap wherever a target abstraction has a free literal parameter alongside a derivable one.

---

## 2026-07-12 — E12: layered abstraction: L2 recolor_flipped minted using the L1 learned in the same run

- **Commit:** working tree atop d53ca8f (same session as perceive→transform, above).
- **Notebook:** [experiments/2026-07-12-e11-e12-gap-climbing/](experiments/2026-07-12-e11-e12-gap-climbing/) — full write-up + run links
- **Question:** the second queued gap-climbing row — can the wake-sleep loop build a **second-generation** abstraction directly on a first-generation one it _just learned_, in one recorded LEARN run: `abs0 = rot180 = flip_h(flip_v(g))`, then `abs1 = recolor_flipped(g,a,b) = map_color(rot180(g),a,b)` composed on `abs0` — not merely a corpus rewrite, but a genuine second WAKE search that needs the grown library to even reach the task?
- **Ran:** new `taskgen` generator + `StudySpec` `layered-abstraction`: library `{flip_h, flip_v, map_color}`, one fixed WAKE budget for the whole LEARN run (`max_depth=3`, two applications) chosen so raw composition reaches `rot180` (two applications) but not `recolor_flipped` (three) — only once `abs0` exists does `recolor_flipped` collapse to two applications and become reachable at the _same_ budget. Corpus mixes both task types: 4 train + 1 held-out `rot180` tasks (single example each — no literal-constant shortcut is possible for a pure `GRID→GRID` transform, so no perceive→transform-style corpus engineering was needed here), 4 train + 1 held-out `recolor_flipped` tasks (`(a,b)` fixed within a task, varied across tasks — the same free-parameter mechanism as `perceive→transform`, minus the var-sharing complication since each param is used once). `arc-lab run-study layered-abstraction`; locked in `test_locks.py`.
- **Result:**
  - Sleep mints `abs0 = flip_h(flip_v(#0))` in iteration 0 (from the 4 solved `rot180` tasks alone — `recolor_flipped` tasks are simply absent from that iteration's solved corpus, unreachable at the shared budget). Iteration 1's WAKE, now with `abs0` in the library, genuinely re-solves the `recolor_flipped` tasks (confirmed via the grid, not inferred): sleep then mints `abs1 = map_color(abs0(#0), #1, #2)` — **built directly on `abs0`**, both behaviorally matching their targets.
  - **Partial-then-full enablement**, at the one shared learn budget: raw `L1` solves **4/8 train, 1/2 eval** (the `rot180` subset only); learned `L2` solves **8/8 train, 2/2 eval** — both task types, including the held-out unseen `(a,b)` pair, confirming `recolor_flipped`'s parameters transfer.
  - Deep budget (`max_depth=4`, three applications): `L1` already solves 8/8 + 2/2 via raw composition, as expected — confirms the composition is real, just one application too deep to reach at the learn budget without `abs0`.
  - **Metrics:** `make check` green (469 passed, 5 skipped). Considered nodes at learn budget: `L1` 47,464 (train) / 11,866 (eval) vs `L2` 183,648 / 45,912 (the grown library's extra vocabulary costs ~4x search — `abs0`/`abs1` widen round-0 leaves without narrowing anything, unlike E1/perceive→transform's negligible deltas).
- **Interpretation:** multi-generation abstraction — L2 built on a _just-learned_ L1, within one recorded LEARN run — works end to end, confirming the wake-sleep loop's iteration structure (not just `GreedyMDLLearnEngine`'s internal corpus-rewrite loop) is what does the work here: `recolor_flipped` was absent from iteration 0's solved corpus entirely, so `abs1` could only be mined after a _second_ WAKE search that needed `abs0` to reach its target at all. The considered-count jump (unlike the two single-generation experiments) is the first measured cost signal of a growing library: unlike E1/perceive→transform where the learned primitive strictly _replaces_ search depth, `abs0`/`abs1` are additive vocabulary the round-0 leaf set must now carry on every task, including tasks that don't use them.
- **Next:** the last queued gap-climbing row — **learned intermediate type** (`Mask`-constructing-and-consuming, the deepest phenomenon: an abstraction that both builds and consumes a learned type). Watch whether the same considered-count growth compounds with three generations of vocabulary.

---

## 2026-07-13 — `make check` speedup: cached-signature reuse + trimmed two study locks to their asserted cells

- **Commit:** working tree atop 2759b69
- **Ran:** `compute_signature` now reuses cached child signatures for `Apply` nodes instead of re-walking whole subtrees (`search/signature.py`, `search_engine.py`); the two study-based locks (`layered-abstraction`, `perceive-transform`) now compute only the 4 grid cells they assert on instead of `run_study`'s full 12-cell diagnostic grid.
- **Result:** `make check` ~120s → ~49s, all locks unchanged. Details in the commit / `tests/program_search/execution/test_locks.py`.

---

## 2026-07-14 — arc1 preset partition census: `d4` / `synth` (real-ARC bearings, `sym` deferred)

- **Commit:** working tree atop 6066040
- **Notebook:** [experiments/2026-07-14-capability-census/](experiments/2026-07-14-capability-census/) — full write-up + `analyze-run` artifacts
- **Question:** the first EXPERIMENT_QUEUE.md real-ARC bearings row — read the search-cost partition (`by_category`/`by_provenance`, outcome mix) for as-shipped presets over full `arc1-train`, not just their solve count.
- **Ran:** `arc-lab search d4 --corpus arc1-train`, `arc-lab search synth --corpus arc1-train`, then `arc-lab analyze-run` on each. `sym` deferred to a follow-up (measured ~32 CPU-min full-corpus per the 2026-07-13 entry above).
- **Result:**
  - Solve counts match the existing locks exactly and `synth`'s solved set is a strict superset of `d4`'s (7 D4 + 4 atomic) — reproduces the 2026-07-06 finding byte-for-byte.
  - **Metrics:**
    - `d4`: 7/400 solved, considered=3,600, deduped=490, evicted=0, pruned=0
    - `synth`: 11/400 solved, considered=2,704,865, deduped=2,090,002, evicted=431,052, pruned=0
  - `d4`'s partition is trivial (one library = one category `geometry`, one provenance `base`) — `anti_transpose`/`rot270` are considered on every task but never `accepted` (live vocabulary, zero payoff on this corpus, not a Table-A island).
  - `synth`'s cost is dominated by `constant`-leaf x `map_color` combinatorics: `constant` + `color` categories account for ~99%/~97% of total considered respectively, vs. `geometry` ~19% and `scaling` ~14% — the first quantified confirmation of `docs/CONFIG-DEFAULTS-2026-07-11.md`'s "finite-enumerate is multiplicative under variadics" cost note. `evicted` is 0 for `d4` (no constants, `max_pool=100` never fills) vs 431k for `synth` (pool-cap pressure once constants blow up the frontier); `pruned`=0 for both (no `Constraint`s registered, matching the default).
- **Interpretation:** the partition-read gives a mechanistic account behind the known solve counts — for `synth`, essentially the entire 2.7M-candidate cost is constant-enumeration x color-recolor, not the D4 geometry it inherited. For `d4`, two of eight primitives are dead weight _for this corpus specifically_ (fire but never win), a materially weaker claim than a true vocabulary island.
- **Next:** run `sym` full-corpus and append; then region-logic (`MASK_BASIC`) and perceiver-recolor (`PERCEIVE_TRANSFORM`) censuses, once their `Config`/bundle-search machinery lands, land in the same notebook folder.

---

## 2026-07-15 — E13: fundamental-floor grain contrast (rot180 across GEOM / UNIVERSAL_FLOOR / MINIMAL_COMPLETE_FLOOR)

- **Commit:** working tree atop 6066040
- **Notebook:** [experiments/2026-07-15-fundamental-floor-grain-contrast/](experiments/2026-07-15-fundamental-floor-grain-contrast/) — full write-up, run links, and the generator bug found en route
- **Question:** EXPERIMENT*QUEUE.md's reframed top row — read the search-behavior object (not solve-count) for the \_same* rot180 target across three floor grains (`GEOM` direct primitive → `UNIVERSAL_FLOOR` build_grid+arithmetic → `MINIMAL_COMPLETE_FLOOR` build_grid+read+if+eq, no arithmetic), on both a designed task (3x3 and 10x10) and a real `arc1-train` task (`3c9b0459`). Decomposes **expressible → findable → cheap** on one target.
- **Ran:** 3 new presets (`geom`/`universal-floor`/`minimal-complete-floor`, `execution/presets.py`, built via `resolve_bundle`) + 1 new `taskgen` generator (`grain-contrast`); `arc-lab search <preset> --corpus grain-contrast --track-all` for the designed legs, a probe script for the real-task leg (no CLI single-task corpus selection exists). Depth swept manually (3/4/5) rather than jumping to a large budget, after the first `universal-floor` attempt at its preset's default depth=6 was killed after several minutes without even finishing its first (smaller) task.
- **A generator bug found and fixed en route:** the first designed-task content formula, `(row*3 + col*7 + offset) % 10`, has `3+7=10` — making `transpose` **algebraically identical** to `rot180` for that formula, at any size/offset. The first `geom` run "solved" both tasks via `transpose(input)`, not `rot180` — a false positive. Fixed to `(row*1 + col*2 + offset) % 10`, verified collision-free against all 7 other D4 members before trusting it.
- **Result:**
  - `GEOM` solves trivially (considered=8-16), as expected — rot180 is a direct 1-application primitive.
  - Designed and real 3x3 tasks track closely at matched depth (depth=4): `universal-floor` 190,054 (designed) vs 210,758 (real) considered, within ~10%; `minimal-complete-floor` 14,001 vs 19,973 — the designed task is a faithful stand-in for the real one.
  - Neither `universal-floor` (through depth 4) nor `minimal-complete-floor` (through depth 5) solved rot180 at a budget cheap enough to be practical. Per-round growth ~17-24x throughout, broad-based across every `by_category` bucket (not one runaway mechanism) — `lambda-synthesis`'s recursive body search (re-triggered from scratch every outer round `build_grid` is offered) makes even a modest target expensive to _find_.
  - **Metrics:** `universal-floor` depth3→4: considered 70,718→1,290,178 (~18x); `minimal-complete-floor` depth3→4→5: 4,329→102,982→1,729,406 (~24x, ~17x). 3x3-vs-10x10 scaling at this shallow depth: a similar ~6x for both floors (not yet showing `MINIMAL_COMPLETE_FLOOR`'s predicted linear-lookup-chain divergence from missing `lt`/`gt`).
- **Interpretation:** the grain ladder is real — `GEOM` is free, `UNIVERSAL_FLOOR`/`MINIMAL_COMPLETE_FLOOR` are both "expressible" but not cheaply "findable" at a depth affordable to reach in this session, and the design/real-task match validates the designed-task methodology going forward. The predicted floor-specific divergence (arithmetic vs. linear-decision-list lookup) never got a chance to show up — neither floor reached the depth where the actual coordinate-remap construction is attempted; what's visible instead is generic literal-leaf-count growth. `minimal-complete-floor`'s near-converging 3x3/10x10 considered-counts (679,288 vs 1,050,118 at depth 5, not the wide gap a pure depth argument predicts) raises a live question — is `max_pool=200` evicting the correct partial construction rather than depth being the binding constraint. Chose not to chase a full solve at deeper budgets given the ~20x/round cost trajectory.
- **Next:** a `max_pool` sweep (independent of depth) to test the eviction-vs-depth hypothesis for `MINIMAL_COMPLETE_FLOOR`, if this floor becomes load-bearing later. The 3 presets + generator are now permanent reusable machinery for any future experiment on this floor ladder.

---

## 2026-07-17 — Derivability DAG: ladder mining goes from ideation to enumeration

- **Commit:** working tree atop a665ddb
- **Notebook:** [experiments/2026-07-17-derivability-dag/](experiments/2026-07-17-derivability-dag/) — full write-up + probe scripts and outputs
- **Question:** can the Abstraction Ladder batch (docs/abstraction*ladders/ABSTRACTION-LADDERS-2026-07-16.md) be supplied by \_enumeration* — computing every shipped primitive's minimal behavioral derivation over (a) the rest of the library and (b) each bundles-doc floor, then reading ladders off the derivability structure?
- **Ran:** a throwaway typed bottom-up enumerator over `BASE_PRIMITIVES` impls (observational-equivalence dedup on a deterministic battery, disjoint holdout verification; mono first-order scope, 55/71 prims, no constant leaves). Pass A: leave-one-out; Pass B/C: 12 floors × targets, then floor+rung re-derivation; Pass D: composite targets (quad/tile towers, E11/E12 compositions) → full static jump matrices.
- **Result:**
  - Pass A: the mono library = a **redundant shell** (~21 prims derivable ≤3–4: D4 clique, mask De Morgan clique, `min`/`max`/`add`/`mod` identities, `map_color` ↔ `swap_colors` — _mutually_ derivable given mask machinery, refining `_PRIMITIVE_BUNDLES.md` Constraint 5) + an **atom core** (~30, incl. all of layout and parameterized iteration).
  - Pass B/C: registry-mined ladders are **height-2 only** — `rot180` over `{flip_h, transpose}` (4→2, four interchangeable rungs), `swap_colors` over `mask_basic_gen` (+`map_color`, 4→3). Cross-domain floors derive nothing: the shipped registry is compositionally flat; taller ladders require composite targets.
  - Pass D: **six statically certified ladders**, incl. two height-3 (`quad-tower`: mirror_h→quad→quad2, jumps 2/3/2, raw ≥6-censored; `tile-tower`: row3→grid3x3→grid9x9, jumps 2/3/2), a diamond-topology arm (`perceive-tower`), and `e12-layered`, whose static profile **reproduces E12's measured ground truth** (rung 2; top 3→2) — the probe's validation anchor.
  - **Skip paths found in hand-designed ladders:** intended `quad` template was size 4; true minimum is 3 (`mirror_h(concat_v(g, flip_v(g)))`). Same for `grid3x3 = row3(col3(g))`. Designer-computed jump depths were wrong; only enumeration catches this.
  - **Metrics:** pass A ~1 min; per-floor pass B cells 0–7s; holdout battery caught 3 would-be false positives during battery design (nested masks, bool pairs, full-bbox masks).
- **Interpretation:** ladder supply is now mechanical: registry mining gives calibration-height ladders, composite-target certification gives anchored height-3 ones, and the static half of the design doc's §6 Ladder Certificate (jump depths `d_i`, double-jump/rung-necessity, skip paths) is computable today by the probe. Static size ≠ search cost (and the probe's tree-size accounting ≠ engine generations — caveat in the notebook); promotion to the batch still requires taskgen + real runs.
- **Next:** promote `quad-tower` as ladder #1 (taskgen generator + StudySpec patterned on E12's); `d4-tower` as the calibration ladder; fold "compute `d_i` by enumeration, never by hand" into the Ladder Linter design.

---

## 2026-07-18 — Abstraction Ladder machinery built; ladder #1 (`al1-mirror`) runs end to end and is ADMITTED

- **Commit:** `11c5457` (branch `feat/abstraction-ladders-machinery`; a 6-commit stack from `a462abc`). Design: `docs/abstraction_ladders/ABSTRACTION-LADDERS-2026-07-16.md`; build plan `~/.claude/plans/i-think-you-compressed-scroll.md`.
- **Built:** the full §6 machinery. Substrate atoms `compositional_depth` (leaf=0, = the engine's generation accounting) + `unfold_program`/`substitute_params` (the inverse of `make_abstraction`; the Linter's `d_i`/inlined-double-jump are now enumerated, never hand-computed — folds in the prior entry's "Next"). Telemetry atoms: the **solution sink** (every goal-match at absorption, keep-cheapest-K), the **per-generation funnel**, and **sleep-cost counters** — all serialized. `LadderSpec`/`Rung`/`TopRung` + `.lint()` -> `LadderShape` (static sandwich checks); `run_ladder` + `certify` + `create_ladder_report`; `arc-lab run-ladder`.
- **Two isolated engine correctness fixes en route (each its own commit; ALL locks unchanged, latent on the current corpora, proven via constructed tests):** (1) eviction-loss (`00fa05a`) — the search returns the globally-cheapest solution from the sink, so a solution found then evicted by `max_pool` is never lost (`solved` now sink-based; the `solved & accepted==0` gap is the eviction-loss signal); (2) `attempts_per_test` wakeup (`4e85fba`) — `ranked_programs` is the full cheapest-ordered ranking, so `predict` actually uses its 2 attempts.
- **Ladder #1 result — ADMITTED.** Climb (max_depth=3, `AntiunifyPairs`): iter0 (L0) solves the rot180 tasks -> mints `abs0`=rot180; iter1 (L1) solves mirror_recolor -> mints `abs1`=mirror_recolor; iter2 (L2) solves the top -> converges. Both rungs recovered behaviorally; certificate: all jumps tractable, zero skip paths, demonstration health 1.0. Raw top intractable at the reference budget (`d_raw`=4 vs depth-2 jumps). Marginal laddered ~53.7k considered, end-to-end ~200k, raw censored at 6.7k (unsolved at L0).
- **Finding — the certificate caught an algebraic collapse the static lint could not.** The first top design `flip_v(mirror_recolor(g,a,b))` collapses via the D4 group law (`flip_v∘rot180 = flip_h`) to a 2-application program, so the "raw" top was solvable at L0 and the certificate correctly REJECTED the ladder (`no_skip_paths[2]=False`, `raw_solved=True`). Fixed to a second _independent_ recolor, `map_color(mirror_recolor(g,1,2),3,4)`, which does not collapse. This is an E13-class shortcut (cf. transpose≡rot180) and validates the design's static-lint + empirical-certificate split: lint passes on the intended solution; only the empirical oracle-chain run exposes the shortcut.
- **Metrics:** `make check` green (459 passed, 1 skipped); every prior lock unchanged; new slow lock `tests/program_search/ladders/test_run.py`.
- **Next:** the amortization _considered_-ratio needs a calibration / deeper-budget raw run to be meaningful (raw is censored at the reference budget) — depth compression (4->2) is the honest headline for now. Then RQ2 families (height / jump-depth / tasks-per-rung, with fan-in>1 to leave the pure-telescope regime) and RQ3 (the sink already gives top-K solution retention nearly free). Deferred report views: vocabulary tax, `b_eff`, break-even horizon.

---

## 2026-07-20 — Budget stop limits (`considered_limit` / `solution_limit`) + lazy composition

- **Commit:** working tree atop `4674fa0`. Design: `docs/abstraction_ladders/ABSTRACTION-LADDERS-2026-07-16.md` §6.9; build plan `~/.claude/plans/i-think-we-need-composed-clover.md`.
- **Question:** the design doc deferred a `max_considered` cap ("would make censored baselines and cost-matched controls much cleaner") and early stop. The stronger motivation turned out to be ladder construction: `depth_limit` was doing two conflicting jobs — the ladder's _semantic_ validity window AND the compute guard — which is why picking one for a new ladder was guesswork.
- **Built:** `Budget.considered_limit` / `Budget.solution_limit`, each with a `*_mode` (`immediate` | `generation-end`). One predicate at the `_absorb_one` choke point + a `BaseException` unwind, per-frame sub-pool repair, and a partial-round `incomplete` flag. Preceded by **lazy composition** (`_enumerate` pulls `_compose` instead of `list()`-ing it) — behaviour-preserving with no limit set, which the locks prove.
- **Result:**
  - **Lazy composition is a prerequisite, not an optimization, for early stop.** Solutions are detected _inside_ absorption, and a round was fully composed before any of it was absorbed — so on the old structure `solution_limit` could have saved **zero** composition cost in either mode, in the largest round. This was not anticipated in the design doc.
  - **Metrics** (`synth` on `arc1-train`, 400 tasks; all four arms solve **11/400**, the lock):
    - baseline considered: 2,704,065
    - `solution_limit=1` (generation-end): 2,632,841 — 11 tasks flagged `stopped_early`, **0** incomplete rows
    - `solution_limit=1` (immediate): 2,632,003 — 11 `stopped_early`, 11 incomplete rows
    - `considered_limit=1000`: **400,000 = 400 × exactly 1000** — every task lands on the cap, 400 `censored`
  - **Per-task, the overshoot is enormous where it exists.** On one solving task (`3c9b0459`, `depth_limit` 3): 54,447 considered uncapped vs **127** (generation-end) and **18** (immediate), same retained program — the solution sat at candidate index 17. Corpus-level saving is only ~2.7% because just 11/400 tasks solve at all; early stop pays off per _solved_ task, not per task.
  - **Certificate hazard found and closed.** `no_skip_paths` / `tractable_jumps` are read off _unsolved_ sets, and a censored search is unsolved too — so censoring would have silently converted "we ran out of budget" into a skip-path **PASS**. `no_skip_paths` is now tri-state (`None` = inconclusive) and `None` fails admission exactly as `False` does.
  - **`_estimate_raw_cost` needed the `incomplete` filter.** A truncated final round reads as decay, not growth: unfiltered it returns ~1.1e3 where the truth is 1.1e5 — a plausible-looking number, no error raised. (An earlier guess that it would _invert_ the low/high bracket was wrong: `min`/`max` already normalize the ordering. The failure is silent wrongness, not visible inversion.)
- **Interpretation:** ladder construction can now separate the two jobs — set `depth_limit` from the ladder's semantics, cap compute with `considered_limit`, and read "did any search censor?" as "this ladder is less tractable". That is only sound because of the tri-state certificate. Early stop additionally removes the _overshoot_ term from §2's laddered-cost breakdown, and its two modes measure §2's two cost-to-first variants directly, rather than as the post-hoc read the 2026-07-17 decision anticipated.
- **run_id churn:** adding fields to `Budget` moves every `run_id` (serde emits keys unconditionally) — accepted, with the 2026-07-18 `depth_limit` rename as precedent. `runs/` is a regenerable cache; no pinned `run_id` exists in any committed artifact; old records still deserialize (absent keys fall back to defaults).
- **Metrics:** `make check` green (513 passed, 1 skipped); every prior lock unchanged, including at the isolated lazy-composition commit.
- **Next:** decide per ladder whether the reference config should carry a `considered_limit` (it enters `run_id` _and_ becomes part of what the certificate is anchored against, so the design doc's depth-only "validity window" would gain a second dimension). Then re-run al1-mirror's raw baseline with a deeper `depth_limit` + `considered_limit` guard — the amortization _considered_-ratio the 2026-07-18 entry left open.

---

## 2026-07-20 — Ladder batch screen: AL2-AL20 built and run; two collapse families found, six-ladder rebuild certifies clean

- **Commit:** `eb06d8e` (back-filled 2026-07-21 — the screen's findings had lived only in registry-module docstrings). Register: `docs/abstraction_ladders/LADDERS.md`.
- **Question:** the batch needed more than one ladder. Build out the candidate slate (AL2-AL14: calibration, height/fan-in/colour arms, and the four controls), run the empirical certificate on all of them, and see which survive.
- **Ran:** 19 new ladders (`LadderSpec` + generator + committed testbed each) and the full `run_ladder` activity on all 20, with a `considered_limit=50000` compute guard applied ad hoc (**not** carried in the committed specs — see run_id note below). 245 recorded runs under `runs/2026-07-20/`.
- **Result:**
  - **Two collapse families, both general rather than incidental:**
    - **Self-similar doubling telescopes** (al3, al7, al9, al11). ANY self-contained doubler satisfies `r_2k == r2^k`, so every even rung is a depth-2 composition of the rung two below. Verified **transform-independent** — a plain-`concat` tower (asymmetric seed, no flips) collapses identically — so no seeding or reordering fixes it; a non-collapsing tall tower needs distinct, non-composing rung ops, ie a richer floor.
    - **Family-B perceiver collapse** (al5, al6, al8). Cheap perception erases the search-cost gap the ladder exists to bridge. al8's floor solves the top outright: `map_color(flip_v(input), least_common_color(input), most_common_color(input))`. al5 likewise solved raw at L_0 despite a static `d_raw` of 8.
  - **The rebuild worked.** al15-al20 were designed directly against those two findings (alternate a combine op with a non-composing modifier; entangle each rung with the one below; keep `map_color` a component, never the whole competence) and **all six certify clean with full rung recovery**, including the first tall non-collapsing tower (al17, h=4) and the first goal-layer recombination (al18/al19).
  - **Controls:** al10 correctly rejected as designed; al12 **admitted with a failed climb** (one demo, so `AntiunifyPairs` cannot pair) — the batch's only failed climb and the reference for reading every success. al9/al11 are compromised: they inherit al7's collapsing spine, so their decoy / greedy-trap questions remain unanswered.
  - **Metrics:** 20 ladders run — 9 admitted (al1, al2, al12, al15-al20), 7 rejected on structure, 1 inconclusive (al14, fully censored), 3 controls resolving as designed or compromised. Static lint passes 18/20 (al10, al12 fail by design).
- **Interpretation:** **structure is the binding constraint, not learning.** Every rejected ladder except al5/al6/al13 recovered its rungs — al7/al9/al11 recovered 5/5 and were still rejected. The climbs largely worked; the _ladders_ were unsound. That inverts the expected failure mode, and it is why the rebuild could be designed on paper (against the collapse families) and certify first time.
- **run_id churn / reproducibility gap:** the `considered_limit=50000` guard used for al1-al14 is not in the committed specs, so `arc-lab run-ladder al3-quad-symmetrize` recomputes uncapped instead of hitting cache. al15-al20 ran as-committed and are cache-clean. This is the open "should the reference config carry a `considered_limit`?" decision from the entry above.
- **Next:** read the results — only al1/al2/al5 had reports generated (done 2026-07-21, below).

## 2026-07-21 — Reading all 20 ladders: the RQ1 estimator's usable range, "r1 never pays", and al14's literal collapse

- **Commit:** `f0fe2e1`. Notebook: [experiments/2026-07-21-ladder-batch-analysis/](experiments/2026-07-21-ladder-batch-analysis/).
- **Question:** 17 of 20 ladders had run but never had a report generated. What does the batch say read across all of it — and specifically, how tractable would al14's climb really be? (al14 is statically the strongest ladder: `d_raw` 54, 18x depth compression, the only validity window wider than 1.)
- **Ran:** cache-only report generation for all 20 (`artifacts/batch_reports.py`), cross-ladder tables (`cross_ladder_tables.py`), and two al14 probes — a bounded depth-3 reachability run and a structural diagnosis (`al14_reachability.py`, `al14_literal_collapse.py`). No ladder was re-run.
- **Result:**
  - **The RQ1 raw-cost estimator degrades to noise past ~3 rounds of extrapolation.** Bracket width (hi/lo) vs rounds extrapolated: 2 → 1-10x · 3 → 20-62x · 4 → 250x · 6 → 111-1,125x · 8 → **3.6e7** · 51 (al14) → **7.6e70**. Since design doc §5.3 makes estimation _the method_, this bounds the whole RQ1 programme — and **al2, the designated validation arm, sits at the trivial end** (amortization 1x-1x, raw 31 vs laddered 27), so it cannot validate the regime everything else depends on.
  - **A third of all measured cells are censoring artifacts**: 172/532 (32%), all-or-nothing per ladder — al3/al4/al5/al6/al13/al14 are 100% censored (hence their vocabulary tax reads exactly `1.00`), every other ladder 0%. Those six have structural verdicts and **no usable cost data**.
  - **r1 never pays for itself.** Own-task speedup (the uncensored measure) is **1.0-1.7x for r1 on every uncensored ladder**; r2+ delivers 1.8x-52x and compounds with level (al17: 1.0 → 30.4 → 52.1). Structurally forced — r1's tasks must be floor-solvable to be learnable, so r1 has no gap to close.
  - **The loop-overhead factor is near-tautological**: it tracks wake-iteration count almost exactly (2→2.00, 3→3.00-3.11, 4→4.04, 5→5.27) because cost-paid-full is nearly task-independent. §2's four-way decomposition is not visible in it without early stop.
  - **Vocabulary tax is an arity phenomenon**: param-free geometric rungs cost 1.03-1.43x with `b_eff` flat (15.3 → 16.0); colour-parameterised ones cost 3.8-4.4x with `b_eff` doubling (al6: 20.8 → **110.5**). Generalises al1's single-ladder finding.
  - **Off-chain necessity, not fan-in, is the recombination axis.** `off_chain_top_solved` is False only for al14/al15/al18/al19 — the recombining tops need _both_ rungs, while al20's telescoping top is off-chain solvable. This resolves the al19/al20 labelling problem: both tops score fan-in **2** under §2's definition, so fan-in cannot distinguish them; shared-input recombination vs chaining is the real property.
  - **al14 — a THIRD collapse family: literal collapse of free INT params.** Compute was never the obstacle (generation 3 costs ~8.0M compositions, ~7 min/task at the measured 18,847/s; the 50k guard was 3 orders too small and killed it in generation _1_). The real failure: **r1 solves at depth 2, not 3** — `set_cell(set_cell(input,1,0,0), 0,0, read(input,1,0))` — because "free params fixed within-task" plus enumerated INT constants makes `sub(ROW,1)` dead weight. One retained solution is an outright **task collision** (reads (0,2) to write (1,1); works only because those cells share a colour in both examples — exactly what the §6 collision check should catch). Sleep therefore mints `set_cell(set_cell(#0,#1,#2,0), #2,#2, read(#0,#3,#4))`: **5 params instead of 3**, semantically wrong (row and col fused), and **289x more expensive to use** (2000x17^4 = 167M/round vs 578k).
- **Interpretation:** al14 is **mis-specified, not merely expensive** — iteration 0 would run in ~1 hour, mint the wrong abstraction, and wall at r2. Its advertised 18x compression and width-3 window are computed on a template the search bypasses at depth 2. The general lesson generalises past al14: a rung whose free parameters are indices into an enumerated constant domain loses exactly the depth that computing those indices was meant to add. The corollary is uncomfortable and probably the most valuable thing here — making the _relation_ load-bearing requires the index to vary within-task, ie to be **perceived**, so **on a cell-level floor "move up" is not learnable as a relation without perception**.
- **Metrics:** 20/20 reports generated in ~10s cache-only (9.3s of it al14). al14 measured throughput 18,847/s at `max_pool=2000`, 5,231/s at 20,000; pool after generation 2 = 10,615 grids / 17 ints / 14 colours.
- **Next:** **do not** re-run al14 as specified (this retracts the day's earlier "highest-value re-run" call) — retire it with the family-3 finding, or rebuild with a perceived index. Check whether the §6 task-collision check is implemented at all. Re-run the six censored ladders at a guard that lets them finish, since they have no cost data. Build a calibration ladder deep enough to test the estimator where it fails (`d_raw − depth_limit >= 4`).

## 2026-07-22 — Lint hardening: the collapse families become static checks (11/20 now fail lint, witnesses printed)

- **Commit:** `72eb123` (sequence `0c63c70..72eb123`). Checks inventory: [docs/abstraction_ladders/LADDER-CHECKS-2026-07-21.md](docs/abstraction_ladders/LADDER-CHECKS-2026-07-21.md); plan of record: [docs/abstraction_ladders/AL-PLAN-2026-07-22.md](docs/abstraction_ladders/AL-PLAN-2026-07-22.md).
- **Question:** the 2026-07-21 read-across established collapse = "the intended template is not the cost-minimal representative of its signature class" and named two static-checkable mechanisms (train-constant subterms; equational shortcuts). Can they be linted — and what do they find on the existing 20?
- **Ran:** three new lint checks + five review fixes, then the batch lint, observe-then-lock. `constant-subterm` (every composite scalar subterm of every stated solution, unfolded to floor, evaluated across the task's train examples; error iff the value is in the ladder's own `policy_constants` domain, warn otherwise). `if-condition-varies` (both truth values per If; dormant, no ladder branches). `rewrite-shallow` (`analysis/equations.py` curated laws + import-time-derived D4 table; `analysis/rewrite.py` bounded normalize-and-enumerate; every witness behaviorally confirmed; caps are silent passes).
- **Result:**
  - **The static checks reproduce the certificate's structural verdicts, witnesses printed.** al3: `tower == quad(quad g)`. al7/al9/al11: `tall4 == stack2(stack2 g)`, `wide8 == stack2(wide4 g)`, top via `stack2(tall4 g)`. al14: `sub(1,1)/sub(2,1)/sub(3,1)` train-constant enumerable (the probe diagnosis that took a day of scripts, now 0.3s of lint). al10's designed skip now prints its witness (`flip_h(transpose(flip_h(transpose(input))))`).
  - **Two NEW static findings beyond the recorded verdicts:** al6's `norm_mirror` hides a `flip_h∘flip_h` cancellation (r1 contributes nothing — witness `map_color(flip_v(#0), most_common_color(#0), #1)`); al4/al5/al6's perceiver calls are train-constant on their flagged demos (search substitutes the literal — statically explaining al5's zero rung recovery). al8 carries the same constancy at WARN tier because it mints no constants — the "does the beating literal exist in this ladder's own search?" clause working.
  - **No false positives:** al1/al2/al15–al20 stay clean, including the constant-enumerating ones; a naive-BFS rewrite engine was rejected at design time (cannot reach al7's witness within caps) in favor of normalize-and-enumerate.
  - **Metrics:** failing-lint set 4/20 → 11/20 (all known-broken; clean set = the usable arms). Batch lint ~8s. al14 lint 40s → 0.34s after memoizing `unfold_program` by node identity (its unfolded top is ~3.4M node occurrences but few distinct nodes; the naive memo aliased recycled `id()`s and produced bogus witnesses — the memo must hold its keyed nodes alive).
- **Interpretation:** the "design modulo the floor's equational theory + within-task variation" principles are now enforced, not advised — a ladder with an al3/al6/al14-shaped defect cannot reach the certificate again. The remaining collision residue (shallow train-support agreement without a constant subterm or known equation) is the rung probe's job (AL-PLAN Phase 0).
- **Next:** rung probe (wake/skip/sleep + forecast; collision's home) → forecaster on `estimate_cost.py` + backtest vs the 245 recorded runs → wake-scheduling param + staged `run_ladder` (AL-PLAN-2026-07-22.md Phase 0).

## 2026-07-22 — The rung probe: the certificate's verdicts, rung-locally, in 100 seconds

- **Commit:** `33dcf77`. Notebook: [experiments/2026-07-22-rung-probe/](experiments/2026-07-22-rung-probe/). Machinery: `ladders/probe.py`, `arc-lab probe-ladder`.
- **Question:** ladder design's only empirical feedback was `run_ladder` — a full climb plus the oracle chain, paid after the testbed and spec were written (which is how 2026-07-20 produced 20 builds and 8 late rejections). Can the certificate's questions be asked per RUNG, at design time, cheaply — and does the answer agree with the recorded runs?
- **Ran:** built the probe (wake / skip / sleep / forecast per rung under `L_{i-1}`, in process, recording nothing), then swept every rung of all 20 ladders.
- **Result:**
  - **The probe-clean set is exactly the certificate's admitted-with-full-recovery set** — al1, al2, al15-al20 (8/20, each <= 1s) — and the other 12 fail with their recorded verdicts restated, programs printed: al7/al9/al11's `stack2(stack2(input))` skip paths, al3's `quad(quad(input))`, al13's `overlay(...)` r1 skip path, al8 collapsing at depth 2, al10's designed reachable top.
  - **The task-collision check (§6.4) is now implemented** — it needs a search, so it could never live in `lint()`; the wake probe compares what search RETAINED against the intended program off the train support.
  - **It needed one non-obvious ingredient.** Comparing on the ladder's own grids found nothing on al14's known collision, because **al14's seed construction confounds cells (0,2) and (2,1) in all 16 of its grids** — so the collision agrees with the intent everywhere in the corpus, heldout included (same generator). This corrects the 2026-07-21 characterisation ("holds only because those cells share a colour in both examples"): the confound is systemic, and no extra tasks from that generator could expose it. Adding deterministic position-separating probe grids (three layouts per corpus shape, no RNG) separates al14 r1 cleanly: `-00` **collapsed** (same function, depth 2 not 3), `-01` **collision** (reads a different cell) — the two failures the notebook found by hand, now told apart automatically, with sleep minting `abs0` at **arity 5** against an intended 3.
  - **al12 is the sharpest validation:** the certificate deliberately separates "is this a ladder?" from "did the loop climb it?", and the probe reproduces that split unprompted — clean wake, clean skip, missed mint (one demo, so `AntiunifyPairs` has nothing to pair).
  - **Metrics:** 20 ladders, 46 rungs, ~100s total (vs 245 recorded runs for the 2026-07-20 certificate); 35 skip paths, 14 collapses, 9 collisions; 8 clean. New fast tests run in ~5s; the al14 collision separation needs `--considered-limit 400000` and is marked `slow`.
- **Interpretation:** the design loop's unit of feedback is now a rung cell, roughly two orders of magnitude cheaper than a ladder, and it agrees with the expensive instrument everywhere it was checked. Standing asymmetry: **the probe convicts, only the certificate acquits** — a clean rung-local probe does not imply admission (the climb pays each jump under a library inflated by earlier mints), but a dirty probe is proof of non-admission.
- **Next:** the calibrated forecaster on `estimate_cost.py` (per-type censuses, new-layer factor, measured survival rates), backtested against the 245 recorded runs — the probe's forecast column currently uses the loose worst-case ceiling (al14: 6.4e13).

## 2026-07-22 — The calibrated cost forecaster: 21.6x geomean error down to 1.07x

- **Commit:** `93f0676`. Notebook: [experiments/2026-07-22-rung-probe/](experiments/2026-07-22-rung-probe/) (second half). Machinery: `execution/forecast_cost.py`.
- **Question:** the probe's cost column was `estimate_cost`'s worst-case ceiling — honest but useless for planning (al14: 6.4e13). Can a _prediction_ be built, and how wrong is it?
- **Ran:** built `forecast_cost.py` (typed per-type census; the engine's new-layer restriction; survival measured off a real funnel), then backtested three predictors against the engine's own per-round funnels on every ladder rung cell — 336 completed rounds across 54 cells, censored rounds excluded since a cut-short round is not a measurement.
- **Result:**
  - **Metrics (predicted/actual):** ceiling geomean **21.63x**, 60% within 2x, worst 1.2e9x over · cold forecast (prior only) geomean **1.07x**, **97%** within 2x · warm (calibrated on the first 2 rounds) geomean 1.17x, 92%. Measured survival across the batch: median **0.445**, mean 0.488 (n=265).
  - **The typed census is the load-bearing correction**: a slot draws only from its own type's bucket, so al14's `set_cell(Grid,Int,Int,Color)` costs `10,615 x 17 x 17 x 14` = 43M rather than `pool**4` = 1.6e13. It is also what makes per-factor attribution possible — the forecaster names the dominant product term per round.
  - **The backtest caught a real bug before it shipped:** truncating survivors with `int(count * rate)` could floor a small round to zero, freezing the census, which made the NEXT round's new-layer term exactly zero — the forecast collapsed to 0 for every deeper round (seen on al3/al11). Fixed by guaranteeing >= 1 survivor per composing primitive; `cold` went 88% -> 97% within 2x.
  - **A finding, not a bug: `warm` is worse than `cold` at depth.** Carrying an early survival rate forward over-predicts, because dedup RISES with pool size (early rounds survive at ~1.0, late rounds far lower). So "probe two rounds and project" is optimistic; the probe now carries the _deepest_ observed rate forward and states the residual bias (over-stating cost — the safe direction for a budget).
  - **First direct evidence for the depth-ceiling correction:** the probe prices al17 r1 at **~160k considered for depth 3** — affordable. The depth-2/3 jump convention was a design habit, not a measured limit.
- **Interpretation:** cost is now a number computed before a run rather than a property discovered by getting burned, and it is accurate enough (97% within 2x) to set guards and choose which cells to run. Standing caveats, all flagged in-module: `max_pool` is modelled as a proportional shrink where the engine cuts cheapest-first; polymorphic slots draw from the whole pool; lambda synthesis is unmodelled (forecast becomes a lower bound).
- **Next:** the Phase 1 tractability-frontier sweep — `cost_L(d)` over (floor x depth x arity x pool) on representative floors, with the forecaster choosing which cells are worth actually running.

## 2026-07-22 — The tractability frontier: depth is not the cost driver, `(depth x max_pool)` is

- **Commit:** `dff6273`. Notebook: [experiments/2026-07-22-tractability-frontier/](experiments/2026-07-22-tractability-frontier/).
- **Question:** every ladder in the batch pins jump depth at 2-4, on a convention that was never measured (the `considered_limit=50000` guard that produced the "intractable" verdicts arrived at the END of the batch, and al14's diagnosis showed it was 3 orders too small). What does enumerating to depth `d` actually cost?
- **Ran:** `cost_L(d)` on four representative floors (param-free geometric; layout+INT/COLOR params; two-COLOR-param; the al14 cell floor) at `depth_limit` 2-5, guard 5M `immediate`. Then re-ran the first three at `max_pool` 400 vs 20,000 after the first sweep turned out to be measuring the wrong thing.
- **Result:**
  - **The first sweep measured pool starvation, not depth.** On layout+params and colour, rounds past depth 3-4 composed **zero** candidates: once `max_pool` binds and nothing new survives dedup + eviction, the new-layer restriction makes every deeper round empty. The searches were not exploring, they were idling — exactly the design doc's §5.3 caveat, walked into.
  - **With the pool freed, the real cost appears.** layout+params at depth 3: **170,627 at pool 400 vs 3,712,560 at pool 20,000** — a 22x swing from the pool knob alone, at fixed depth. geometric depth 5: 492k (7.3s) at pool 400 vs **>5M censored (79s)** at pool 20,000.
  - **So the depth-2 convention was never the binding constraint — but relaxing it alone buys nothing.** Depth 4-5 costs seconds at the batch's pool settings, and explores almost nothing extra. The useful knob is the **pair**; "deep jumps" only mean something at a pool that can hold the frontier.
  - **The cell floor is the genuine outlier:** depth 3 alone blew the 5M guard (269s, censored) at its own small pool — arity 4 x enumerated INT/COLOR constants, corroborating the batch's cross-ladder reading that arity and parameters dominate depth.
  - **Forecaster validation extended, with a limit found:** exact pre-saturation (geometric d=4 predicted **320,801** vs actual **320,801**), 0.92-0.94x on layout+params — but **0.15x at saturation** on the colour floor and 4.36x on the cell floor at d=2. The failure mode is the approximation its docstring already flags (`max_pool` as a proportional shrink vs the engine's cheapest-first cut). Trust it in the growth regime, not at/past saturation; the backtest's 97%-within-2x was measured in the growth regime.
- **Interpretation:** the batch's depth folklore was wrong in both directions at once — depth was never as expensive as feared, and raising it alone was never going to help. Budgets should be stated and reasoned about as `(depth_limit, max_pool)` pairs. A round that composes zero is a _pool-starvation_ signal the probe should surface, since it means the depth setting is decorative.
- **Next:** micro-probe batteries (if-tax, constant-domain width, param count, arity trade) — single-factor attribution, now that the (depth x pool) confound is understood and can be controlled.

## 2026-07-22 — Micro-probe batteries: arity is not the driver, the type-population product is

- **Commit:** `3a2824f` (fixes: `b1c9b91`). Notebook: [experiments/2026-07-22-micro-probes/](experiments/2026-07-22-micro-probes/). Instruments: `ladders/probe.py::Saturation`, `execution/forecast_cost.py::TaskForecast.saturated_at`.
- **Question:** the batch ranked cost factors by comparing ladders that differed in several ways at once. With the `(depth x pool)` confound now controlled, what does each factor cost _on its own_ — arity, constants, conditionals, HOFs?
- **Ran:** first the frontier sweep's two open follow-ups (saturation surfaced in both probe and forecaster, so a starved cell can never be mistaken for a cheap one again), then six batteries: one task, one budget, one factor moved per cell, on a floor deliberately **unsolvable** so every cell pays the full depth (`cost_L(d)`, not cost-to-solution). No cell saturated.
- **Result:**
  - **The product law is exact, not approximate.** Predicted round-1 count == actual, **24/24 cells**. A rung's cost is computable from its floor's signature before anything is built. _[Amended 2026-07-23: the second sentence overstates — round-1 (and any round's count GIVEN its census) is exact arithmetic; whole-cell cost also needs the survival/dedup term, which is semantic and must be measured. The 2026-07-23 validation put one-round-ahead projection at 0.04x–1.24x across floors. The law supports attribution and guards, not cost prediction.]_
  - **Arity is not the cost driver.** Arity 4 (`set_cell`, COLOR slot) cost **632,944** at depth 3 against arity 5 (`move_cell`, all-INT slots) at **591,302** — one fewer slot, more cost. Pinning arity at 3 and moving only slot types: `translate(int,int)` **1,012** · `pad(int,color)` **41,596** · `map_color(color,color)` **408,612** — identical arity, **404x** apart, exactly the `(10x10)/(4x4)` ratio compounded by depth. What dominates is `PROD(type populations)`; arity is only the number of factors in it.
  - **The constant policy is a 19-46x lever, and the cheap option is the collapse-safe one.** `harvest-from-instance` 1.5x vs `finite-enumerate` 46x on the same floor. The same knob manufactures collapse (`constant-subterm`'s severity rule is literally "does the beating literal exist in this ladder's own search?" — al14, al4/5/6 are all enumerate findings; al8 mints none and sits at warn).
  - **Two arity-2 primitives beat one arity-3, for the same expressible set:** 379 vs 1,012 at depth 3 (**0.37x**). Decompose parameters across rungs, not into slots — which also mints a lower-arity abstraction.
  - **If-tax: 2.0-7.1x** — real but modest, quadratic in the pool rather than a product term.
  - **HOF tax is not one number.** A HOF whose holes can't be filled costs **exactly nothing** (`@none` byte-identical to the control — it is skipped, not enumerated-and-failed, correcting my own framing). `point-free` ~1.3x flat. `lambda-synthesis` spans **1.0x (`map`) to 250x (`fold`)** at depth 3.
  - **`map @ lambda-synthesis` is silently a no-op under every preset.** `map`'s hole return type is a free type variable and `unpinned_type_var_mode='reject'` skips exactly that shape; relaxing it moves `map` 140 -> 625 at depth 3 while `filter`/`fold` (pinned hole returns) don't move at all.
  - **The batteries found two forecaster bugs** — overlapping constant sources double-counted (28 vs 19); BOOL-typed `if` branches omitted (3 vs 7). Both fixed, both now exact. **The 336-round backtest is byte-identical before and after**: neither floor exists anywhere in the batch.
- **Interpretation:** correction #4 ("batches validate, micro-probes discover") earned its keep in the most literal way available — the two defects were reachable only by building floors on purpose, and no amount of ladder-batch running would have surfaced them. The practical output is three design rules: reason in `PROD(type populations)` per slot (never arity, never depth); prefer harvesting to enumeration for parameterised rungs; split parameters across rungs rather than widening a slot.
- **Next:** estimator validation (Phase 1 item 8) is the last open Phase 1 item — a calibration ladder at `d_raw - depth_limit ~ 3-4` with raw actually run; al1's 78x-530x headline is unsupported until it exists. Cheap addition on the way: a probe warning when a floor's HOF holes cannot be synthesised under the configured mode.

## 2026-07-23 — RQ1's raw-cost estimator: the bracket holds in 2 of 10 cells

- **Commit:** `72a6954`. Notebook: [experiments/2026-07-23-estimator-validation/](experiments/2026-07-23-estimator-validation/).
- **Question:** every ladder's RQ1 headline rests on `report.py::_estimate_raw_cost`, which extrapolates raw cost from the rounds a stopped search completed and claims "the truth sits between" its two fits. Never checked. Does it?
- **Ran:** the planned calibration ladder first — and **al2 cannot do the job**: its floor `{flip_h, transpose}` generates the 8-element D4 group, so its composed counts are FLAT (1,2,4,4,4,2,0) and the space is exhausted by depth 5. No growth regime, nothing for a growth fit to be right about. Replaced with a sweep: 4 floors x every depth gap, estimate-from-observed vs **measure the raw run outright**, pool freed to 200,000 with per-cell saturation reporting, guard 40M (censored cells excluded — not ground truth).
- **Result:**
  - **Metrics:** measured inside the bracket **2/10 cells**. Curve fit predicted/actual spans **0.004x – 3.71x** (~900x spread). `forecast_cost` on the same cells: **0.04x – 1.24x**, median **1.00x** (~30x spread).
  - **"The truth sits between" is false, and fails in both directions.** Where growth _accelerates_ (binary primitives taking products of a growing pool) **both** fits under-project — the geometric floor at a 2-round gap read 3,616–11,821 against a measured **973,013** (82x–269x under). Where the reachable space is a finite group both fits over-project (up to 3.71x). The docstring's premise (low under-projects, high over-projects) only holds near saturation.
  - **Error compounds per extrapolated round, but gap size does not bound it:** geometric 1-round gap 0.19–0.34x vs 2-round gap 0.004–0.012x; yet at a 1-round gap the error still spans 0.015x (layout+params) to 1.4x (d4-group).
  - **The structural forecaster is tighter but is NOT a drop-in replacement:** it fixes the geometric (0.33–1.24x) and d4 (1.00–1.12x) cells, and is **25x under on al1's own floor** — the exact ladder whose headline is cited. Neither method earns the word "measurement".
  - **Consequence:** an RQ1 ratio carries ~3 orders of magnitude of multiplicative uncertainty in an **unknown direction**, so even "the true saving is at least N" is unavailable. al1's 78x–530x happened to bracket correctly on al1's floor; the same method is wrong by 269x elsewhere.
- **Interpretation:** this is a negative result that condemns one method without blessing the other, so nothing was swapped — the code change is confined to making the report tell the truth about itself (docstring warning, measured caveats shipped in every report, a test pinning them). The deeper read is that RQ1 as defined asks for a number that is not obtainable: it needs a cost that is intractable by construction. **Marginal rung value** — cost of layer _i_ under `L_{i-1}` vs under `L_i`, measured on both sides, already computed by the report — is the candidate replacement, but redefining RQ1 changes what the AL program claims and is a research decision.
- **Next:** take the RQ1 decision (report-with-error-band / redefine on marginal rung value / restrict to 1-round gaps), then re-derive the batch's headline numbers under it. Secondary: the forecaster's two 0.04x cells are a fixable modelling gap (modelled census growth lags the engine's real pool growth), not an inherent limit.

## 2026-07-22 — Higher-order depth is computable; example propagation is not sound under a wrapper

- **Commit:** _pending_ (working tree). Notebook: [experiments/2026-07-22-ho-depth-and-propagation/](experiments/2026-07-22-ho-depth-and-propagation/).
- **Question:** designing `.ladder` syntax for `Lam`/`Var`/`AppFn` raised a prerequisite — is compositional depth meaningful at all once a program contains a `Lam`, and can the generation search finds it be predicted? If not, a higher-order ladder could be written but never certified, since the whole tractability sandwich is stated in depth.
- **Ran:** a 10-case matrix (`artifacts/depth_matrix.py`) sweeping non-HO depths 0-3 and lambda body depth 0-3 against a fixed outer depth, plus a same-effective-depth pair differing only in whether the HO call is at the root. Each case builds a task from the target program, then sweeps `depth_limit` for the smallest one at which the engine returns it.
- **Result:**
  - **Two laws, exact in every measured row.** Generation found = `compositional_depth(p)` (the existing `lam_as_leaf=True`). Minimum `depth_limit` = **effective depth** = `max over frames of (frame depth + descent)`, where each _function-hole fill_ opens a frame one `budget.descend()` down. Without lambdas there are no descents and the two collapse to today's single number.
  - **Metrics:**
    - body depth 0/1/2/3 at fixed outer depth 2 → min `depth_limit` 2/2/**3**/**4**, generation **2 throughout**.
    - curried 2-binder lambdas cost **1** descent, not 2 (`_synthesize_for_hole` descends once, then wraps binders).
    - **depth is not a cost proxy:** B1 and B3 share depth 2 and generation 2, and cost **44 vs 4,000,000+** considered.
  - **A wrapped higher-order call is unreachable at any depth.** `flip_h(build_grid(…))` (effective depth 3) is not found at `depth_limit` 3, 4 or 5 — `considered` is identical (118) at all three, i.e. the subterm is never built. Dropping `build_grid`'s `body_target` to `None` (the §8 "complete baseline") finds it at limit 3, generation 3, 862 considered.
- **Interpretation:** the depth model was never the blocker — effective depth is a cheap static reachability predictor and reproduces every row. The blockers are elsewhere. (1) On a HO floor `d_raw` stops standing in for cost, so such a ladder can be certified for _reachability_ but not read for _amortization_. (2) The unreachability is an engine bug: ARCHITECTURE.md §8 frames propagation as an optimization over a _complete_ baseline (so it must be a sound restriction), and §7 explicitly intends propagation to stop under a wrapper — but guards it with a **type** check (`List[b]` cannot unify with `GRID`). That only stops it when the wrapper changes the type; `flip_h` is `GRID -> GRID`, so a wrong target flows down and the correct body is discarded by `entry.sig != body_target`. Latent today (no committed ladder uses a HO floor) but it affects any solution wrapping `build_grid`/`map`/`filter`/`fold`/`sort_by` in a same-type transform.
- **Next:** decide among the four propagation options (always-baseline / union / document as an inductive bias / build the inverse-semantics propagation §7 already names as unbuilt — `inverse_semantics_propagation: bool = False` marks the gap, but was created for the type-mismatch case and does not route the same-type case). No engine behavior changed. Secondary: implement `effective_depth` in `analysis/depth.py` and use it in the sandwich checks, which would turn the `no-lambda-in-templates` advisory into a real check.

## 2026-07-23 — First measured RQ1: al1's amortization is 3.59x, not the estimated 78x-530x

- **Commit:** `db7c867` (the raw-arm machinery; the number is its first output).
- **Question:** with the estimator retired (bracket held 2/10), what does decision 1's raw arm actually say about al1 — the one ladder whose estimated headline (78x-530x) was ever quoted?
- **Ran:** al1's raw arm per the K-rule: top task under the Floor at depth 4 (`min_depth_limit` of the unfolded top), pool 200,000, guard = 10 x the chain-measured laddered marginal (537,110 per task), `solution_limit=1 immediate`.
- **Result:**
  - **The arm SOLVED** — raw is not intractable for al1 at a freed pool: 192,865 considered to first solution, 16s.
  - **Metrics:** laddered marginal 53,711 · raw-to-first 192,865 · **measured amortization ratio 3.59x** (conservative: numerator cost-to-first, denominator cost-paid-full — the true paid-full ratio is higher).
  - The retired estimate had raw at 4.17M-28.4M: **22x-147x above the measured cost**, on the very ladder it was headline-quoted for. Not even the bracket's low end was close.
- **Interpretation:** al1's ladder does pay for itself, measured — but by ~4x, not by two orders of magnitude. The honest sentence for the register: "al1: RQ1 = 3.6x measured (conservative)". This also retro-validates decision 1's design: the same arm that produced this number would have produced a proven ">= 10x" bound had raw been genuinely intractable, and it cost 16 seconds. al2's arm, run the same day, measured 0.48x — the trivial-regime instrument honestly reads "not worth a ladder", which the estimator (raw "estimated" at 31) could also never say.
- **Next:** the Phase 2 clean-set re-run gives every admitted ladder its measured-or-bounded RQ1 under the staged pipeline; quote no ratio until then.

## 2026-07-23 — Clean-set re-run: al15-al20 prove >= 10x amortization; al1, the old headline, is the weakest

- **Commit:** `83adf2f`. Notebook: [experiments/2026-07-23-clean-set-rerun/](experiments/2026-07-23-clean-set-rerun/).
- **Question:** the 2026-07-20 batch's numbers predate every instrument and the RQ1 decision. What do the 8 admitted ladders actually measure under the staged pipeline + raw arms (K=10)?
- **Ran:** gates first — all 8 lint clean and probe clean after the `.ladder` format upgrades and depth rework (a real regression check). Then `run-ladder --artifacts` per ladder: chain -> certificate -> climb -> raw arm, artifacts written (al15-al20's first folders; al1/al2's stale pre-decision-1 artifacts regenerated).
- **Result:**
  - **Metrics:** all 8 re-admitted · **16/16 rungs recovered** · loop overhead 2.2x-4.0x · no saturated arm (every bound sound).
  - **al15-al20: RQ1 >= 10x, PROVEN** — each raw arm spent 10x its laddered marginal (18.6k-478k considered) at a freed pool without solving. The rebuild set's amortization is no longer an estimate of any kind.
  - **al1: 3.59x measured** — the ladder whose estimated 78x-530x anchored the program's story is the WEAKEST admitted ladder; the estimate had the ranking upside down.
  - **al2: 0.48x measured** — the trivial-regime instrument's ladder costs ~2x raw, stated plainly for the first time.
  - **Off-chain necessity separates the set as designed:** al15/al18/al19 tops need the whole chain; the telescoping tops (al1/al2/al16/al17/al20) do not — al19-vs-al20 reads exactly as the 2026-07-21 resolution said.
- **Interpretation:** decision 1's mechanism works at batch scale and produces claims in both directions: proven bounds where raw is genuinely out of reach, honest small ratios where it is not. The set's story inverts the estimator era: the _rebuilds_ — designed against the collapse findings — are the strong ladders, and the shape-control telescope is marginal. This table supersedes every previously quoted number for these 8 ladders.
- **Next:** the second half of Phase 2 item 4 — grow the set (new ladders must lint clean, probe clean, and produce usable cost data); sleep-side attribution (item 5); `w` calibration so RQ1 denominators can include learning cost.

## 2026-07-23 — Loop overhead decomposed: the flavor gap is zero; skip-solved buys 1.8-2.5x and mints junk

- **Commit:** `6e069af`. Notebook: [experiments/2026-07-23-loop-overhead/](experiments/2026-07-23-loop-overhead/).
- **Question:** what separates al1's 3.59x marginal ratio from its 0.96x achieved one — and how much does the new `wake_schedule` machinery recover?
- **Ran:** (1) learned-vs-oracle flavor gap read off the fresh artifacts: wake iteration i under `L^learned_i` vs oracle column `L_i`, all 8 ladders; (2) al1 + al17 climbs re-run under `skip-solved` (fresh run_ids; arm-labeled), extra mints inspected.
- **Result:**
  - **Flavor gap = 1.00x on all 24 wake iterations of all 8 ladders — byte-identical considered counts.** On the clean set sleep's mints are cost-equivalent to the oracle rungs; the gap is a property of imperfect climbs, of which the admitted set has none. Phase 2 item 6 answered for the clean set; measuring a nonzero gap needs a deliberately imperfect cell (folded into item 5).
  - **skip-solved: e2e 2.5x cheaper on al1 (200,280 -> 80,852), 1.8x on al17.** Wake sets shrink 5->3->1->0 / 7->5->3->1->0 as designed. **al1 breaks even again under it: 2.39x achieved** (vs 0.96x full) — the difference between al1 paying and not paying is entirely re-search.
  - **But the arm label earns its keep immediately:** both climbs mint EXTRAS from stale carried solutions — al1 a frozen specialization (`map_color(#0, 3, 4)`), al17 a reparameterized duplicate of frame1. Recovery still 100%, costs already include the junk's vocab tax, but the downstream library is dirtier — a transfer liability the e2e number does not price.
- **Interpretation:** the loop-overhead story is now fully attributed: re-search (removable by scheduling, at a library-quality price), full-budget failures on unreachable tasks, and vocabulary tax. And the first witnessed sleep-side mechanism — specialization-from-stale-input — gives item 5's micro-probes a concrete target.
- **Next:** sleep-side single-factor batteries (item 5): demonstration-mix vs mint arity, proposer choice, governance refusals, stale-input specialization; `w` calibration alongside.

## 2026-07-23 — Sleep-side micro-probes: mint arity is determined by the demo value-patterns

- **Commit:** `0bce5ca`. Notebook: [experiments/2026-07-23-sleep-probes/](experiments/2026-07-23-sleep-probes/).
- **Question:** the learn half had no single-factor attribution. What actually determines what sleep mints?
- **Ran:** six batteries, no search: demo count, param mix, governance break-even, stale input, proposer head-to-head (`GreedyMDLLearnEngine.run` on synthetic solved sets), and the `w` calibration on the clean set's recorded runs.
- **Result:**
  - **Pairing floor = exactly 2 demos** (al12's mechanism isolated); more bought nothing.
  - **Mint arity = number of distinct value-patterns across demos**, mechanically: varied → intended arity; frozen → literals baked (al13, isolated); **equal-valued params fuse into one shared param** — a new, un-linted failure mode (param covariance).
  - **The skip-solved junk mechanism is frozen-param specialization, not staleness**: plain stale spellings mint nothing; repeated frozen-param solutions mint exactly al1's witnessed junk. Full mode erases the pattern by re-expressing each wake; carrying preserves it.
  - **Proposers surface different kinds**: AntiunifyPairs mints NOTHING on fragment-shared sets (structurally cannot); FrequentSubtree finds fragments but only the arity-1 inner motif on whole-similar sets. The register's real-ARC claim, isolated.
  - **`w` measured: ~3 considered-equivalents per sleep unit; a whole clean-set sleep ≈ 6-8 equivalents vs 2k-50k wakes** — the wake-side-only RQ1 caveat is a rounding error at this scale.
  - Governance refused nothing in the probed range (2-3 op motifs x 2-3 uses) — the break-even frontier sits below it.
- **Interpretation:** the demonstration plan IS the mint's specification. With S-B's law, ladder generation can guarantee intended arities by construction, and two of the clean-set table's three caveats (flavor gap, sleep cost) are now measured no-ops — marginal-vs-end-to-end is the one that matters.
- **Next:** param-covariance lint (small); grow the ladder set (needs interestingness steer); granularity family.

## 2026-07-23 — Wake-schedule comparison: the oracle curriculum IS the marginal accounting (loop overhead 3-4x)

- **Commit:** `68db634`. Notebook: [experiments/2026-07-23-schedule-comparison/](experiments/2026-07-23-schedule-comparison/).
- **Question:** complete the full / skip-solved / curriculum table across the admitted set (TODO item 4, multi-ladder) — how much would perfect scheduling save, and does the cheap honest mode approach it?
- **Ran:** 7 clean ladders x 3 schedules; curriculum built from each spec's own rungs (one group per level, top last); per cell e2e considered + recovery + mints.
- **Result:**
  - **Curriculum e2e == the ladder's MARGINAL laddered cost, EXACTLY, on all 7** (verified against the clean-set report.json marginals). Definitional and now demonstrated: marginal = each rung's demos searched once under L\_{i-1}, which is what the curriculum schedule does. So **the loop-overhead factor is exactly full/curriculum: measured 3.0x-4.0x**, growing with height (al17 the tallest at 4.04x).
  - **skip-solved (honest, no injected knowledge) recovers ~40-60% of that gap**: 1.69x-2.48x vs curriculum's 3.0x-4.0x. The rest needs knowing which rung a task belongs to.
  - **Recovery 100% in every cell.** Junk mints are schedule-invariant among carrying schedules and ladder-dependent: al1 (+1) and al17 (+2) mint the SAME extras under both skip-solved and curriculum (frozen-param specializations, sleep-probes S-D confirmed at batch scale); the other 5 mint clean under either. full mints clean everywhere (re-expresses each wake).
- **Interpretation:** the marginal-vs-end-to-end gap — the one accounting distinction that survived the sleep-probes cleanup — now has an achievable interpretation: marginal is what an oracle scheduler delivers, curriculum delivers it, and skip-solved captures about half of it honestly. For 5/7 ladders skip-solved is a strictly-better screening mode than full (cheaper, full recovery, clean library); for the 2 with frozen-param demos it is cheaper + recovers but dirties the library — the arm label's exact use case.
- **Next:** refold-carried-solutions (TODO item 9) — re-express carried solutions through the current library before sleep to keep the 1.7-4x saving without the pollution; testable against these al1/al17 cells.

## 2026-07-23 — Primitive-promotion loop: cfb2ce5a's floor made real; `retain_colors` demolished, `largest_filled_square` proved irreducible

- **Commit:** `bd7b424` (lint-ladder draft-proxy fix: `9c25b5f`)
- **Question:** can a floor-lowering claim ("this powerful primitive is really just these simpler ones") be _checked_ rather than eyeballed — and does cfb2ce5a's floor actually lower?
- **Ran:** implemented the cohort's 6 assumed primitives as reference impls (`substrate/primitives/tiles.py`), then drove the loop: reference `Primitive` + decomposition as a `Program` template -> `make_abstraction` -> `observationally_equivalent_functions`. No new machinery; it is assembly of the equivalence core built in `701d914`.
- **Result:**
  - **The references are validated externally, not against their own docstrings:** v1 and v3 each solve all 3 cfb2ce5a train examples **and the held-out test**, first try, against `arc1-eval` ground truth.
  - **`retain_colors` IS the mask algebra**, at depth 4: `paint_through_mask(g, mask_complement(mask_union(mask_by_color(g,c1), mask_by_color(g,c2))), 0)`.
    - **Metrics:** 2,400 (grid x color x color) cases, **0 splits** (the by-hand check was 120). The mutant that drops `mask_complement` is refuted, so the check has teeth.
    - It therefore earns no place on any floor — a genuine floor reduction, decided automatically.
  - **`largest_filled_square` is NOT decomposable over the substrate at all** — and for a precise, structural reason: selecting "the largest all-nonzero square" needs `map`/`filter`/`sort_by`/`fold` to range over candidate regions, and **no primitive anywhere produces a `list[mask]` or `list[grid]`** (every list-valued primitive yields `list[color]` or a list of pairs). Pinned as a tripwire test.
  - **Side finding (tooling defect, fixed):** `lint-ladder` used "has assumed primitives" as its proxy for "is a draft". Implementing the floor falsified that proxy and the command crashed with an unhandled `ValueError`. The two incompletenesses — _assumed floor_ and _no train/heldout corpus_ — are independent; both now route to the structural tier and report INCOMPLETE.
- **Interpretation:** the loop works and pays immediately, but it **partitions the floor rather than lowering it uniformly**. `retain_colors` was compositional slack and vanished on contact; the perception primitive is irreducible _by construction_, not by accident. That is the cfb2ce5a headline ("the difficulty is perceptual, not compositional") converted from an assertion into a structural fact with a named cause. Lowering that floor is therefore not a rewrite but a deliberate substrate extension — a region-enumeration tier.
- **Next:** decide on the region-enumeration tier (a `list[mask]`/`list[grid]` producer + size/predicate selection), which is what any taller cfb2ce5a ladder needs; then a v5 with the lowered floor. `retain_colors`'s decomposition is a candidate law for `analysis/equations.py::BASE_EQUATIONS`.

## 2026-07-24 — The addressing tier: Coord/Offset/Rect + region enumeration; largest_filled_square decomposed

- **Commit:** (pending -- branch `feat/ladder-editor-tooling`). Plan: `docs/abstraction_ladders/` (addressing-tier plan, this session).
- **Question:** the promotion loop found `largest_filled_square` irreducible _by construction_ (nothing produced a list of regions to select among). Add the missing capability, then check whether the percept -- and the other bespoke cfb2ce5a atoms -- decompose.
- **Ran:** built three base types (`Coord`/`Offset`/`Rect`, `core/geometry.py`) and ~30 primitives (`primitives/addressing.py` + `primitives/regions.py` + `range`/`nth`): region producers (`filled_squares`, `maximal_filled_squares`, `squares`, `quadrants`, `connected_regions`, `content_coords`), the affine algebra (`coord_add`/`coord_sub`/`offset_scale`/`to_local`...), and the missing elim `paste`. Extended `Const` to carry Coord/Offset literals so displacement-typed rungs keep their depth. Refactored the _displacement_ family (`translate`/`blank`/`from_cells` -> Offset) + added `extent`. Validated decompositions via the existing `observationally_equivalent_functions` loop.
- **Result:**
  - **`largest_filled_square` is decomposed:** `crop_rect(g, head(filled_squares(g, 0)))`, depth 3, 0 splits. The tripwire test that asserted the substrate _could not_ do this fired on the new producer and became the proof it now can -- the whole point of a negative result with a named structural cause.
  - **`relative_tile` is decomposed:** depth 9, 950 cases, 0 splits (source square + per-axis lattice step + `crop_rect`). Depth 9 is the honest measure of how much that atom fused.
  - **`nth_nonzero_color` is NOT a clean composition** -- the loop refuses it with witness `(2x2 grid, i=3)`: its core is `read` at the i-th `content_coords`, but the reference returns color 0 when the index is absent (totalizes) while `nth` raises. Retiring it needs the guard made explicit, which the loop told us exactly.
  - **`filled(g, bg)` is parameterized, not duplicated:** `filled_squares(g, 0)` (depth 1) and `filled_squares(g, most_common_color(g))` (depth 2) are the specialized and general readings as a real one-level rung.
  - **A latent search bug, caught + fixed + regression-locked:** `signature.py::_inhabits` fell through to `False` for every unknown type, so **every Coord/Offset/Rect value was silently pruned from every pool** -- the tier would have been dead in search while every `impl`-level unit test passed. Now checked exactly (a Coord is not an Offset).
  - **Three duplicated copies of "which types a constant policy supplies" collapsed to one** (`leaves.py::CONSTANT_SOURCE_TYPES`); they had silently disagreed, making a `translate` bundle read as structurally dead. Same duplicated-knowledge shape as the earlier draft-proxy bug.
  - **Locks unmoved:** `test_locks.py` byte-identical (the four ARC presets contain none of the touched primitives, as predicted). al15-al20 depth sandwiches unchanged (Offset literals did their job); lint 9/20 clean before and after.
- **Interpretation:** "cfb2ce5a's difficulty is perceptual, not compositional" is now a _lever_, not a wall -- the perception laddered into `filled_squares` -> `head` -> `crop_rect`. The diagnosis held: the bespoke atoms were private re-implementations of three missing capabilities (region plurality, region measures, the way back), and supplying those decomposes them. `nth_nonzero_color`'s refusal shows the totalizing-vs-partial seam a naive rewrite would miss.
- **Deferred (blocked, not skipped):** the _cell-indexing_ family (`read`/`set_cell`/`move_cell`/`swap_cells`/`build_grid`) stays int-based. al14's rung uses COMPUTED coordinates (`sub(n, 1)`), so a `coord()` wrapper pushes `move_cell_up` from d_i=3 to 4, past its `depth_limit: 3` -- and al14 has pinned probe tests (the 2026-07-21 collapse/collision finding). A literal cannot rescue a computed coordinate. Making read Coord-typed therefore needs either raising al14's depth_limit (moves its run identity + recorded measurements) or redesigning al14 (moves its pinned probes) -- a research-artifact decision that is the user's call, not a behavior-preserving edit. `build_grid`'s cell body is int-coupled to the (int-based) `read`, so it stays int-based too.
- **Next:** build cfb2ce5a **v5** with the lowered floor (`filled_squares`/`crop_rect`/`head` in place of `largest_filled_square`), the first taller-than-the-perception ladder; promote the validated decompositions to `analysis/equations.py::BASE_EQUATIONS`.

## 2026-07-24 — Totality is a third design axis; the cells cluster splits by flavor rather than moving

- **Commit:** (pending -- same branch). Follow-on to the addressing tier, same day.
- **Question:** `nth_nonzero_color` was the one cfb2ce5a atom the promotion loop REFUSED. Why, and what does the refusal actually say?
- **Ran:** traced the refusal to its witness, then measured three candidate decompositions against the reference.
- **Result:**
  - **The gap was never the indexing -- it was TOTALITY.** The natural reading `nth(content_colors(g, 0), i)` (depth 2) splits at witness `(2x2 grid, i=3)`: the reference returns color 0 for an absent index while `nth` raises, and `0 != BOTTOM`. Identical on every in-range input; visible only at the boundary.
  - **Two ways to close it, and the cheaper won by 2x:** `If(lt(i, length(cc)), nth(cc, i), 0)` is depth 4 **and** needs the branching summoner in the floor -- because (measured) only the `If` **node** short-circuits; the `if` **primitive** evaluates both branches and raises anyway. `nth_or_default(cc, i, 0)` is **depth 2 with no `if` at all**. Validated: 155 cases, 0 splits.
  - **So 4 of cfb2ce5a's 6 reference atoms are now decomposed** (`retain_colors` d4, `largest_filled_square` d3, `relative_tile` d9, `nth_nonzero_color` d2); `write_relative_tile` and `nth_seed_source_color` remain unattempted.
  - **The al14 blocker dissolved by splitting the family, not refactoring it.** The cell operations now ship in two flavors -- int-indexed (`read`, ...) and coord-indexed (`read_color_at_coord`, ...) -- side by side. al14 keeps its int rung at `d_i=3` under `depth_limit: 3`; decompositions get the clean coord form. **All four ship together on purpose**: `swap_cells`/`move_cell` are withheld study targets rederivable from their own floor's accessors, so a half-split family (coord targets over an int floor) would silently break that derivability. Mirrored in `bundle_sheet.py` as `COORD_CELL_*`, coherence-checked.
  - **`content_colors(g, bg)` added as the index-aligned sibling of `content_coords`** -- distinct from both existing color-list producers (`palette` = distinct/ascending/bg-included; `cells` = every cell; this = non-bg, row-major, duplicates kept). The alignment is what makes `nth` over either name the same cell.
  - **`tiles.py` renamed to `cfb2ce5a_reference.py`** (`CFB2CE5A_REFERENCE_PRIMITIVES`): the old name overstated the module's generality and collided conceptually with the unrelated `tile` primitive in `combinators.py`. Naming it for the cohort makes its scope, and its scaffolding status, visible at the import site. Its docstring now carries the per-atom demolition status.
- **Interpretation:** totality/partiality deserves to sit alongside "how high-level" and "how complex the decomposition" as a **third** design axis. It is invisible to in-distribution testing, it materially changes the decomposition (+2 depth and an `if` summoner, or a new primitive), and only exact-equivalence over an edge-inclusive battery surfaces it. The broader pattern both findings share: when a decomposition refuses, the right move is usually to _name the missing capability_ and add it -- region enumeration for `largest_filled_square`, a totalizing index for `nth_nonzero_color` -- rather than to contort the composition.
- **Next:** cfb2ce5a v5 over the lowered floor, then `probe-ladder` it -- the addressing tier is validated for CORRECTNESS but its SEARCH COST is still unmeasured (region producers put many rects in the pool; addressing constant leaves are quadratic in max dimension when `constant_sources` is opted into, though the ladder default is empty and the cfb2ce5a drafts inherit that).

## 2026-07-24 — The last two cfb2ce5a atoms decompose, and expose a SECOND flavor of the totality axis

- **Commit:** (pending -- same branch).
- **Question:** do the two heaviest cfb2ce5a atoms (`write_relative_tile`, `nth_seed_source_color`) decompose over the addressing floor? A full v5 lowering needs them.
- **Ran:** the promotion loop on both, then re-tested the splits on strictly coherent (task-realizable) inputs.
- **Result:**
  - **Both decompose, so all 6 percepts are now lowered.** `nth_seed_source_color` = `If(lt(i, length(content_coords(seeds, 0))), read_color_at_coord(pattern, nth(content_coords(seeds, 0), i)), 0)` (d4); `write_relative_tile` = `paste(base, tile, coord_add(rect_origin(src), offset(mul(down, size), mul(right, size))))` where `src = head(filled_squares(anchor, 0))` (d8).
  - **But neither is EXACTLY equal to its reference -- and the seam is a DIFFERENT partiality than `nth_nonzero_color`'s.** The full exact battery splits; restricting to strictly-aligned inputs (pattern.shape == seeds.shape; tile.shape == source-square size) gives **0 splits over 780 cases**. The references **defensively raise** on incoherent inputs (mismatched shapes, wrong tile size); the decompositions are more permissive. So this is *defensive partiality* (the reference guards its domain and raises) vs `nth_nonzero_color`'s *totalizing fallback* (the reference returns 0 past the end). Same axis, opposite direction.
  - **The distinction that matters: "same function" is not "same function + same defensive contract."** Both decompositions compute the reference's function on every input the TASK can produce (cfb2ce5a inputs are always coherent), so they are task-correct; they simply do not reproduce guards that only fire on inputs the task never generates. Exact-equivalence over an OOD-inclusive battery correctly refuses to call them identical -- which is the right conservative behavior, not a bug in the loop.
- **Interpretation:** the totality axis has (at least) two flavors, and they pull opposite ways: a reference can be *more* defined than the natural decomposition (totalizing fallback -> the decomposition must add an `if`/`nth_or_default`) or *less* defined (defensive guard -> the decomposition would need shape-validation to match exactly). For building a ladder, the useful equivalence is "equal on the reachable input domain," which is weaker than exact-over-OOD; the promotion loop measures the strict version, so a "coherent-inputs-only" verdict is a deliberate, named weakening, not a silent pass. v5's floor can safely use the permissive decompositions because the task domain is coherent.
- **Next:** probe ONE percept rung (the source square, `crop_rect(g, head(filled_squares(g, 0)))`, d3) off the addressing floor before building the full ~23-rung v5 -- `write_relative_tile`'s d8 warns that some percept jumps may be too tall to certify in one leap, which is the height/granularity question on a real task.

## 2026-07-24 — The breadth axis: what a specialization buys, and why the current machinery cannot cash it

- **Commit:** measured at `6432fdc` (4-cell micro-probe, real engine; setup reproduced in [docs/abstraction_ladders/BREADTH-AXIS-2026-07-24.md](docs/abstraction_ladders/BREADTH-AXIS-2026-07-24.md)).
- **Question:** a constant-specialization abstraction adds zero depth (inlining a depth-1 body preserves every consumer's depth — verified). So what, if anything, does it buy — and does minting one actually reduce search cost?
- **Ran:** one task (`flip_v(map_color(g, 1, 2))`), `BottomUpSearchEngine`, `depth_limit=2, max_arity=3, max_pool=400`, four cells: **A** floor `{map_color, flip_v}` + `finite-enumerate`; **B** floor `{mc12, flip_v}` (the specialization), constants off; **A′** = A at `considered_limit=60`; **C** = post-mint reality: `{map_color, flip_v, mc12}` + constants still on.
- **Result:**
  - **Metrics:**
    - A: solved, **4,758 considered** · B: solved, **7 considered** — **680x**, at identical depth 2.
    - A′: **censored, unsolved** at limit 60 — the specialization is *necessary under the joint budget* while being depth-skippable: an empirical **breadth sandwich**, using only `Budget.considered_limit`.
    - C: solved, **4,805 considered (> A)** — ranked solution improves to `flip_v(mc12(input))`, but search cost goes UP by the mint's own vocabulary tax.
  - So: **the mint improves the solution (MDL/ranking), not the search.** Three mechanisms: exhaustive round-based enumeration (no cost-ordering, `solution_limit` off); dedup is post-generation (`considered` already paid); and the chicken-and-egg — a climb has ONE `Config`, and the constants that the binding rung's own wake *needs* keep taxing every later rung.
- **Interpretation:** search cost has two axes — **depth** (rounds of composition; the sandwich, `d_i`, `depth_limit`) and **breadth** (choices per round; census products, `considered`, pool). Constants live entirely on the breadth axis (a literal is a nullary quasi-primitive whose membership is decided by *config*, not the library). A specialization is real learning on the breadth axis — the choice doesn't vanish, it *moves* to a cheap early round and is cached — but the current engine cannot realize the saving. The lint's sandwich is depth-only; the probe already checks the joint budget. "Constants should cost depth 1" was considered and rejected: it prices a multiplicative, context-dependent cost in an additive, uniform currency. Full analysis + approaches (removal / weighting / abstraction-normal-form pruning): [BREADTH-AXIS-2026-07-24.md](docs/abstraction_ladders/BREADTH-AXIS-2026-07-24.md).
- **Consequences for the ladder program (Phase 1 design disciplines):** the specialization-prelude control arm is **struck** (it would measure ~zero under current machinery, and we know it in advance); Phase-1 cohorts keep `constant_sources` off wherever a perceiver route exists, so they stay clean depth experiments. Breadth machinery (static breadth-sandwich lint leg; normal-form pruning; weighting/neural-guided) is Phase 2+.
- **Next:** the set-design and plan docs this fed into: [LADDER-SET-DESIGN-2026-07-24.md](docs/abstraction_ladders/LADDER-SET-DESIGN-2026-07-24.md), [LADDER-SET-PLAN-2026-07-24.md](docs/abstraction_ladders/LADDER-SET-PLAN-2026-07-24.md).

## 2026-07-24 — Phase-0 percept probes: cheap percept is a clean rung, deep percept needs laddering + a scalars-only constant policy

- **Commit:** (pending -- same branch). Executes the queued Phase-0 step of [LADDER-SET-PLAN-2026-07-24.md](docs/abstraction_ladders/LADDER-SET-PLAN-2026-07-24.md) and resolves two of its named risks (addressing-tier search cost unvalidated; quadratic Coord/Offset constant leaves).
- **Question:** does a decomposed cfb2ce5a percept search cheaply as a rung off the addressing floor? The tier is proven CORRECT but its COST was unmeasured; `write_relative_tile`'s d8 decomposition warned some jumps may be too tall to certify in one leap.
- **Ran:** `probe_rung` on the source-square percept (d3) as a single rung; then a bounded search-cost sweep on the write-tile percept (`place_copy_right`, d7) as one jump vs. laddered via intermediate rungs, under `finite-enumerate` vs a new `finite-enumerate-scalars` policy.
- **Result:**
  - **Cheap percept = a clean rung.** Source square `crop_rect(g, head(filled_squares(g, 0)))` (d3): wake AS-INTENDED on both demos at depth 3/3, **40 and 33 considered**, sleep recovered at arity 1. The O(n^3) `filled_squares` did NOT blow up search (the big list is ONE pooled value; only `head` puts rects in the pool). Draft: `drafts/cfb2ce5a-5-lowered-source-tile.ladder`.
  - **Deep percept = intractable in one jump.** `place_copy_right` (d7) never solves; considered explodes between depth 4 (112k) and depth 5 (>300k), never reaching depth 7. **A d7 percept cannot be one rung** -- the d8 warning, confirmed and priced.
  - **Laddering it works, but only with the right constant policy.** With `source_rect`/`source_tile`/`tile_size` as learned floor abstractions, the final placement step is d4. Under `finite-enumerate` (position-varied demos) it still **hits 500k considered, unsolved** -- the quadratic addressing-constant battery (measured **128 of 146 round-0 leaves** on a 7x7 grid) drowns it. Under **`finite-enumerate-scalars`** it solves in **24,927 considered (0.5s), retaining the EXACTLY intended program** -- a **>20x reduction**, intractable -> tractable.
  - **New tool: `finite-enumerate-scalars`** (`search/leaves.py`) -- `finite-enumerate` restricted to INT/COLOR/BOOL, dropping the coord/offset square. An addressing value still wanted as a constant is built one level deeper from cheap INT leaves (`offset(0, 1)`), linear not quadratic. Read-side consumers (coherence, forecast) follow via the shared `CONSTANT_SOURCE_TYPES`. Locks unmoved.
  - **Collision reminder (al14, recurring):** the first laddered probe "solved" with a baked constant coord `(0,1)` because all demos shared source-origin (1,1); position-varying the source square across demos is what forces retention of `rect_origin(source_rect(g))`.
- **Interpretation:** this is the addressing tier's first cost validation, and it lands on the **breadth axis** (2026-07-24 breadth entry): the perception primitives v1 shipped were hiding not just depth but a large breadth cost, and the lowered floor pays it unless constants are kept minimal. It refines a Phase-1 design discipline: "`constant_sources` off wherever a perceiver route exists" needs a **third option** between off and full `finite-enumerate` -- the lowered floor has a perceiver route for COORDINATES but not for the small direction literals (`0`,`1`), so it wants scalars-only, not off. v5's spine is now shaped by two measured facts: cheap percepts are d3-ish single rungs, and the write-tile percept needs a ~3-rung sub-climb (`source_rect` -> `{source_tile, tile_size}` -> `place`) under `finite-enumerate-scalars`.
- **Next:** author cfb2ce5a v5 (Phase-1 Cohort B) as an all-d2 spine per the set-plan methodology, cut-sets chosen off these probe results; gate `lint-ladder` -> `probe-ladder` -> `taskgen` -> `run-ladder` + raw arm (the v5-vs-v1 ablation prices what the perception primitives were worth).

## 2026-07-25 — Floor-lowering has a search-cost law: cost ~ (primitives x constants)^depth

- **Commit:** `c6b19bb` (same uncommitted branch; includes the double-jump lint-severity relaxation below). Full write-up + scripts: [experiments/2026-07-25-v5-lowering-search-cost/notebook.md](experiments/2026-07-25-v5-lowering-search-cost/notebook.md).
- **Question:** how far can we lower v1's 6-primitive floor before per-rung search goes intractable, and what actually controls it?
- **Ran:** three v5 ladder iterations (`probe-ladder`, real engine per rung) + a controlled pruning sweep. Iterations: (1) decompose `relative_tile` into coordinate arithmetic; (2) partial lowering (4 of 6 lowered, `relative_tile`/`write_relative_tile` kept built-in); (3) core, no placement, fragment-demoed Color/Mask rungs + StitchProposer. Sweep: re-ran each censored rung's wake search under {no pruning, primitive-pruning (library -> only referenced prims), constant-pruning (`policy_constants` patched to only used values), both}, at the rung's **matched target depth**.
- **Result:**
  - Every iteration hit the SAME wall with a DIFFERENT dominant primitive: coord constructors (`offset(3213)`) -> `write_relative_tile: grid(7999)³` -> `map_color: grid x color²`. Same base of an exponent, different top term.
  - **Metrics (all-lowered core, matched depth; considered / 50k cap):**
    - none / prim-only / const-only: **CENSORED** on every censored rung (prim pruned 20->4 prims; const pruned 21->3 values — neither alone helped).
    - **both** together: **solved** depth-3/4 rungs (first_target_color d4: 4,720; seeded_source_mask d4: 8,387; instantiate_first d3: 13,675) but **still censored** every depth-5 rung.
    - depth sweep on a d4 target: dL3 unsolved, **dL4 solved 4,720**, dL5 censored — depth, not library/constants, dominated at dL5.
- **Interpretation:** **search cost ~ (primitives x constants)^depth.** The base is a *product* — cutting one factor leaves the other multiplying, so you must prune BOTH; depth is the *exponent*, which pruning cannot beat (depth-5 rungs censor even fully pruned). Lowering a primitive swaps a built-in for a depth-*d* composition — it **raises the exponent** — which is the irreducible cost. **Rule: a lowering is tractable iff its rungs stay shallow (<=4 with full pruning, ~2-3 without); no pruning rescues a deep (>=5) decomposition.** Corrects the raw-primitive-count hypothesis (count is one factor of the base, not the exponent, and not sufficient alone). Quantifies the [breadth axis](docs/abstraction_ladders/BREADTH-AXIS-2026-07-24.md); the two pruning knobs = its "removal" cashing approach.
- **Side-findings:** `largest_filled_square` == `relative_tile(g,0,0)` (delete it, no decomposition needed); fragment-demos + StitchProposer correctly learn non-Grid (Color/Mask) rungs (task-less was unnecessary); symmetric source squares cause false pattern collisions (use asymmetric); double-jump skip-freeness checks relaxed error->**warn** (certificate-profile reframe) so mixed-depth ladders lint-clean, al10 control preserved, `make check` green.
- **Next:** deep (>=5) decompositions need breadth machinery past removal — weighting / normal-form pruning / cost-ordered search (BREADTH-AXIS approaches 2-3), Phase 2+. Pruned-vs-unpruned gap is a direct measure of a growing library's breadth tax if a pruned control climb is adopted.

## 2026-07-25 — MVE task screen: 3 real ARC tasks made expressible; the demo-affordability law; two DAG/lint gaps closed

- **Commit:** (pending -- same branch). Executes step 1 of [MVE-PLAN-2026-07-25.md](docs/abstraction_ladders/MVE-PLAN-2026-07-25.md).
- **Question:** which real `arc1-train` tasks can host an MVE ladder over the current substrate, and what actually blocks authoring one?
- **Ran:** a structural screen of all 400 arc1-train tasks (bucketed by input->output relation, then a targeted pass for the four families the substrate expresses WITH depth); a propose-check harness (elaborate a term against the full 117-primitive library, evaluate on every train example AND the held-out test); then authored two ladders and probed them.
- **Result:**
  - **Metrics (verified against ground truth, zero new primitives):**
    - `dae9d2b5` (OR two halves, 3x6->3x3) -- **depth 9**, solves 5 train + both tests.
    - `94f9d214` (NOR two halves, 8x4->4x4) -- **depth 8**, solves 4 train + test.
    - `4347f46a` (hollow every solid rectangle, same-shape <=18x16) -- **depth 8**, solves 3 train + test.
  - Screen families found: two-halves-combined (10 tasks), region-recolor (48), denoise/keep-regions (8), symmetry-repair (2). The 12-task `tiling_d4` bucket is the **al3/al7 self-similar-doubling family** -- excluded by construction, not screened out by accident.
  - **The demo-affordability law (new, general).** `jump-affordable` measures the rung TEMPLATE, but the wake at rung `i` searches for the DEMONSTRATION over `L_{i-1}`. A rung returning a non-Grid value (Mask/Int/Offset/Coord) **must** be wrapped -- a task solution has to produce a Grid -- so its demo costs `d_i + (wrapper depth - 1)`. Both authored ladders linted **clean** while EVERY rung's demo was structurally out of reach; the probe then censored uniformly, reporting only "not affordable" with no cause (2 x ~10 min to discover what the new check names in ~1 s).
    - **Corollary: a ladder with any non-Grid rung requires `depth_limit >= 3`** (proper-composition forces `d_i >= 2`, the wrapper adds 1). The whole al1-al20 batch is Grid-valued at `depth_limit = 2`, which is exactly why this never surfaced.
  - **Three breadth mechanisms measured, all distinct:** `floordiv` closing over a growing INT pool (`int(1034) x int(1034)`, censored at 2M considered -- integer division is a breadth bomb, and exact-halves splitting inherently needs it); four sibling `shift_*` rungs each independently floor-reachable, so every one probed as an explicit SKIP PATH while taxing every later round; and `paint_through_mask`'s ternary `grid(2004) x mask(129) x color(10)`, which Mask rungs force into the floor since it is the only mask->grid writer.
  - **Two machinery gaps closed** (`make check` green, 907 fast tests):
    - `rewrite-shallow` used `rungs[i]` -- the next rung by LEVEL -- as "the layer above", so on a DAG it convicted sibling branches of skip paths that never existed (skipping `left_half` "leaves `right_half` reachable"; `right_half` never called it). Now consumer-driven, matching the generalisation `double-jump-intractable` already had. **Byte-identical on chains**, so the batch locks are untouched -- this is TODO item 9's gate ("only bites once a DAG ladder has a real config") firing for real.
    - New check **`demo-affordable`** (D-family, error, structural), + its lock and a dormancy lock over the batch.
- **Interpretation:** the demo-affordability law is **structural** (a non-Grid rung costs one extra depth level, always); the cost blow-up I hit alongside it is **not** -- it was a property of the FLOORS I authored, and conflating the two would be wrong. Cost is `b^d` with `b = primitives x constants` (same-day rule); the return type fixes `d >= 3`, and `b` alone decides whether that round is cheap. Both of my floors had fat `b` for authoring reasons: the OFFSET constant battery is **(max_dim + 1)^2** (400 leaves on 19-wide grids), `translate` multiplies grid pool x offset pool, and `paint_through_mask` is **ternary** (a 3-way product). The 4347f46a floor had only SIX primitives and still blew up -- so the driver was constants and arity, not primitive count and not Mask-ness. Counter-evidence that non-Grid rungs are fine on a lean floor, from this lab's own cells: the source-square percept (d3, Rect-bearing) at **33-40 considered** (07-24 Phase-0 probe), and `instantiate_first` (d3, Mask) at **13,675**, `first_target_color` (d4, Color) at **4,720**, `seeded_source_mask` (d4, Mask) at **8,387** (same-day pruning sweep). Cheap perception did NOT erase these gaps (`raw-intractable` holds at `d_raw` 7-9) -- the obstacle was breadth, not Family-B.
- **Design rule this yields (supersedes the rung-type framing):** constrain `b`, not the rung's return type -- floor small, `finite-enumerate-scalars` or narrower, no addressing-constant battery unless coordinates genuinely come from constants, prefer unary/binary primitives over ternary. Depths: `d <= 2` always fine (the whole al1-al20 batch); `d = 3-4` fine on a lean floor (the cells above) and censoring on a fat one (2M considered, this entry); `d >= 5` unrescuable by pruning (same-day sweep; `place_copy_right` d7 never solved, considered exploding between depth 4 at 112k and depth 5 at >300k).
- **Next:** re-author under the corrected rule -- **lean floor, `d <= 4`, rung return type unconstrained**. Explicitly NOT "Grid-valued rungs only": that would exclude cfb2ce5a Cohort B, whose Rect/Int/Coord/Mask rungs are the MVE's laddered-perception arm and are already known to search cheaply on a pruned floor. `4347f46a` is re-authorable with a leaner floor (drop the ternary writer from the search space, shrink the offset battery); `dae9d2b5` stays parked -- exact-halves splitting needs `floordiv`, whose INT pool is quadratic by construction and not a floor-authoring choice. Both authored ladders stay in `drafts/` as the fixtures that make the demo-affordability law concrete.

## 2026-07-25 — First real-ARC ladder that lints and learns clean: `dae9d2b5`, and the perceived-vs-computed geometry rule

- **Commit:** (pending -- same branch). Continues the same-day MVE screen entry; executes MVE-PLAN step 1.
- **Question:** the first screen's three tasks all hit breadth walls. Was that the TASKS, or the floors I chose for them?
- **Ran:** re-screened with the ENGINE (real search over all 400 arc1-train tasks, lean floor, `solution_limit=1`) instead of by eye; then rebuilt the `dae9d2b5` ladder on a re-designed floor and probed it.
- **Result:**
  - **It was the floors, not the tasks.** Two substitutions removed the cost entirely rather than trimming it:
    - **`halves_h` / `halves_v`** (new, ~20 lines in `primitives/regions.py`): the `concat_h`/`concat_v` inverses as region producers. The split becomes **perceived** rather than **computed**, so `floordiv` -- whose INT pool squares each round -- leaves the search space altogether. `quadrants` already computed exactly this split internally; it only ever bundled it with the row split.
    - **`overlay` is the grid-level OR.** Combining two grids needs no Mask rung, so every rung stays Grid-valued, every demo is a FULL SOLUTION (so `AntiunifyPairs` serves it and `demo-affordable` is trivially met), and no addressing battery is minted.
  - **Metrics:** `d_raw` 9 -> 6 -> 5 across the rebuilds; ladder cost from *censored at 2M considered* to a **~90k-considered depth-3 enumeration**. Lint: **0 errors, validity window [3,4]** (non-degenerate -- both double-jumps 5, `d_raw` 5). Probe: **both rungs wake AS-INTENDED and sleep RECOVERS each at the intended arity**. Ground truth: the top reproduces the real arc1-train task on all 5 train + both held-out tests (`test_real_arc_anchoring.py`, the general form of the cfb2ce5a check).
  - **`map_color` DISTRIBUTES over `overlay`** -- found as an explicit probe SKIP PATH: `overlay(0, map_color(west(input), 4, 6), map_color(east(input), 3, 6))` reaches the top without the `merged` rung, because merge-then-recolour equals recolour-then-merge when the colours do not collide. Stating the top in that distributed form is what makes the ladder skip-free. A **candidate `BASE_EQUATION`** (TODO item 19) -- with it `rewrite-shallow` catches this in ~1s instead of a 25-minute probe -- but it is CONDITIONAL (breaks when the recolour target equals the overlay mask colour, and it depends on colour ordering) and is not depth-reducing in the abstract, so it is recorded, not landed.
  - **A THIRD instance of the chain-adjacency assumption**, this time in the probe: `skip_ids = spec.rungs[level].demonstrations` -- the next rung by LEVEL. On a DAG that is a sibling, not a consumer, so `west` was convicted of leaving `east` reachable when `east` never called it. Now consumer-driven, byte-identical on chains. The three sites were `double-jump-intractable` (fixed earlier), `rewrite-shallow`, and `probe.py`.
  - **Engine screen (400 tasks, lean floor, depth 3, 40k guard, 983s): 28 solved** -- 15 at gen 1 (too shallow to host a ladder) and 13 at gen 2. Five of the gen-2 hits are the D4-tiling family (excluded: al3/al7's self-similar doubling). **Remaining candidates: `28bf18c6`, `5614dbcf`, `7468f01a`, `9565186b`, `9f236235`, `a740d043`, `d9fac9be`, `f25fbde4`.** A gen-2 solve is a ladder candidate precisely because a ladder's floor is a SUBSET of the screen's -- withholding the primitive that made a task shallow is how depth is manufactured.
- **Interpretation:** the operative distinction for real-ARC ladder design is **perceived vs computed geometry**. Perceived geometry (regions, bboxes, content coords, half-splits) mints no constants and keeps `b` small; computed geometry (halves via division, unit shifts via an offset battery, fixed indices) forces either a quadratic addressing battery or binary INT arithmetic, and both are breadth bombs. This is the *actionable* form of the same-day `(primitives x constants)^depth` rule: it names which floor to author, not merely which depth to fear. It also corrects this session's earlier over-generalisation -- the rung's return type is a one-level cost, not the driver.
- **Resolved -- the ladder PROBES CLEAN (1/1), and the guard is now a measured quantity.** Proving `no_skip_paths` needs the depth-3 enumeration to run to EXHAUSTION (a censored skip search proves nothing), and that search measures **22,153,716 considered** -- dominated by `overlay`'s quadratic grid product (10 x G^2, the price of having a grid-level OR at all). Guards of 300k / 2M / 3M all returned INCONCLUSIVE; at 40M the verdict is **`no-skip`**, wake AS-INTENDED, sleep RECOVERED. The ladder pins 30M. **The cost datum worth carrying: this ladder is certifiable but each probe cell costs ~20M considered (~6 min)**, which prices a full `run-ladder` and is a floor-design signal -- `overlay` is the expensive primitive here, not the depth.
- **Next:** granularity members on this cohort (raw cancels -- the cheapest scientific value in the set); `94f9d214` / `fafffa47` on the same skeleton via `halves_v`; cfb2ce5a v5-core as Cohort B. A `panels` producer (split on constant-coloured dividers) would unlock the six separator two-halves tasks at once.

## 2026-07-26 — Optimal pruning: `dae9d2b5` rung 1's 21M considered is floor breadth the rung never uses (21,149,854 -> 4)

- **Commit:** (pending -- same branch). Corrects the attribution in the preceding entry.
- **Question:** the `dae9d2b5-halves-union` skip search costs ~22M considered at depth 3. The preceding entry attributed that to `overlay`'s quadratic grid product. Is that right?
- **Ran:** two cells on the SAME wake search (rung `west`, demo `west-00`, library `L_0`, the ladder's own budget): (A) the full 6-primitive floor with `by_primitive` attribution dumped, (B) the library cut to the 3 primitives the rung's own unfolded program references ("optimal pruning"). Because constants are minted only for types the library USES (`leaves.py::_type_in_use`), pruning the library prunes the constant battery with it.
- **Result:**
  - **Metrics (same rung, same budget, same goal; both SOLVED at generation 3):**
    - full floor `[crop_rect, halves_h, head, map_color, nth, overlay]`: **21,149,854 considered**
    - pruned floor `[crop_rect, halves_h, head]`: **4 considered**
    - ratio: **~5.3 x 10^6**
  - Attribution on the full floor: `__const__` 21,149,850 (100.0%), `map_color` 21,149,791 (100.0%), `overlay` 21,136,640 (99.9%); the three primitives the rung actually needs total **779** (`halves_h` 394, `crop_rect` 256, `nth` 137, `head` 129). Shares overlap because one composition counts in several buckets.
- **Interpretation:** **the preceding entry's "dominated by `overlay`'s quadratic grid product" is wrong** -- I asserted a mechanism I had the attribution data to check and did not check. The cost is not `overlay` specifically and not intrinsic to depth 3: it is **breadth this rung never uses**, and it splits three ways, not two -- (1) constants the rung needs, (2) constants it doesn't, (3) *primitives* it doesn't, which cost twice over (their own compositions, plus the pool inflation those feed into every later round). `overlay` and `map_color` are needed by the TOP, not by rung 1, and their presence in `L_0` is what mints COLOR at all.
- **Next:** this is the per-rung sibling of TODO item 29's per-rung budgets -- a rung's search need not see the whole floor. Not landed: pruning to `names_in(intended)` is an ORACLE (it reads the answer), so it can price a floor but cannot be the run's machinery. What it can do is size guards honestly and flag floors whose breadth no rung justifies.

## 2026-07-26 — Depth definitions unified on the programs the searches actually run (batch: a no-op, by measurement)

- **Commit:** (pending -- same branch).
- **Question:** `jump-affordable` measured the rung TEMPLATE while the top's affordability was already measured on its TASK SOLUTIONS, which is why `demo-affordable` had to ship as a second check (07-25 entry). Is the same split present elsewhere, and should the rung-depth definition itself move to the demonstrations?
- **Ran:** audited every derived depth quantity in `checks/context.py`, moved both bounds onto the search-side programs, then diffed the whole batch's validity windows and error sets before/after.
- **Result:**
  - **The upper bound had the same break, unnoticed.** `graph.consumer_programs` fed the double-jump "higher rung TEMPLATES + top task solutions". But a consuming rung's wake searches for ITS demonstrations, and a demo can only be deeper than its template (wrappers add), so **the computed double-jump was <= the true one and validity windows were reported narrower than they are.** Conservative, but it can call a ladder degenerate that isn't.
  - Now: `RungShape` carries `template_depth`/`template_needs` (mint-side, what sleep must recover) alongside `jump_depth`/`jump_needs` (search-side, demo-sourced) + `depth_source`; `CheckContext.consumer_targets` is the search-side twin of `consumer_programs`; the window is stated in `min_depth_limit` throughout (it was mixing `compositional_depth` into the bounds while every check read `min_depth_limit`). `demo-affordable` is **deleted** -- folded back into `jump-affordable`, one claim again. `proper-composition` stays on the template deliberately (a wrapper cannot make a bare primitive an abstraction).
  - **Metrics (21 ladders, before -> after):** validity windows **unchanged on every ladder**; error sets **unchanged on every ladder**; exactly one rung reports a different number -- `al4-mask-crop`'s `nonbg_mask` (the batch's only Mask rung) now says needs 4, not 3, which is its truth and inside al4's pinned window (4,4). 30 checks -> 29. `make check` green (921 tests).
  - **Two latent bugs this exposed, both of which silently LOST convictions:**
    - `analysis/rewrite.py::_target_leaves` collected `Param`/`Input` but not **`Const`**. A target stated with its parameters BOUND (which every demo target is: `3` where the template has `#1`) could not be re-expressed, because the witness couldn't name the literal. This un-convicted `rewrite-shallow[rot180]` on al6 the moment targets became demo-sourced.
    - `rewrite_verdicts` emitted one verdict per TARGET, so a consumer with two demonstrations convicted its rung twice. Now one verdict per consumer, first confirmed witness wins.
  - **Dead end recorded:** `dae9d2b5-3-recolor-rungs` (drafts/) -- promoting the two recolours to rungs so the top becomes a plain `overlay`. Lints **0 errors** but the validity window is **[3,2]**, degenerate: `west`/`east` are d3 so the ladder cannot run below `depth_limit` 3, and at 3 the recolour rungs' double-jump (3) is already reachable -- the skip witness is verbatim the SHIPPED ladder's own top. **The shipped 2-rung form is the maximal granularity for this competence on this floor.** Cost to establish: one lint, ~1s, no probe.
- **Interpretation:** the template/demonstration split was one defect wearing two faces, and fixing only the lower bound (as `demo-affordable` did) left the upper one wrong in the opposite direction. That both bounds move and NOTHING in the batch moves is the result worth having: the al1-al20 set is Grid-valued and full-solution-demoed, so the two readings coincide there by construction -- the correction is dormant until a ladder ladders below the Grid type, which is exactly when it starts to matter.
- **Next:** a 4-rung `dae9d2b5` granularity member needs the floor to make the half-crop d2 -- a `split_h: (Grid) -> List[Grid]` primitive (no `List[Grid]` producer exists; all 17 list-returning primitives yield rects/colors/coords/masks). That gives window [2,2], `d_raw` 4, and a genuine granularity pair on the same real task.

## 2026-07-26 — Per-rung depth budgets (derived by default), and the DAG reads that were level reads

- **Commit:** (pending -- same branch; base `99d38b2`). Wave 1 of [docs/abstraction_ladders/2026-07-26-AL-PLAN.md](docs/abstraction_ladders/2026-07-26-AL-PLAN.md).
- **Question:** two things the `dae9d2b5` build exposed. (1) Four sites read "the layer above rung `i`" as `rungs[i]` -- the next rung BY LEVEL -- which on a DAG names a sibling; two were still live. (2) A single `depth_limit` must be at once deep enough for the deepest jump and shallow enough that the shallowest double-jump stays out of reach, so a ladder whose rungs differ in depth can have NO valid budget. Does a per-level schedule dissolve that, and what does it cost?
- **Ran:** converted the level reads to the consumer graph; made the depth schedule per-level and derived-by-default, with an explicit `ladder.depth_schedule: 'pinned'` opt-out; swept the derived schedule across all 22 registry ladders before changing anything.
- **Result:**
  - **The level reads fail in BOTH directions at once, and one of them silently rejects.** On `al21-dag-siblings` (built for this, below) `no_skip_paths[1]` probed rung 2's tasks under `L_0` -- but rung 2 composes over the bare floor, so `L_0` solves them trivially and the ladder is **rejected for a "skip path" that is just a sibling being independently reachable**. `demonstration_health[2]` separately demanded rung 2 route through rung 1, scoring it **0.0**. A third site was found in the sweep that the plan had missed: `report.py`'s marginal-rung-value view (`layer_above`, and the cost ratio computed from it).
  - **`max(pinned, need)` does not work.** Tried first because it preserves the batch byte-for-byte; it fails precisely on the ladders per-rung budgets are for, which need levels **lowered** below the pinned value, not raised. The schedule has to be purely derived, which is why the `pinned` opt-out exists rather than a merge rule.
  - **Metrics (22 ladders, sweep):**
    - schedules differing from the pinned value: **8** (al3 `[4,3,3,3]`, al4 `[4,4,3,3]`, al5 `[3,3,3,2]`, al6 `[2,3,3,3,2]`, al8 `[3,3,3,2]`, al10 `[2,2]`, al13 `[2,3,2]`, al14 `[3,3,3,2]`); the other 14 are already uniform and their run identity is unmoved.
    - per-rung sandwich (`jump_needs < double_jump_needs` at every level): **22/22 clean**.
    - lint error sets and validity windows: **unchanged on every ladder**. `make check` green, **926** tests (from 921).
  - **The 2026-07-25 dead end is overturned.** The preceding entry recorded `dae9d2b5-3-recolor-rungs` as degenerate (window `[3,2]`) and concluded "the shipped 2-rung form is the maximal granularity for this competence on this floor", with a `split_h: (Grid) -> List[Grid]` primitive as the way out. Under a derived schedule it lints **0 errors** at `[3, 3, 2, 2, 2]` -- `top-double-jump-intractable` clears too, because level 3 now runs at 2 while the skipped top needs 3. **The 4-rung granularity member exists, needs no new primitive, and is cheaper than the 2-rung form** (three of its five levels run a whole round shallower).
  - **`al10-skippable` is the one ladder that must stay pinned.** Its content IS the uniform budget: `depth_limit: 4` makes its rung skippable, and derived would set `L_0` to `rot90`'s own need of 2, where the skip path is simply unreachable. So the control now declares `ladder.depth_schedule: 'pinned'` and its verdict is unmoved -- and the same file under the two regimes is itself a head-to-head on what the schedule buys.
  - **New instrument: `al21-dag-siblings`** -- two independent branches under one top, over a 4-primitive floor at `depth_limit` 2. Admitted, 2/2 rungs recovered, health 1.0/1.0, certified end-to-end in **0.26s**. `dae9d2b5-halves-union` is the real-task DAG but its chain is hours, so it can guard nothing in `make check`; this can.
  - **A stale artifact flushed, for an unrelated reason.** Regenerating `al5-perceiver-chain` (the only mover carrying committed run artifacts) showed its committed report predated the 50k `considered_limit` entering the ladder default: cells went from 328k-4.07M **unguarded** to the 50k guard. `no_skip_paths[2]` moved `False -> None` as a consequence -- the skip path it used to find is now censored before it is reached, which is the tri-state design working. Its `L_3` column (15-23k) no longer hits the guard at all: that part IS the derived schedule, level 3 running at depth 2.
- **Interpretation:** the two fixes are the same shape of error -- **a structural question answered with an index**. "Who consumes rung `i`" is not "rung `i+1`", and "what budget does level `j` need" is not "the one number in the config". Both readings are exactly right on a chain with uniform rung depths, which is every al1-al20 ladder, which is why neither surfaced until a real ARC task produced a DAG with mixed depths on the first try. The batch not moving is the load-bearing part of the result: 14 ladders' identity is untouched and no lint verdict changed anywhere.
- **Next:** Wave 2 -- the probe overhaul (fixed guard, pruned-then-full cells, `by_primitive` surfaced, recorded through `execute()`) and the floor-breadth census. `dae9d2b5-halves-union`'s own certificate is still unpurchased: its chain is a multi-hour enumeration at ~21M considered per cell, which is the breadth problem Wave 2 exists to make visible, not a depth one.

## 2026-07-26 — The breadth axis, instrumented: a static census and a measured floor tax

- **Commit:** (pending -- same branch). Wave 2 of [docs/abstraction_ladders/2026-07-26-AL-PLAN.md](docs/abstraction_ladders/2026-07-26-AL-PLAN.md).
- **Question:** the 07-25 pruning diagnosis found rung 1 costing 21,149,854 considered on the full floor and **4** pruned, and concluded the data to see it had been available all along. What instrument makes that visible BEFORE an hour is spent, and what does it say about the ladders already committed?
- **Ran:** built two instruments -- a static round-1 breadth census in the lint (`ladders/breadth.py`) and a pruned-vs-full cell pair in the probe (`FloorTax`) -- then swept the census over all 22 registry ladders.
- **Result:**
  - **The census finds it in ~1s, statically.** `dae9d2b5-halves-union` r_1 reads **b1 1,211 vs 1 = 1,211x**, with `color: 10 minted / 0 used` and the responsible primitives named (`map_color`, `overlay`). No search, no probe, no run.
  - **It ranks the whole batch, and the outlier is the real-task ladder** -- by 4x over the next worst:
    - **Metrics (round-1 tax, worst first):** `dae9d2b5/east` 1,212x · `dae9d2b5/west` 1,211x · `al13/sym_h` 278x · `al13/sym_both` 225x · `al14/move_grid_up` 110x · `al19/pair` 59x · `al20/pair` 58x · ... · `al2/rot90`, `al3/quad`, `al10/rot90`, `al12/rot90` all **1.0x**.
    - The 1.0x rows are the D4 floors, which mint no constant battery at all -- the census separates "this floor is fat" from "this rung is deep" cleanly enough that the lean floors read exactly 1.
    - **Previously invisible:** al19/al20's `pair` and al17's `shift2` carry 25-30 wasted constant leaves each, entirely from `map_color`/`translate` putting COLOR and INT "in use" for rungs that take neither.
  - **The census UNDERSTATES by ~4,000x, and is still decisive.** Round 1 says 1,211x where the probe measured ~5.3e6 at depth 3 -- the tax compounds, so this is an INDICATOR for ranking, never a forecast. Both the module and the rendered table say so in those words.
  - **Two of A3's premises were false, which made it much smaller than planned.** (1) The plan called content-hashed `Corpus` identity for probe cells "the bulk of A3's work" -- but `RunSpec.run_id` already hashes `corpus.content_hash()`, not the name, so it exists. (2) It said probe cells "search over synthesized position-separating grids which are not a registered corpus" -- they do not: every probe search runs the ladder's own committed corpus tasks, and the synthesized grids are only used for BEHAVIOURAL comparison (`_agrees_off_train`, `matches_target`), never as search inputs.
  - **Constant pruning needed no new search-config field.** The plan specified "an explicit value allowlist threaded into the search config". Not needed for the measurement: `policy_constants` already gates each base type on `_type_in_use(type, library)`, so pruning the LIBRARY drops the constants with it -- which is exactly how the 07-25 cell got "3 primitives, zero constants". Adding a field to `BottomUpSearchEngine` would have changed `config.to_dict()` and therefore **every `run_id` in the repo** (`to_data` emits all fields), invalidating the whole `runs/` cache and every committed `report.json`, to make one probe cell tighter. The remaining value-level headroom is instead computed exactly and for free by the census's `b1_constant_pruned` corner.
  - **The CLI was actively instructing the failure.** `--considered-limit`'s help read *"a censored probe is inconclusive, so raise it when a cell censors"* -- a direct instruction to do the thing that cannot work. Renamed `--guard`, help states the asymmetry, and raising it now prints a warning naming `run-ladder` as the authority.
  - **Probe cells are RECORDED RUNS** (the plan's A3 decision, built). `execute()` is a stack, not a step -- `execute` -> `_run_search` -> `_run_task` -> `search_once` -> `engine.run` -- and the probe was reimplementing the last of those, five arguments spelled out a second time. Each cell (one rung x one library x one task) is now a single-task corpus under a `probe:` namespace driven through `execute()`, so it caches, resumes after a crash, and can be read back; a re-probe of an unchanged rung is a cache hit rather than a fresh enumeration. `arc-lab runs` hides cells by default and prints how many it hid (`--probes` to show them), so the ledger stays a list of results without the cells becoming invisible.
  - **The risk that created, and its lock.** The probe no longer reads the engine's return value -- it reads the trace and rebuilds it (`execute.search_result_from_row`, the exact inverse of `_search_stats`, written adjacent to it). A field the writer emits and the reader forgets would NOT raise: it would become a default, and a probe verdict would come back plausible and wrong. Pinned field-by-field over every `SearchStats` field against the same search driven straight through the engine. One field needed explicit recovery rather than a default -- `returned_solution_count` is not serialised (it is `len(ranked_programs)` by construction), and defaulting it to 0 would have made every rebuilt result read `solved == False`.
  - **A property that changed and needed re-pinning.** The probe used to sit below `_run_task` and so structurally could not see a test grid; through `execute()` it predicts and scores like any run. Still sound -- the probe reads `search_stats` and the retained programs, and the scorer touches neither -- but it is now a property of what the probe READS rather than of what it can reach, so it gets a test: a ladder whose test outputs are replaced with garbage must probe identically.
  - `make check` green, **935** tests (from 926).
- **Interpretation:** the 07-25 failure was not a missing measurement, it was a missing DEFAULT. `by_primitive` existed, the pruning trick existed, `forecast_cost`'s typed census existed -- what did not exist was anything that showed them without being asked. Two of the three instruments built here are reuse, not new code (the census is `forecast_cost.round_terms` under four prunings; the floor tax is the wake search re-run on a pruned library), which is the strongest evidence that the gap was surfacing rather than capability.
- **Next:** Wave 3 -- Compromise Options, the generator overwrite guard, the lint output pass, and the process doc.

## 2026-07-26 — Wave 3: labelling compromises, guarding the write, and the process doc

- **Commit:** (pending -- same branch). Wave 3 of [docs/abstraction_ladders/2026-07-26-AL-PLAN.md](docs/abstraction_ladders/2026-07-26-AL-PLAN.md), completing it.
- **Question:** what is left that machinery can enforce, and what genuinely cannot be — the residue that has to be a written process rather than a check?
- **Ran:** built the Compromise Options registry, the guarded `.ladder` writer, and the remaining lint-output pass; then wrote [LADDER-PROCESS-2026-07-26.md](docs/abstraction_ladders/LADDER-PROCESS-2026-07-26.md) for what was left.
- **Result:**
  - **Compromise Options are DETECTED, not declared** (`ladders/compromise.py`). Three entries -- `pruned-library` (voids all cost interpretation), `wake-schedule`, `solution-limit` (narrows: RQ1 survives, since `first_solution_index` is exact either way) -- each stating what it saves, what it forfeits, and when it is justified. `compromises_in(config)` reads them off the `Config` itself: an oracle-pruned library by its `:pruned` name, an early stop by `solution_limit`, assistance by `wake_schedule`/`curriculum`. **A label you must remember to set is the one that gets forgotten**, which is the entire failure mode; deriving it removes the choice. The report banners them AHEAD of the numbers (generalising the `wake_schedule` arm-label precedent, which is now one entry among three).
  - **A6's premise did not match the repo, and the guard moved.** The plan said "a generator must refuse to overwrite an existing `.ladder`" -- but nothing in the repo writes `.ladder` files; `taskgen` reads one to build a testbed, and the five destroyed variants were written by throwaway scratch scripts. So the machinery is a supported PATH rather than a guard on an existing one: `arc-lab new-ladder <name> [--from <existing>] [--registry] [--force]`, which refuses to overwrite and makes "the next variant" (copy + rename the `ladder` header, original untouched) cheaper than reusing a name. It cannot stop a scratch script -- nothing can -- but there is now a path that cannot lose work.
  - **The scaffold has to LOAD.** First version was a comment skeleton and failed to parse (`a rung section starts with its definition`), which would make the first thing a new ladder does a failure unrelated to its design. Now a placeholder spine over two real primitives that lints from the first edit.
  - **A7:** `spec.md` carries all six depth quantities plus the per-level budget (`L_i-1` · `d_i` · `needs` · `d_tmpl` · `tmpl needs` · `double-jump` · `dj needs`); the terminal table keeps the four that carry claims. Measured costs stay OUT of `.ladder` files (LADDER-FORMAT's chosen/derived split; a stale cost comment is worse than none).
  - **Stale claims corrected where they were found**, not just where planned: AL-PLAN-2026-07-23's pipeline line quoted the probe at "~1s/rung" (true only at the default guard on a lean floor -- it is minutes on a fat one), and MVE-PLAN-2026-07-25's screen leg 3 encodes DEPTH as the filter when the measurement says breadth was the variable that mattered. Both now carry a dated correction rather than a silent edit.
  - **Metrics:** `make check` green, **945** tests (from 921 at the start of this plan, +24). No regression lock moved across all three waves. 14 of 22 ladders' run identity untouched; the 8 that moved did so deliberately (derived depth schedule).
- **Interpretation:** the split the plan predicted held up -- almost everything worth enforcing turned out to be enforceable, and the doc is short because of it. What genuinely could not be machinery is exactly three things: **instrument contracts** (what a probe can and cannot prove is a fact about inference, not code), **design taste** (a rung being a "nameable competence" has no checkable form), and **operational discipline** (estimate before launching; do not pipe long output through a buffering filter). Everything else in the original 9-section outline collapsed into a table pointing at a check that already fires.
- **Next:** the plan is complete; the MVE build ([MVE-PLAN-2026-07-25.md](docs/abstraction_ladders/MVE-PLAN-2026-07-25.md)) is unblocked. First real use of the new instruments: re-screen candidate tasks with the breadth census in the loop, and purchase `dae9d2b5-halves-union`'s certificate against a floor whose tax has been read rather than guessed.

## 2026-07-26 — Value-level constant pruning, added without moving a single `run_id`

- **Commit:** (pending -- same branch). Closes the last open A3 item of [docs/abstraction_ladders/2026-07-26-AL-PLAN.md](docs/abstraction_ladders/2026-07-26-AL-PLAN.md).
- **Question:** the probe's "pruned" cell pruned only the LIBRARY. The plan specified a value-level constant allowlist too, and that was skipped on the reasoning that `_type_in_use` already drops whole types when their primitives go. Is that reasoning right, and what does adding the allowlist cost?
- **Ran:** measured the four breadth corners across the batch to test the reasoning, then added `BottomUpSearchEngine.constant_allowlist` and wired it into the probe's pruned cell.
- **Result:**
  - **The reasoning was wrong, and wrong in the case that matters.** Library pruning is the whole story on `dae9d2b5-halves-union`'s `west` (b1 1,211 -> 1) -- which is the case it was generalised from. On `al14-cell-row-grid`'s `move_cell_up` it does **nothing at all**:
    - **Metrics (round-1 width, `move_cell_up`):** full **300** · primitive-pruned **300** · constant-pruned **12** · both **12**.
    - Every primitive the program references survives pruning, so the INT/COLOR batteries survive with them. A floor-tax cell built on library pruning alone reports **ratio 1.0x, verdict `clean`** on that rung -- i.e. "this rung is expensive" -- when the truth is "its constant battery is". A live wrong-diagnosis path in the instrument whose entire purpose is that diagnosis.
    - End to end after the fix, `probe-ladder al14-cell-row-grid --level 1`: `pruned 8,879 vs full 50,000 = 6x [floor-too-broad]`, `dropped []` (library pruning still drops nothing), attribution `__const__ 49,999 (100.0%)`. The whole reduction is the allowlist, and the verdict is now the right one.
  - **The cost objection dissolved.** Adding a field to a search engine changes `config.to_dict()` and therefore every `run_id` (`to_data` emits all fields) -- which was the stated reason to skip it, and it was real. Resolved instead of paid: `to_data` now honours a `SERDE_OMIT_WHEN_NONE` class marker, so a field whose `None` provably means "no effect" is left out of the serialised form entirely.
    - **Verified, not assumed:** all **446** recorded runs' stored ids still recompute exactly (0 mismatches), a freshly-built preset config carries no `constant_allowlist` key, and a recorded run's stored config still equals a freshly-built preset byte for byte. No migration, no cache loss, no artifact regeneration.
    - The bar for the marker is deliberately high and documented: `None` must mean the component behaves as if the field did not exist. It is NOT a general omit-defaults rule -- that would move existing hashes, since `Budget.solution_limit` is already `None` and emitted.
  - `make check` green, **949** tests.
- **Interpretation:** two lessons, and the second is the one worth keeping. First, "type gating already handles it" was a generalisation from one rung to a batch, and the counterexample was a `lint-ladder` call away. Second: the identity-churn objection was true but was treated as a *reason not to act* rather than a *problem to solve* -- and the solution was four lines of serde. The cost of a change is a thing to attack, not a verdict.
- **Next:** the plan's machinery is complete. Ladder work (`dae9d2b5-3-recolor-rungs`, and the d2-throughout variant that needs a `split_h: (Grid) -> List[Grid]` producer) resumes as its own thread, separate from the process upgrades.

## 2026-07-27 — The first certified real-ARC ladder: plumbing depth was the cost, and an abstraction that typed itself out of its own search

- **Commit:** (pending -- same branch). Executes MVE-PLAN-2026-07-25 on `dae9d2b5` under the 2026-07-26 process; the predecessor `dae9d2b5-halves-union` stays unrun as the baseline.
- **Question:** `dae9d2b5-halves-union` costs 21,149,854 considered per cell against **4** optimally pruned. The 07-26 attribution blamed floor breadth. Given the base `b` is pinned by the TOP's vocabulary and no rung redesign can remove it, is the ladder redesignable at all -- and if so, on what lever?
- **Ran:** re-derived the spine on the principle that **the only lever a spine controls is the EXPONENT at each level**; added `split_h`/`split_v`; authored a granularity pair over the new floor; lint -> probe -> taskgen -> `run-ladder`.
- **Result:**
  - **The cost was PLUMBING DEPTH, not the floor.** `crop_rect(g, head(halves_h(g)))` is d3 and only ONE of its three levels computes anything -- the other two convert `List[Rect] -> Rect -> Grid`. `split_h: (Grid) -> List[Grid]` returns the halves in the type its consumers want, making the same competence **d2**. The floor also shrinks 6 -> 4 primitives (`halves_h`/`crop_rect`/`head` all leave).
  - **`dae9d2b5-split-recolor` is ADMITTED -- the program's first certified ladder on a real ARC task.**
    - **Metrics:** 4/4 jumps tractable · no skip paths (every skip search ran to EXHAUSTION, not censored) · demonstration health **1.0** on all four rungs · learned climb converged at iteration 2 with **all 4 rungs recovered** (no junk, no cascade) · RQ1 **>= 10x** (raw arm censored at 25,007,890) · loop-overhead **3.08x** · depth compression `d_raw` 4 -> max jump depth 2 · cells exhaust at **230,497 -> 341,629** considered (vs 21,149,854 on the predecessor, **~62x** cheaper per cell at 5 levels instead of 3).
    - Derived schedule `[2,2,2,2,2]`: every level runs at depth 2, which is the entire design.
  - **The granularity pair inverts the naive reading.** `dae9d2b5-split-halves` (2 rungs, same floor, same top, **same budget**) folds the recolours into the top, making it d3 -- schedule `[2,2,3]`, and its skip test must reach d4. Both rungs wake AS-INTENDED and sleep recovers, but at the pair's shared 2M budget **every skip search censors**, where the fine member's exhausted at ~300k. **Fewer rungs cost MORE**, predicted in the file's header before running. Certifying it needs a larger shared budget -- a second escalation, left as a decision rather than taken.
  - **A substrate bug that makes a ladder silently unclimbable** (`make_abstraction`). `Program.result_type` reports a primitive's DECLARED return type without unification, so a template rooted at a POLYMORPHIC primitive derives a free type variable: `nth(split_h(g), 0)` typed as **`a`**, not `grid`, even though `split_h : (Grid) -> List[Grid]` pins it. An abstraction with an unpinned result is not rejected -- the enumerator **silently skips** it (`unpinned_type_var_mode='reject'`). So the ladder linted **98 checks clean** and its consumer's wake exhausted the whole depth-2 space (230,497 considered) **without once composing the rung it had been gifted**, reporting `unsolved`.
    - `load.py::_template` builds the primitive WITH the declared signature, asserts it, and then **discards it**; `chain.py::oracle_libraries` rebuilds without one. Every prior rung in the repo is rooted monomorphically (`crop_rect`, `map_color`, `rot180`), which is why this never fired. `learn/engines.py` mints with no signature either, so learned abstractions had the same hole.
    - Fixed by unifying inference in `make_abstraction`, applied ONLY when the declared reading leaves free variables -- a refinement, so every existing abstraction keeps byte-identical types, library serialisation and run identity. 5 regression tests.
  - **New check `primitive-necessity`** (A-family, warn): per floor primitive, which level's search first has to COMPOSE it, and which levels carry it below that. It reads `dae9d2b5-halves-union` in ~1s as `overlay`/`map_color` needed at L_2 (top), carried from L_0 at depth 3 -- the thing that cost hours to find by measurement.
    - **Its first version was a false negative on the very ladder it was built for.** Gated on depth >= 3 alone, it read `dae9d2b5-split-recolor` clean at an all-depth-2 schedule -- while `overlay` took **98.8%** of a 230,497-considered cell. Now flags two independent reasons, one per side of `b^d`: a deep carried level (the exponent) OR **arity >= 3** (the base -- a ternary/variadic primitive is superlinear in the pool inside a single round, so depth never enters). Fires on ~half the batch; it is a map to read against attribution, not a defect list.
- **Interpretation:** two things generalise. First, **the base is pinned by the top and the exponent is the spine's to choose** -- so the actionable question is never "is this rung expensive" but "which primitive is expensive, what necessitates it, and can that thing search shallower". Here the answer was a type conversion, not the competence: a `Rect`-returning producer is right when the rect is an ADDRESS, and wrong when every consumer immediately crops it. Second, **arity is a breadth multiplier independent of depth** -- the 2026-07-25 "prefer unary/binary over ternary" rule, now measured on a variadic at `max_arity` 3 and encoded in a check. The abstraction-typing bug is a third kind of lesson: lint proves unsoundness, never soundness, and this is the sharpest instance yet -- 98 green checks over a ladder that could not be climbed at any budget.
- **Next:** the coarse member's certificate (needs the pair's shared budget raised, ~20M, and re-running both so the comparison stays licensed); then MVE step 2 (cfb2ce5a Cohort B demo fix) and a third task from a different family. `split_v` ships alongside `split_h` and unlocks `94f9d214`/`fafffa47` on the same skeleton.
