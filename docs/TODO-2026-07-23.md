# TODO (2026-07-23)

The cross-cutting master list: everything on deck or plausibly worth doing, in one place. A dated snapshot — supersede it by writing the next one (old ones to `docs/archive/`).

**Discipline (so this doesn't rot):**

- **Pointer-heavy, drain-only.** An item's _content_ lives in its proper home — a phase in [abstraction_ladders/AL-PLAN-2026-07-23.md](abstraction_ladders/AL-PLAN-2026-07-23.md) (the active program), a MACHINERY.md row, or a backlog row in [../EXPERIMENT_QUEUE.md](../EXPERIMENT_QUEUE.md) (non-ladder / deferred) — this file only indexes and adds the items that have no home yet. Done or absorbed elsewhere → delete the line here.
- **Effort tags** `[S]` < half a day · `[M]` a real work item · `[L]` large/gated. **`⚑ steer`** = needs a user decision before starting.
- Orientation, not a log: what's DONE lives in EXPERIMENTS.md and the plan's DONE markers, not here.

## Small and ready `[S]`

1. **DONE (2026-07-23)** — `free-params-covary` lint (commit `96c2c24`): two free rung positions holding the same value at every call site fuse into one shared param under antiunification, costing an arity (sleep-probes S-B). Dormant on the batch (no ladder has covarying params), locked as such.
2. **DONE (2026-07-23)** — `hof-holes-fillable` lint (commit `96c2c24`): a higher-order floor primitive whose holes this config cannot fill is silently skipped at zero cost (micro-probes battery E: `map @ lambda-synthesis` under `unpinned_type_var_mode='reject'`). Landed in lint rather than the probe (the answer is static); tracks the config, reproduces battery E exactly. Dormant on the batch (no HOF floors), locked.
3. **Governance-refusal frontier** — sleep-probes S-C found no refusals in 2-3-op x 2-3-use cells; extend with single-use / near-zero-compression cells to map where GreedyMDL actually says no.
4. **DONE (2026-07-23)** — full / skip-solved / curriculum across all 7 clean ladders ([experiments/2026-07-23-schedule-comparison/](../experiments/2026-07-23-schedule-comparison/notebook.md)). Curriculum e2e == the marginal laddered cost EXACTLY (all 7), so the loop-overhead factor is full/curriculum = 3.0-4.0x; skip-solved recovers ~40-60% of that honestly. Junk mints appear under both carrying schedules on the 2 frozen-param ladders (al1, al17), clean on the other 5.
5. **DONE (2026-07-23)** — EXPERIMENT_QUEUE.md triage. The single-task search-behavior line is superseded by the ladder program; the queue is now the backlog of non-ladder / deferred experiments. Drained: extrapolator validation (run), control-floor cost concentration (absorbed into micro-probes battery C), point-the-loop-at-real-ARC (promoted to item 11). The rest moved to Backlog with a status-vs-ladder-program note each.
6. **Raw-arm K policy** — default K=10 everywhere; decide whether headline ladders buy stronger bounds (K=100 stays cheap where marginals are small). ⚑ steer

## Medium `[M]`

7. **`.ladder` lint cleanups**
   - (a) relocate `branching-summoned` from lint to the load layer (pure coherence, eval-free, corpus-free)
   - (b) drop `well-typed` from lint as redundant with load-time elaboration
   - (c) doc note only for the "references-only-downward" property — already enforced at load by the incremental-library scope, just record that load owns it.
   - (d) Independent tidy-ups.
   - (home: `spec.py::lint`, `LADDER-CHECKS-2026-07-21.md`)
8. **`.ladder` DAG support + draft-tier linting** (one coherent unit, motivated by cfb2ce5a — a real DAG the current chain-only check spuriously fails on 17/23 rungs, 0 actually dead). Four parts that compound:
   - (a) reformulate `rung-referenced` from chain-only ("called by the immediately next rung") to reachability ("called by a higher rung or the top").
   - (b) derive-and-report chain-vs-DAG from the reference edges (no declared flag).
   - (c) partition `LadderSpec.lint()` checks by input tier — templates+config only / corpus / computed outputs.
   - (d) `--draft` runs the templates+config tier instead of skipping wholesale, with a named ran/skipped report.
   - (c)+(d) are what make (a)'s dead-rung check actually fire on an assumed-primitives draft.
   - All template-only, safe for the all-chain batch.
   - (home: `spec.py`, `cli/lint_ladder.py`, `LADDER-CHECKS-2026-07-21.md`)
9. **Per-consumer double-jump / validity-window** — generalize the depth sandwich from chain-adjacency (`rungs[i+1]`) to the shallowest consumer with the rung inlined; likely simplifies the code by absorbing the last-rung special case.
   - Gated: only bites once a DAG ladder has a real config to test against — defer until then.
   - (home: `spec.py`, `analysis/depth.py`)
10. **Grow the ladder set** (AL-PLAN Phase 2 item 4, second half — the count problem). ⚑ steer on targets. Candidates, cheapest first: **al9/al11 control rebuilds on al17's spine** (new IDs; their distractor/governance questions are still unanswered) · **deep-jump ladders** at freed pools (budgets as `(depth, pool)` pairs, probe-gated) · **perceived-index al14 rebuild** (family-3 falsification) · **expensive-perceiver al5 variant** (family-2 falsification) · **first HOF floor** (item 2 lands first) · **first conditional floor**.
11. **Granularity family** (AL-PLAN Phase 3 item 7, but fully measurable NOW — same floor+top, so raw cancels): more smaller rungs vs fewer bigger rungs — the "optimal learning path" question made empirical. Arguably the highest research-value item on the board. ⚑ steer (spine + heights)
12. **Refold-carried-solutions option** — machinery candidate from sleep-probes S-D: skip-solved's junk mints come from frozen-param repeats surviving in sleep's input; re-expressing or deduping carried solutions each sleep might make the cheap schedule library-safe. Needs honest cells.
13. **Worksheets for al15–al20** — design narrative for the rebuild set's folders (generated artifacts exist; rationale is the designer's to write). ⚑ steer (authorship)

## Large / gated `[L]`

14. **Real-ARC anchoring** — cfb2ce5a is the first contact (in flight, other session). Standing tension: cheap perception erases gaps, so real-ARC ladders likely need perception laddered. Overlaps the queue's "point the loop at real ARC" row (see item 5).
15. **Generate-and-certify ladder generation** — de-risked this week: S-B's arity law lets demo plans guarantee mints by construction; lint+probe are a seconds-scale inner-loop verifier.
16. **Bisection-failure logging** — "what makes an abstraction atomic?" has zero logged instances; they only appear once deep-jump ladders (item 7) are attempted. Design doc §5.1 mandates the logging.
17. **Estimation research** (deferred by AL-PLAN correction #7 — not abandoned): saturation-aware census growth (the forecaster's two 0.04x cells are a diagnosed, fixable gap), lambda-synthesis modelling. Gate: nothing may depend on an estimator that has not survived a 2026-07-23-style validation.
18. **Nonzero learned-vs-oracle gap** — measured 0 on the clean set (perfect recovery); a real measurement needs a deliberately imperfect climb — fold into whichever new ladder first fails recovery honestly.
19. **Stitch-scale proposer + real-corpus `w`** — sleep-probes S-E/S-F scope limits; needed before real-ARC claims.

## Deferred by explicit decision (dormant; pointers only)

- Overlapping/branching ladders — MACHINERY.md.
- Shared-spine per-task caching (F5) — trigger: wall-clock binding (it currently isn't).
- Adaptive sleep triggers (plateau / frequency / online) — MACHINERY.md F4.
- Old-world tree deletion pass — `tests/test_integration.py` locks guard it until then.

## Suggested sequencing (as of this snapshot)

Items 1–4 clear autonomously and close trap doors before new ladders are designed over them (1 and 2 especially). The board's center of gravity is **7 + 8** — nearly everything in `[L]` is gated behind having more ladders — and both need target steer. 5, 6, 10 are quick decisions.
