# AL plan of record (2026-07-23)

Supersedes [AL-PLAN-2026-07-22.md](AL-PLAN-2026-07-22.md) after the estimator validation and the 2026-07-23 alignment session. The design doc ([ABSTRACTION-LADDERS-2026-07-16.md](ABSTRACTION-LADDERS-2026-07-16.md)) stays normative for concepts and metrics **except its §5.3 "estimation is the METHOD" decision, which this file reverses** (see decision 1). Check inventory: [LADDER-CHECKS-2026-07-21.md](LADDER-CHECKS-2026-07-21.md). Register: [LADDERS.md](LADDERS.md).

## Corrections of record

Carried forward from the 2026-07-22 plan (its #1-#6 stand, as amended there), plus:

7. **Search-cost estimation failed, across the board.** We tried to estimate three things and all three came back with error margins too wide to use:
   - **Raw ladder search cost** — the extrapolated bracket contained the measured truth in **2 of 10 cells** at gaps of 1-2 rounds; the batch's real gaps are 4-51 rounds, where the bracket spans up to 36 orders of magnitude ([experiments/2026-07-23-estimator-validation/](../../experiments/2026-07-23-estimator-validation/)).
   - **A rung's cost as a prediction (without running it)** — the forecaster's one-round-ahead projection ranged 0.04x-1.24x across floors (25x under on two of five). Its stronger backtest numbers were near-interpolation on shallow cells and do not generalize.
   - **`b_eff`** — estimates fitted on early rounds do not hold: the ratio accelerates in the growth regime, collapses at semantic saturation (al2's finite D4 group), and drifts with pool size. An overall `b_eff` cannot be estimated well from a prefix of rounds.

   What survives, and what the program now runs on: **actual measurements** (rung costs when cells are run — exact, no estimate needed), **censored lower bounds** (a raw arm that spends without solving proves raw cost exceeds the spend), and **comparisons where raw cancels** (A/B ladders on the same floor+top, the loop-overhead factor, break-even horizon). The one scoped-and-validated predictive fact: round-1 composition counts are exact arithmetic from the typed leaf census (24/24 cells) — useful for attribution and guards, not a substitute for measurement. Estimation itself is **deferred research, not abandoned** — a structural model that handles dedup/saturation would reopen forecast-guided design, but nothing may depend on one until it survives a validation like 2026-07-23's.

## Decisions of record (2026-07-23)

1. **RQ1 is measured-or-bounded; the extrapolated estimate is retired from results.** Definition per design doc §4: `amortization ratio = raw cost / (laddered cost + learning cost)`, so ratio > 1 = the ladder won. The mechanism:

   > Run the raw arm (top task under the Floor) deliberately: `depth_limit` raised toward `d_raw`, pool freed, saturation-checked, with guard = **K x the measured laddered cost** (K >= 1, chosen per ladder). Either it **solves** — RQ1 is a *measured* ratio (<= K) — or it **censors** — RQ1 is a *proven lower bound*: ratio >= K.

   Soundness conditions, both mandatory: the raw arm must not saturate (a pool-starved search idles; its spend stops being evidence — the probe's saturation flag detects this), and every bound is reported with its `(depth_limit, max_pool, spend)` conditions. Until the wake/sleep conversion weight `w` is calibrated (Phase 2), the denominator is the wake-side laddered cost, stated as such. `_estimate_raw_cost` survives only as an advisory diagnostic carrying its measured 2/10 caveats — never in a headline.

2. **Nine ladders retired** — the lint-failers whose failure is a defect: al3, al4, al5, al6, al7, al9, al11, al13, al14. Terminal: no fix-and-rerun under these IDs (rebuilds, e.g. al9/al11 on al17's spine, get new IDs). Rows and `.ladder` files persist — the rejections are findings and the files are lint-lock fixtures. **Not retired**: al8 (kept as the head-to-head cost arm, not a ladder), al10/al12 (controls that fail by design and function correctly).

3. **al2 is retired as the calibration instrument** (its finite-group floor has no growth regime to validate against) — though it remains an admitted, runnable ladder.

## The pipeline (unchanged)

Author/load (`.ladder`) → **lint** (~8s batch) → **rung probe** (~1s/rung) → **oracle chain + certificate** → **LEARN climb + reports**. Gate layering: LADDER-CHECKS.

## Phase 0 — alignment & record (DONE 2026-07-23, this commit)

The framing corrections above, written into the places that carried the old claims: this plan; LADDERS.md (al1's estimated 78x-530x flagged unsupported; al2's validation-arm row resolved; the batch summary's "estimator has a usable range" bullet superseded; retirements applied); the micro-probes and frontier notebooks' overstated predictability lines scoped; EXPERIMENTS.md entries carry dated amendment notes.

## Phase 1 — pipeline

1. **Staged `run_ladder`** — chain + certificate before the climb; a rejected ladder never pays for learning.
2. **Raw-arm spend policy** — decision 1's K-rule wired into the activity: guard derived from the measured laddered cost, saturation-checked, conditions recorded in the report; report computes measured-ratio-or-bound and retires the bracket to advisory.
3. **Wake task-scheduling param** (`full` / `skip-solved` / `oracle-curriculum`) — `LearnSpec` param in run identity, arm-labeled (curriculum cells cannot purport to measure full-wake costs). MACHINERY.md F4 row exists.

## Phase 2 — measurements

4. **More ladders, run clean.** The binding limitation is a count problem: only 8 ladders have ever been admitted with data (al1, al2, al15-al20), al2 is the trivial-regime instrument, and al15-al20 have no committed artifacts. First: re-run the admitted set through the current pipeline (probe-gated, lower-bound raw arms, artifacts written out) → the first trustworthy aggregates. Then grow the set — new ladders must lint clean, probe clean, and produce usable cost data to count.
5. **Sleep-side single-factor attribution** — the learning half has no micro-probes: mint arity determinants, proposer x demonstration-mix effects, what governance refuses, the `w` calibration.
6. **Learned-vs-oracle gap sized** — climbs pay under `L^learned`; all probing is under oracle libraries.

## Phase 3+ — new science

7. **Granularity family** (more smaller rungs vs fewer bigger rungs) — same floor+top, so raw cancels and the comparison is fully measured. The "optimal learning path" question made empirical.
8. **Deep-jump ladders** at freed pools, probe-gated.
9. **Family falsification probes** — perceived-index al14 rebuild (new ID); expensive-perceiver al5 variant; al9/al11 rebuilt on al17's spine.
10. **First HOF / conditional floors** — zero ladder data exists; the `map @ lambda-synthesis` config trap is documented and must be set deliberately.
11. **Bisection-failure logging** — "what makes an abstraction atomic"; zero instances logged.
12. **Generate-and-certify; real-ARC anchoring** — the probe as a generator's inner-loop verifier; cfb2ce5a is the first real-ARC contact.

## Deferred (recorded, not planned)

- Estimation research (correction #7's closing clause): structural forecaster at saturation, lambda-synthesis modelling, dedup-aware census growth.
- Shared-spine per-task caching; wake scheduling beyond item 3 (MACHINERY.md F5/F4 rows).
- `rewrite-shallow` law growth — add when a firing needs them.