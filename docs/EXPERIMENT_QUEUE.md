# Experiment queue

Planned experiments for arc-lab — a **drain-only queue**. This is _state, not events_ (the counterpart to `EXPERIMENTS.md`): it holds what we currently intend to run, and it only shrinks or gets deliberately re-fed.

> **Superseded as the active program (2026-07-23).** The single-task **search-behavior** line this queue was built around (the 2026-07-14 priority spine) has been superseded by the **Abstraction Ladder Experiments**, whose phased plan of record is [docs/abstraction_ladders/AL-PLAN-2026-07-23.md](archive/abstraction_ladders/AL-PLAN-2026-07-23.md) and whose cross-cutting index is [docs/archive/TODO-2026-07-22.md](archive/TODO-2026-07-22.md). **Active experimental work now lives there, not here.** This queue is retained as the **backlog of non-ladder / deferred experiments** — real ideas orthogonal to the ladder program or gated behind it. It only shrinks (run / reject / absorb) or gets a row promoted into the ladder plan.
>
> **Drained in the 2026-07-23 supersession** (the whole former Active section): _extrapolator validation_ → **run** (the estimator failed it; EXPERIMENTS.md 2026-07-23, `experiments/2026-07-23-estimator-validation/`); _control-floor cost concentration_ → **absorbed** into the if-tax micro-probe (EXPERIMENTS.md 2026-07-22 micro-probes, battery C — the `eq`/`if` O(pool²) shape is measured there); _point the loop at real ARC_ → **promoted** to the ladder plan's real-ARC anchoring (AL-PLAN Phase 3+ item 12 / TODO item 11). The remaining former-Active rows moved to Backlog below.

**Discipline** (or this rots like its predecessor):

- **Drain on run / absorb.** When an experiment runs, its `EXPERIMENTS.md` entry is the record — delete the row same session. Rejected or absorbed without running → delete too (record a one-line reason in the supersession note above, or a dead-end EXPERIMENTS entry if the reason is itself a finding).
- **No IDs.** E-numbers are minted at run time, in the log — never here. Rows get names, not numbers.
- **Experiments only.** Machinery-level work is never queued here — `MACHINERY.md` tracks it (Status / Gates). The **Unmet machinery** column links each experiment to the not-yet-built mechanisms that gate it.
- **One row's worth of sizing.** The columns _are_ the sizing frame; full design is still design-time work for the experiment about to run.

---

## Backlog — deferred / non-ladder / downstream

Real ideas, but either orthogonal to the ladder program or gated behind it. Promote a row into [AL-PLAN](archive/abstraction_ladders/AL-PLAN-2026-07-23.md) or [TODO](archive/TODO-2026-07-22.md) when it becomes the active thing to build; otherwise it waits here.

| Experiment | Type | Floor / setup | What it asks | Status vs the ladder program | Unmet machinery |
| --- | --- | --- | --- | --- | --- |
| **learned intermediate type** | study / ladder candidate | `MASK_MIN` (`nonbg_mask`, `crop_to_mask`) | `crop_to_content(g) = crop_to_mask(g, nonbg_mask(g))` — construct AND consume a learned `Mask` type; var-sharing (grid ×2) + abstraction over a non-grid type | **Promotable to a ladder.** A designed-family invention experiment — essentially a Mask-floor ladder. Best realized as a growth-set ladder (TODO item 7); its "learned intermediate type" question is a genuine gap no current ladder exercises | none (Mask + both prims shipped; the engine pools any hashable typed value); needs a generator + spec |
| **size-scaling (cost vs grid size)** | search (handful) | one floor, one target type, ~3 grid sizes (5×5 / 12×12 / 25×25) | how `considered` + pool growth scale with grid size; where `finite-enumerate` explodes (`harvest` vs `finite` contrast) | **Orthogonal, still relevant for real-ARC.** Grid size is not a ladder axis, but real tasks are large and the constant-explosion mechanism matters before real-ARC claims. Gated behind real-ARC anchoring (TODO item 11 / 16) | designed multi-size tasks (`taskgen`) |
| **grain spine (cost-vs-grain curve)** | search (single-task) | `FLOOR → FLOOR_AFFINE → FLOOR_DIV → UNIVERSAL_FLOOR` (pixel lattice), fixed target | the cost-vs-grain _curve_ for one target — where a reflection sits in the cost ranking as the floor's grain changes | **Mostly answered, kept for the curve.** The micro-probes (2026-07-22) measured cost-per-factor at points; this samples the continuous floor-grain curve. Low priority — revisit only if a granularity-family result (TODO item 8) raises a floor-grain question | `search --bundle`; grain-contrast generator (largely exists) |
| **gifting-collapse** | search | gen→full floor pairs (`MASK_MIN→MASK_BASIC`, `D4_GEN_MIN→D4_GEN→D4`, `LAYOUT_GEN→LAYOUT_FULL`), single task each | the search-cost value of each individually gifted primitive | **Feeds the ladder gifting question.** The per-primitive version of what the oracle chain measures per-rung; a component study for deciding floor composition | `search --bundle` |
| **payoff-mirror_index** | learn | `FLOOR_AFFINE`, invent `mirror_index` on a small reflection family | payoff = search-savings − invention-cost, for one concrete idiom | **Absorbed in spirit** by the RQ1 raw-arm machinery (payoff = amortization ratio); kept only if the specific `mirror_index` idiom is wanted as a case study | selection-correct governance (F4) |
| **reusable-idiom governance** | learn | `FLOOR_AFFINE` + `build_grid` | does usefulness-scored selection mint the reusable idiom over larger read-bodies, no type gag (the E8 compression/reusability divergence; fix the `mirror_index` false negative) | **Connects to sleep-side attribution** (TODO item 3 / AL-PLAN Phase 2 item 5) — the governance questions those probe are this row's, generalized | search-aware / selection-correct governance (F4, 🔜) |
| **basis/ablation science** (cluster) | search / study | a complete low floor (`UNIVERSAL_FLOOR` / `MINIMAL_COMPLETE_FLOOR`) | which abstractions invent + how fast · which are hardest · do gifted mid-level primitives speed invention · primitive/machinery ablations | **The ladder program is now this cluster's concrete realization** (RESEARCH-08 axis-2). Kept as the framing; the near-term rows are the ladder growth set + micro-probes | measurement layer (landed) + global completeness (L0 control block) |
| **full-corpus partition census** | search | `SYMMETRY`=`sym` · `MASK_BASIC` · `PERCEIVE_TRANSFORM` | aggregate `by_category` / `by_provenance` across `arc1-train`; new-floor solve counts | **Dormant gauge**, demoted 2026-07-14. A solve-count aggregate, not an object of study; `d4`/`synth` legs already ran (`experiments/2026-07-14-capability-census/`). Run opportunistically only | `search --bundle` for the two new floors |

_Dropped 2026-07-14: **old-suite recalibration** — starting fresh; not preserving the old E2–E10 environments is a deliberate choice, not a loss._
